# 第5章：实战篇

## 本章需要做什么 ？



上一章我们把 Agent Loop 跑起来了。



GuoLaiCode 已经能自主多步干活，但它的「驾驶手册」只有三行，告诉它「你是 GuoLaiCode，一个终端 AI 编程助手」就完了。理论篇里我们看到，同一个模型、同一套工具、同一个 Loop，三行 prompt 和七模块完整 prompt 跑出来是两辆车。



这一章要把这三行展开成一套完整的 Prompt 工程体系。做完之后，GuoLaiCode 的 System Prompt 会有清晰的七模块结构，组装管线会按七源到三通道的规则正确分发信息，Prompt Cache 会真正命中并把每轮的 input token 成本打下来。Plan Mode 的指令也从硬拼接改成走 system-reminder 通道，不再每次都让缓存失效。



具体要新增和重构这些东西：

\- **七模块 System Prompt 组装器&#x20;**：Section 结构体 + Priority 排序，IdentitySection / BehaviorSection / ToolUsageSection / CodeQualitySection / SecuritySection / TaskPatternSection / OutputStyleSection

\- **Prompt 组装管线&#x20;**：assembleAPIPayload 函数把七类信息分发到 system / messages / tools 三通道

\- **环境上下文重构&#x20;**：从 ch04 的 system 通道挪到 messages 通道首条 user 消息，避免污染 cache

\- **工具描述强化&#x20;**：ReadFile / EditFile / WriteFile / Bash / Glob / Grep 的 description 字段补齐用法、优先级、配合关系

\- **Prompt Cache 控制&#x20;**：system 通道整体设 \`cache\_control: ephemeral\`，tools 通道同样设，并从 API 返回 usage 验证命中

\- **system-reminder 注入机制&#x20;**：role=user + \`\<system-reminder>\` XML 标签包裹的消息，注入位置区分会话级与轮次级

\- **Plan Mode 改造&#x20;**：Plan Mode 文本不再拼进 System Prompt，改成按轮次注入 system-reminder（第 1 轮完整版，每 5 轮重复一次）

\- **典型场景评估脚本&#x20;**：5 个定性评估场景，方便每次改 prompt 后做人工对照



这章 **不做&#x20;**：GUOLAICODE.md 项目指令文件加载（章节 7）、自动记忆系统（章节 9）、真实 MCP Server 接入（章节 6）、LLM-as-judge 自动评估管线（依然是人工跑场景对比）。

***

## Vibe Coding 实战

### 生成四份文档

把任务换成本章的内容：

```markdown
# 我的初步想法
这一步的目标是：把原来三行的全局指令展开成结构化的系统提示，让稳定的指令走缓存通道省钱省时间，把环境信息和动态指令通过系统级补充消息注入，让 GuoLaiCode 从「能干活」变成「干得好」。

技术要求：

- 把全局指令按职责拆成若干模块（身份、行为、工具使用、代码规范、安全边界、任务模式、输出风格），按优先级拼装，方便以后插新模块
- 区分稳定内容和变化内容：稳定的指令和工具描述走可缓存通道，环境信息和对话历史走消息通道
- 在工具描述和全局指令里双重强化关键规则，提高模型对「优先用专用工具」「编辑前必先读」这类约定的遵守率
- 用一种带特殊标签的消息形式在运行中注入补充指令，既不污染缓存，也不会被模型当成用户输入来回复
- 会话级开关（如规划模式）的指令按轮次注入，首轮完整、间隔轮次重复、其余精简，控制注入频率
- 通过解析 API 返回的缓存命中字段验证策略是否真生效，再准备一组典型场景做人工对比当定性评估

这一步先不做项目指令文件加载、自动记忆、真实 MCP 接入、自动化评估，这些是后面几章的事。

系统提示结构：

- 七个固定模块按优先级排：身份、系统约束、任务模式、动作执行、工具使用、语气风格、文本输出，最后接环境信息
- 另有几个可选模块排在后面：自定义指令、已激活的 Skill、长期记忆
- 模块之间空行分隔，整体按优先级从前往后拼
```

然后 AI 就会开始问你问题，进行需求澄清。

![]()

你根据理论篇学到的内容回答这些问题，反复循环对齐需求，最后生成四份文档。



### 正式开发

四份文档有了之后，施工图纸定好了，让 Claude Code 根据这四份文档开发

![]()

经过一段时间后，开发完成。

![]()



### 功能验证过程

来验收一下结果



我们先来看看「identify」层

![]()

可以看到，这里会告诉LLM他是谁，那么我们启动GuoLaiCode，在没有任何我们去提示上下文的情况下，输入

![]()

可以看到我们的身份是注入成功的，LLM已经知道自己是谁



「环境」层信息注入

![]()

这个其实我们上面那张图也展示出来了

![]()



「system」层这里，我们能看到是这样的信息

![]()

在GuoLaiCode输入

![]()

我们可以看到，它已经能根据system提示词的指引去回答问题了



「DoingTasks」层，提示词这样

![]()

我们打开GuoLaiCode，输入

![]()

我们能看到这里是能对应上的，这层没问题



「ExecutingActions」层，提示词如下

![]()

打开GuoLaiCode，输入

![]()

也是对照着我们的提示词的，这块没问题



「UsingTools」层，提示词是这样的

![]()

我们输入

![]()

跟提示词对得上，这块没问题



「ToneStyle」层，提示词如下

![]()

我们在GuoLaiCode，输入

![]()

可以看到，这层也遵守了，没问题



「TextOutput」层，提示词如下

![]()

我们打开GuoLaiCode，输入

![]()

可看到，最后这层也没问题，LLM都收到和根据提示词工作了



验收没问题，那么本章的主要任务就完成了。

***

## 参考提示词和代码

如果你在澄清需求的过程中遇到困难，或者生成的四份文件效果不理想，可以直接使用下面的参考版本。

把下面四个文件保存到项目根目录，然后告诉你的 AI 编程助手（在 `[你的语言]` 处填入你使用的编程语言）：

### Go

```markdown
# 系统提示工程化 Spec## 背景ch04 给 GuoLaiCode 装上了 Agent Loop——能自主多轮想 → 调工具 → 看结果地干活。但「**干活的指令**」还很原始:系统提示是一段揉在一起的固定常量(身份、工具用法、简洁性挤在同一块文本里),没有结构、没有优先级、想加一类新指令只能往字符串里塞;每轮请求都把整段系统提示与全部工具定义**原样重发**,稳定不变的内容反复计费、反复占用首字延迟;模型对**当前环境**(工作目录、平台、日期、git 状态)一无所知;规划模式提醒每轮无差别地拼在系统提示尾部,注入频率不可控。

ch05 给 GuoLaiCode 做**系统提示工程化**:把全局指令拆成按职责划分、按优先级拼装的模块;把稳定内容与变化内容分到**可缓存通道**与**消息通道**,让稳定的指令和工具定义走缓存省钱省首字延迟;引入一种带特殊标签的**补充消息注入**机制,在运行中注入指令而不污染缓存、不被模型当成用户提问来回复;会话级开关(规划模式)的提醒按**轮次频率**注入。这是从「能干活」到「干得好」的一步。

## 目标

- 把全局指令拆成按职责划分的若干模块,按优先级拼装成完整系统提示;新增一类指令只需挂载新模块,不改装配逻辑。
- 区分稳定与变化内容:稳定的系统模块与工具定义走**可缓存通道**;环境信息与对话历史走**不缓存的通道**。
- 在工具描述与系统提示中,对关键约定(优先用专用工具、编辑前必先读)**双重强化**,提高遵守率。
- 提供一种带特殊标签的**补充消息注入**机制:运行中注入补充指令,既不污染缓存,也不会被模型当成用户输入来回复。
- 会话级开关(规划模式)的提醒按轮次注入:**首轮完整、间隔轮次重复完整、其余轮次精简**,控制注入频率。
- 能从 provider 返回中**解析缓存命中信息**(写入 / 读取 token),用于验证缓存策略真实生效。

## 功能需求

- F1: 模块化系统提示拼装
  把全局指令按职责拆成模块,每个模块带名称、优先级、内容。装配时按优先级从高到低拼接成完整系统提示,模块间以空行分隔。固定模块七个,按优先级排:**身份 → 系统约束 → 任务模式 → 动作执行 → 工具使用 → 语气风格 → 文本输出**。其后预留三个可选模块位置:**自定义指令、已激活 Skill、长期记忆**——本章这三个**不接入真实内容来源**(留待后续章节),装配时内容为空则跳过、不留多余空行。新增一类指令只需定义新模块挂到对应优先级,不改装配主逻辑。

- F2: 环境信息构造与呈现
  收集运行环境信息(工作目录、操作系统/平台、当前日期、git 状态、应用版本、当前模型等),构造成一段供模型感知环境的文本。环境信息作为系统提示的**独立第二段**呈现——结构上接在固定模块之后,物理上与可缓存的稳定模块**分属不同内容块**(见 F3),自身不进缓存。

- F3: 稳定内容走可缓存通道
  稳定不变的内容(拼装好的稳定系统模块 + 工具定义)归入**可缓存通道**,变化内容(环境信息 + 对话历史)归入**不缓存通道**。可缓存内容必须**跨轮逐字节一致**(不含任何随轮次变化的成分),否则缓存无法命中。Anthropic 对稳定内容显式标记缓存断点,使工具定义与稳定系统模块被缓存复用,环境信息块与历史在断点之后不缓存;OpenAI 把稳定内容置于请求前缀,靠端点前缀缓存自动受益(不强制端点支持)。

- F4: 缓存命中解析
  从 provider 响应用量中解析缓存字段——**缓存写入**(首次创建缓存的 token)与**缓存读取**(命中复用的 token),随用量一并对外暴露,供烟囱/调试程序打印验证缓存是否真生效。Anthropic 解析其缓存创建/读取字段;OpenAI 解析其缓存命中字段(端点提供则有,否则为零)。

- F5: 关键约定双重强化
  对模型最易违反的关键约定(如「优先用专用工具而非 shell 拼凑」「编辑文件前必先读取」),在**工具描述**与**系统提示模块**中双重表述,提高遵守率。

- F6: 补充消息注入机制(system-reminder)
  提供一种带特殊标签(如 `<system-reminder>`)的补充消息注入形式:构造请求时把补充指令注入**消息通道**,标签语义让模型理解这是系统补充上下文而非用户提问——不针对它直接回复。此类消息**每轮动态构造、不写入持久历史**,因而不污染缓存、不破坏历史角色交替。

- F7: 规划模式按轮次注入
  规划模式提醒改由 F6 机制承载(不再拼到系统提示尾部)。按 Agent Loop 轮次控制注入**频率与详略**:**首轮注入完整提醒,之后每隔固定轮次重复一次完整提醒,其余轮次注入精简版**。模式跨轮保持,`/plan` 进入、`/do` 退出并触发按计划执行;规划模式下仍仅放开只读工具(均沿用 ch04)。

- F8: 跨协议一致
  Anthropic 与 OpenAI(含兼容 base_url)都装配同一套模块化系统提示 + 环境信息、都注入相同补充消息、都尽力走缓存(Anthropic 显式断点 / OpenAI 前缀顺序)。两协议在模块内容、注入时机、规划模式注入节奏上**行为一致**;缓存命中解析在各自字段可得时生效。

## 非功能需求- N1: 缓存确定性——可缓存块(工具定义 + 稳定系统模块)在跨轮、会话内多轮之间**逐字节稳定**:模块装配顺序、工具顺序固定,不混入任何随轮次/时间变化的成分。否则缓存永不命中,本章核心目标落空。
- N2: 不破坏 ch04 行为——Agent Loop 多轮编排、流式双路收集、保序分批并发、用户取消、历史一致、用量/迭代进度展示等 ch04 能力**不退化**。系统提示重构与消息注入是叠加,不改这些既有路径的语义。
- N3: 历史合法性——每轮动态注入的补充消息(不持久)拼进请求后,消息序列仍**角色交替合法、工具调用与结果配对不破坏**,不致 provider 返回 400;补充消息不写入持久历史,故不影响后续轮次与可恢复性。
- N4: 环境采集不阻塞、可降级——环境信息采集(含可能外调 shell 的 git 状态)**快速且有界**,不阻塞界面、不显著拖慢请求发起;任一项取不到时降级(留空/省略),不中断会话。
- N5: 密钥与敏感信息不泄漏——沿用 ch03/ch04,对话区与任何输出均不出现 api_key;环境信息构造**不带入密钥或敏感环境变量**。
- N6: 缓存字段解析健壮——provider / 兼容端点未返回缓存字段时按零处理,不 panic、不因缺字段中断本轮或会话。
- N7: 代码规范——gofmt / goimports 合规(本地包与第三方包分组正确)、`go vet ./...` 无告警(遵循项目 CLAUDE.md)。
- N8: 可扩展性——新增一类系统提示模块或一类补充消息提醒时,改动局部:不触及装配主逻辑,不改 provider 适配层对外签名的语义稳定性。

## 不做的事- **项目指令文件加载**——「自定义指令」为预留空槽,本章不从 CLAUDE.md / 项目级指令文件读取内容(留待后续章节)。
- **自动记忆 / 长期记忆**——「长期记忆」为预留空槽,本章不做记忆写入与召回。
- **真实 MCP 接入**——「已激活 Skill」等可选模块为预留空槽,不接入 MCP 工具/资源。
- **自动化评估 / 评测框架**——本章只做缓存字段解析验证 + 一组典型场景人工对比的定性评估,不建评测流水线。
- **历史增量缓存**——只缓存稳定前缀(工具定义 + 稳定系统模块),不对对话历史另打缓存断点。
- **缓存 TTL / 断点数配置化**——TTL 用默认值、断点位置内置,本章不通过配置调整。
- **状态栏展示缓存命中**——缓存信息只走 smoke / 调试输出,不进 TUI 状态栏。
- **提示词运行时热加载 / 外部模板文件**——模块内容内置于代码,不做外部模板。
- **多模态、流式工具结果**——沿用 ch04,不做。

## 验收标准

- AC1: 模块化装配——系统提示由按优先级排列的固定模块拼成,模块间以空行分隔;新增一个模块只需挂载、不改装配逻辑。(F1)
- AC2: 可选空槽——「自定义指令/已激活 Skill/长期记忆」内容为空时装配自动跳过、不留多余空行;架构支持后续填充。(F1)
- AC3: 环境信息呈现——系统提示第二段含工作目录、平台、当前日期、git 状态、应用版本、当前模型;与稳定模块分属不同内容块。(F2)
- AC4: 缓存命中(Anthropic)——首轮 `cache_creation_input_tokens > 0`、次轮 `cache_read_input_tokens > 0`(稳定前缀命中);仅环境信息/历史变化时稳定前缀仍命中。(F3/N1)
- AC5: 缓存确定性——两次构造的稳定块逐字节相等;故意改变环境信息不改变稳定块内容。(F3/N1)
- AC6: 缓存字段解析——provider 用量对外暴露缓存写/读;OpenAI 解析 `cached_tokens`,端点不返回时为 0 且不报错。(F4/N6)
- AC7: 双重强化——关键约定(优先用专用工具、编辑前必先读)在**工具描述**与**系统提示模块**文本中均出现。(F5)
- AC8: 补充消息注入——注入消息以 `<system-reminder>` 标签包裹,模型不把它当用户提问来复述/回应;该消息**不写入持久对话历史**。(F6/N3)
- AC9: 规划模式按轮次注入——`/plan` 后:首轮注入完整提醒、之后每隔固定轮次重复完整、其余轮次注入精简版;规划模式仅放开只读工具;`/do` 切回全工具并按上文计划执行。(F7)
- AC10: 跨协议一致——anthropic 与 openai(含兼容 base_url)都装配同一系统提示 + 环境信息、注入同一 reminder、都尽力缓存(anthropic 看缓存命中、openai 看 `cached_tokens`),行为一致。(F8)
- AC11: 不破坏 ch04——ch04 关键行为(多轮连环、用户取消、历史一致、流出错恢复)仍成立;`go test ./...` 通过。(N2)
- AC12: 历史合法——注入 reminder 后请求不返回 400;规划模式多轮 + 中途取消后再发一条仍可继续(历史配对合法)。(N3)
- AC13: 环境采集降级——在非 git 目录或 git 不可用时,环境段对应项降级(留空/省略),界面不卡、请求正常发起。(N4)
- AC14: 代码规范——`gofmt -l .` 无输出、`go vet ./...` 无告警;对话区与输出无 api_key 明文。(N5/N7)
```

````markdown
# 系统提示工程化 Plan

> 技术栈:Go;anthropic-sdk-go v1.46.0、openai-go/v3 v3.37.0。所有 SDK 缓存 API 已对 vendored 源码核实(见技术决策表)。

## 架构概览ch05 在三层叠加,**不改 ch04 的 Agent Loop 控制流**:

- **prompt 包(重写)**:从「单个常量字符串」升级为「模块化装配 + 环境采集 + 补充消息构造」。对外产出三类文本——**稳定系统提示**(可缓存)、**环境信息段**(不缓存)、**system-reminder 包裹的补充指令**。prompt 包不依赖 llm 包(避免环依赖)。
- **llm 包(改造)**:`Provider.Stream` 入参从位置参数改为 `Request` 结构体,承载 `Messages / Tools / System{Stable,Environment} / Reminder`。Anthropic 把 Stable 块打缓存断点、Env 块不打;OpenAI 把 Stable 置于系统消息前缀。`Usage` 增加缓存写/读字段。两 provider 把 `Reminder` 按各自协议安全地织入消息通道(N3)。
- **agent 包(改造)**:每次 `Run` 开始采集环境、装配稳定系统提示;每轮迭代按 `mode + iter` 计算本轮 reminder(规划模式按轮次详略),组装 `Request` 发起请求;把缓存用量透传到 `Event.Usage`。
- **smoke(改造)**:打印每轮用量的缓存写/读字段,作为缓存策略生效的验证手段(TUI 状态栏不变)。

数据流:`agent.Run` → `prompt.BuildSystemPrompt()`(稳定) + `prompt.GatherEnvironment().Render()`(环境) + `prompt.PlanReminder(full)`(本轮补充) → 组装 `llm.Request` → `provider.Stream` → Anthropic/OpenAI 各自装配缓存通道与消息通道 → 流式事件回到 agent → `Event{Usage:{...CacheWrite,CacheRead}}` → smoke 打印。

## 核心数据结构### prompt.Module(新增)
```go
type Module struct {
    Name     string // 模块标识(身份、系统约束 …),仅用于可读性与测试断言
    Priority int    // 数值越小优先级越高、排越前;固定模块 10..70,可选模块 80..100
    Content  string // 模块正文;为空则装配时跳过(可选空槽)
}
```

### prompt.Environment(新增)
```go
type Environment struct {
    WorkingDir string // os.Getwd()
    Platform   string // runtime.GOOS
    Date       string // time.Now().Format("2006-01-02")
    GitStatus  string // `git status --porcelain` 摘要;非 git 目录/取不到则留空
    Version    string // 应用版本(从 agent 透传)
    Model      string // provider.Model()
}
```

### llm.System(新增)
```go
type System struct {
    Stable      string // 可缓存:装配好的稳定系统提示(工具定义随 tools 一并进缓存前缀)
    Environment string // 不缓存:环境信息段
}
```

### llm.Request(新增,替换 Stream 位置参数)
```go
type Request struct {
    Messages []Message        // 持久对话历史(不含本轮 reminder)
    Tools    []ToolDefinition // 本轮工具集(普通=全量 / 规划=只读)
    System   System           // 稳定系统提示 + 环境段
    Reminder string           // 本轮 system-reminder 内容(已含标签;空=不注入)
}
```

### llm.Usage(扩展)
```go
type Usage struct {
    InputTokens  int64
    OutputTokens int64
    CacheWrite   int64 // Anthropic: cache_creation_input_tokens;OpenAI: 恒 0(自动缓存无写计数)
    CacheRead    int64 // Anthropic: cache_read_input_tokens;OpenAI: prompt_tokens_details.cached_tokens
}
```

### agent.Usage(扩展,对外事件)
```go
type Usage struct {
    Input, Output       int64
    CacheWrite, CacheRead int64 // 透传自 llm.Usage,供 smoke 打印
}
```

## 核心接口### prompt 包
```go
func FixedModules() []Module                 // 七个固定模块(身份…文本输出),内容内置
func OptionalModules() []Module              // 三个可选空槽(自定义指令/已激活Skill/长期记忆),Content=""
func AssembleSystem(mods []Module) string    // 按 Priority 升序、跳过空 Content、以 "\n\n" 连接
func BuildSystemPrompt() string              // = AssembleSystem(append(FixedModules(), OptionalModules()...))
func GatherEnvironment(version, model string) Environment // 采集环境;git/date 失败降级留空
func (Environment) Render() string           // 渲染为「环境信息」第二段文本
func SystemReminder(body string) string      // 用 <system-reminder>…</system-reminder> 包裹 body
func PlanReminder(full bool) string          // 返回包好标签的规划模式提醒(full=完整 / 否=精简)
```

### llm.Provider(签名变更)
```go
type Provider interface {
    Name() string
    Model() string
    Stream(ctx context.Context, req Request) <-chan StreamEvent // 由 Request 承载全部入参
}
```

## 模块设计### prompt 包
**职责:** 模块化装配稳定系统提示;采集并渲染环境信息;构造 system-reminder 与规划模式提醒。
**对外接口:** 见上。
**关键点:**
- 七个固定模块按优先级排:**身份(10) → 系统约束(20) → 任务模式(30) → 动作执行(40) → 工具使用(50) → 语气风格(60) → 文本输出(70)**;可选空槽:**自定义指令(80) → 已激活 Skill(90) → 长期记忆(100)**(Content="" 跳过)。
- **F5 双重强化**写在「工具使用(50)」模块:明确「优先用专用工具(read_file/glob/grep)而非用 bash 拼凑」「编辑文件前必须先 read_file 读取」;同时同义强化到 `edit_file`、`bash` 工具描述(见 tool 包改动)。
- `AssembleSystem` 只用常量内容 → 跨轮逐字节一致(**N1**);环境与时间相关内容只进 `Environment`,绝不进稳定模块。
- `GatherEnvironment`:git 状态用一条 `git status --porcelain` 带短超时执行,失败/非 git 目录则 `GitStatus=""`;不读取任何环境变量(**N5**)。
**依赖:** 标准库(os/runtime/time/os-exec);不依赖 llm。

### llm 包(provider.go / anthropic.go / openai.go)
**职责:** 把 `Request` 装配为各协议请求,分离缓存通道与消息通道,解析缓存用量,安全织入 reminder。
**对外接口:** `Stream(ctx, Request)`。
**关键点:**
- 删除 `effectiveSystem` 与对 `prompt` 包的 import(系统提示改由 agent 传入)。
- **Anthropic**:`System = []TextBlockParam{}`;`Stable` 非空 → `{Text:Stable, CacheControl: anthropic.NewCacheControlEphemeralParam()}`(断点,默认 5m TTL；**必须用构造器**——空字面量 `CacheControlEphemeralParam{}` 因 `json:"cache_control,omitzero"` 被判零值丢弃、断点不会发出);`Environment` 非空 → `{Text:Environment}`(无 CacheControl)。请求顺序 tools→system→messages,断点打在稳定块 → **缓存前缀 = 全部工具 + 稳定块**;env 与历史在断点后不缓存,env 每轮变化不影响前缀命中。`Usage.CacheWrite=acc.Usage.CacheCreationInputTokens`、`CacheRead=acc.Usage.CacheReadInputTokens`。
  - reminder 织入:`req.Reminder!=""` 时,把 `NewTextBlock(reminder)` **追加到最后一条消息的 content 块**(循环中最后一条恒为 user 或 tool_result→user,追加文本块仍是合法 user 消息,保 N3 角色交替);极端情形(末尾为 assistant)则新起一条 user 消息。
- **OpenAI**:系统消息 = `Stable`(若 `Environment!=""` 则拼为 `Stable + "\n\n" + Environment` 单条 system 消息——兼容端点对多条 system 消息支持不一,统一单条);`Stable` 居前缀 → 端点前缀缓存命中稳定部分。`Usage.CacheRead=acc.Usage.PromptTokensDetails.CachedTokens`、`CacheWrite=0`。
  - reminder 织入:`req.Reminder!=""` 时**追加一条尾部 user 消息**(OpenAI 容忍连续 user / tool 后接 user)。
- **N6**:缓存字段缺失即零值,不额外校验、不报错。

### agent 包(agent.go)
**职责:** 采集环境、装配系统提示、按轮次构造 reminder、组装 Request、透传缓存用量。
**关键点:**
- `New(p, r, version)` 增加 `version` 字段(供环境段);`Model` 取 `p.Model()`。
- `Run` 起始:`env := prompt.GatherEnvironment(a.version, a.provider.Model())`、`sys := prompt.BuildSystemPrompt()`(稳定,普通/规划模式一致——规划提醒已移出系统通道)。
- 每轮迭代计算 reminder:`mode==ModePlan` → `prompt.PlanReminder(full)`,`full = (iter==1 || (iter-1)%planReminderInterval==0)`;否则 `""`。`planReminderInterval=4`(内置常量)。
- `streamOnce` 组装 `llm.Request{Messages:conv.Messages(), Tools:defs, System:llm.System{Stable:sys, Environment:env.Render()}, Reminder:reminder}` 调 `provider.Stream`。
- 删除 `suffix`/`ReadOnlyDefinitions` 的「系统后缀」用法;**只读工具集仍按 mode 选择**(规划=`ReadOnlyDefinitions`),`PlanModeReminder` 常量从系统后缀迁移为 `prompt.PlanReminder` 的内容。
- 缓存用量透传:`Event{Usage:&Usage{Input,Output,CacheWrite,CacheRead}}`。

### smoke(cmd/smoke/main.go)
**职责:** 端到端验证缓存生效。
**关键点:** 消费 `Event.Usage` 时打印 `input/output/cache_write/cache_read`;跑两轮(或多轮)观察次轮 `cache_read>0`。`agent.New` 传一个版本字面量(如 `"dev"`)。

### tool 包(描述微调,F5)
- `edit_file.Description`:补「编辑前请先用 read_file 读取目标文件,确认 old_string 唯一」。
- `bash.Description`:补「读文件/找文件/搜内容请优先用 read_file/glob/grep,不要用 bash 拼凑」。
- 仅改描述文本,不改行为、不改 schema(N2)。

## 模块交互

```
TUI/smoke ─Run(ctx,conv,mode)→ agent
  agent.Run:
    env  = prompt.GatherEnvironment(version, provider.Model())
    sys  = prompt.BuildSystemPrompt()
    for iter:
      reminder = (mode==Plan) ? prompt.PlanReminder(full(iter)) : ""
      req = llm.Request{conv.Messages(), defs(mode), llm.System{sys, env.Render()}, reminder}
      provider.Stream(ctx, req) ──→ StreamEvent{Text/ToolCalls/Usage(+cache)/Done/Err}
    Event{Usage:{...CacheWrite,CacheRead}} ──→ smoke 打印 / TUI 状态栏(忽略 cache 字段)
```

依赖方向(无环):`agent → {prompt, llm, conversation, tool}`;`llm → config`(不再 import prompt);`prompt → 标准库`。

## 文件组织

```
guolaicode/
├── internal/prompt/
│   ├── prompt.go        — 改:Module/装配/BuildSystemPrompt;保留 banner(CatBanner/RenderBanner/ReadyHint)
│   ├── modules.go       — 新:FixedModules()/OptionalModules() 七固定+三空槽的内容常量
│   ├── environment.go   — 新:Environment/GatherEnvironment/Render
│   ├── reminder.go      — 新:SystemReminder/PlanReminder(完整版/精简版常量)
│   └── prompt_test.go   — 新:装配顺序/跳空槽/N1 确定性/双重强化文本 断言
├── internal/llm/
│   ├── provider.go      — 改:Request/System 结构;Usage 加缓存字段;Provider.Stream 签名;删 effectiveSystem
│   ├── anthropic.go     — 改:两块 System(断点+env)、缓存用量解析、reminder 织入
│   └── openai.go        — 改:单条 system(Stable+env)、cached_tokens 解析、reminder 尾部注入
├── internal/agent/
│   ├── agent.go         — 改:New(+version)、Run 采集环境/装配系统、按轮次 reminder、缓存透传
│   └── agent_test.go    — 改/新:断言 Request 装配(System 两段、规划按轮次 reminder)、缓存用量透传
├── internal/tool/
│   ├── edit_file.go     — 改:Description 补强化
│   └── bash.go          — 改:Description 补强化
├── internal/tui/
│   └── stream.go        — 改:agent.New 传 version(m.version 已有)
└── cmd/smoke/main.go    — 改:打印缓存用量;agent.New 传版本字面量
```

## 技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 系统提示组织 | 模块化(Module{Name,Priority,Content} + AssembleSystem) | 满足 F1「挂载即扩展」;优先级排序使顺序确定(N1) |
| 环境信息归属 | system 通道独立第二块(用户拍板) | 结构上接系统提示之后;物理上与稳定块分离,不进缓存 |
| Anthropic 缓存断点 | 仅在稳定 system 块打 `CacheControl`(默认 5m) | 请求序 tools→system→messages,断点在稳定块即缓存「工具+稳定块」整段前缀;env 在其后不缓存,env 变化不冲前缀命中(源码核实:TextBlockParam.CacheControl) |
| 工具是否单独打断点 | 否 | 稳定块断点的前缀已含全部工具,无需 ToolParam.CacheControl 再标 |
| OpenAI 环境信息 | 拼入单条 system 消息(Stable 在前) | 兼容端点对多条 system 支持不一;Stable 居前缀,端点前缀缓存自动命中稳定部分。代价:env 居 system 尾,OpenAI 工具可能不进缓存前缀——本章 OpenAI 缓存为尽力而为、不强制(F8) |
| 缓存用量字段 | Usage 加 CacheWrite/CacheRead | Anthropic 取 cache_creation/cache_read;OpenAI 取 prompt_tokens_details.cached_tokens(源码核实) |
| Stream 入参 | 改 `Request` 结构体 | 入参从 4 个增至含 System/Reminder,结构体更清晰、后续扩展不再改签名(N8) |
| reminder 注入位置 | Anthropic 并入末条 user 消息 content 块;OpenAI 追加尾部 user 消息 | Anthropic 严格角色交替——并入避免连续 user 触发 400(N3);OpenAI 容忍连续 user |
| reminder 持久化 | 不写入 conversation(用户拍板) | 每轮动态构造;不污染缓存、不破坏历史可恢复性 |
| 规划提醒节奏 | iter==1 或 (iter-1)%4==0 → 完整,否则精简(per Run 内 iter) | 实现 F7「首轮完整、间隔重复、其余精简」;复用已有 iter 计数 |
| 缓存验证呈现 | smoke/调试打印(用户拍板) | 不动 TUI 状态栏;Usage 携带字段供打印 |
| prompt↔llm 依赖 | 系统提示由 agent 传入,llm 不再 import prompt | 打破潜在环依赖;职责更清晰 |
````

````markdown
# 系统提示工程化 Tasks## 文件清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `internal/prompt/modules.go` | `Module` 类型;`FixedModules()` 七固定模块、`OptionalModules()` 三空槽的内容常量 |
| 改   | `internal/prompt/prompt.go` | `AssembleSystem`/`BuildSystemPrompt`;删旧 `SystemPrompt`/`PlanModeReminder`/`ExecuteDirective` 常量(迁移);保留 banner |
| 新建 | `internal/prompt/environment.go` | `Environment` 结构、`GatherEnvironment`、`Render` |
| 新建 | `internal/prompt/reminder.go` | `SystemReminder` 标签包裹、`PlanReminder(full)`、规划提醒完整/精简常量、`ExecuteDirective` |
| 新建 | `internal/prompt/prompt_test.go` | 装配顺序、跳空槽、N1 确定性、双重强化文本断言 |
| 改   | `internal/tool/edit_file.go` | `Description` 补「编辑前先 read_file」 |
| 改   | `internal/tool/bash.go` | `Description` 补「优先用专用工具而非 bash 拼凑」 |
| 改   | `internal/llm/provider.go` | `System`/`Request` 结构;`Usage` 加缓存字段;`Provider.Stream(ctx, Request)`;删 `effectiveSystem` 及 prompt import |
| 改   | `internal/llm/anthropic.go` | 两块 System(稳定块打断点 + env 块)、缓存用量解析、reminder 并入末条 user |
| 改   | `internal/llm/openai.go` | 单条 system(Stable+env 拼接)、`cached_tokens` 解析、reminder 追加尾部 user |
| 改   | `internal/agent/agent.go` | `New(+version)`;`Run` 采集环境/装配系统;按轮次 reminder;缓存用量透传 |
| 改   | `internal/agent/agent_test.go` | 断言 Request 装配(System 两段、规划按轮次 reminder)、缓存用量透传;修既有用例适配新签名 |
| 改   | `internal/tui/stream.go` | `agent.New` 传 `m.version` |
| 改   | `cmd/smoke/main.go` | 打印缓存用量;`agent.New` 传版本字面量 |

---

## T1: prompt 模块化装配**文件:** `internal/prompt/modules.go`、`internal/prompt/prompt.go`
**依赖:** 无
**步骤:**
1. 在 `modules.go` 定义 `Module struct { Name string; Priority int; Content string }`。
2. `FixedModules() []Module` 返回七个固定模块,内容内置(中英按现有 SystemPrompt 风格,英文为主):
   - 身份(10):GuoLaiCode 是终端编码 Agent。
   - 系统约束(20):操作边界——在工作目录约定内行事、不外泄密钥、对破坏性操作谨慎。
   - 任务模式(30):ReAct——多步推进、读后再改、完成才给终答。
   - 动作执行(40):何时调工具、连续只读可并发、有副作用谨慎。
   - 工具使用(50):**优先用 read_file/glob/grep 而非 bash 拼凑;编辑文件前必先 read_file**(F5)。
   - 语气风格(60):简洁、直接、不奉承。
   - 文本输出(70):必要时用 Markdown(代码块/列表),终答精炼。
3. `OptionalModules() []Module` 返回三个空槽:自定义指令(80)、已激活 Skill(90)、长期记忆(100),`Content` 均为 `""`。
4. 在 `prompt.go`:
   - `AssembleSystem(mods []Module) string`:按 `Priority` 升序稳定排序、**跳过 `Content==""`**、以 `"\n\n"` 连接。
   - `BuildSystemPrompt() string`:`AssembleSystem(append(FixedModules(), OptionalModules()...))`。
   - 删除旧 `SystemPrompt`、`PlanModeReminder` 常量(内容迁至模块/reminder);`ExecuteDirective` 迁至 `reminder.go`。保留 `CatBanner`/`ReadyHint`/`RenderBanner`。

**验证:** `go build ./internal/prompt/...` 通过;临时 `fmt.Println(BuildSystemPrompt())` 观察七模块按序、空槽不留空行。

## T2: 环境采集与渲染**文件:** `internal/prompt/environment.go`
**依赖:** 无
**步骤:**
1. 定义 `Environment struct { WorkingDir, Platform, Date, GitStatus, Version, Model string }`。
2. `GatherEnvironment(version, model string) Environment`:
   - `WorkingDir = os.Getwd()`(失败留空)、`Platform = runtime.GOOS`、`Date = time.Now().Format("2006-01-02")`。
   - `GitStatus`:用 `exec.CommandContext` 加 ~2s 超时跑 `git status --porcelain`;非零退出/非 git 目录/超时 → `""`;有输出则取摘要(如「N 个文件改动」或前几行)。
   - `Version=version`、`Model=model`。**不读任何环境变量**(N5)。
3. `(e Environment) Render() string`:渲染为「环境信息」段——逐行 `Key: Value`,空值项省略。

**验证:** 单测在临时非 git 目录 `GatherEnvironment` 得 `GitStatus==""` 且不 panic;`Render()` 含 cwd/platform/date。

## T3: 补充消息与规划提醒构造**文件:** `internal/prompt/reminder.go`
**依赖:** 无
**步骤:**
1. `SystemReminder(body string) string`:返回 `"<system-reminder>\n" + body + "\n</system-reminder>"`。
2. 规划提醒常量:`planReminderFull`(完整版,含「仅可用只读工具调研、产出分步计划、等 /do 批准」)、`planReminderConcise`(精简版,一两句)。
3. `PlanReminder(full bool) string`:`SystemReminder(full?planReminderFull:planReminderConcise)`。
4. `ExecuteDirective`(从 prompt.go 迁来):`/do` 注入的用户消息文案。

**验证:** 单测断言 `PlanReminder(true)` 含 `<system-reminder>` 与完整文案;`PlanReminder(false)` 用精简文案。

## T4: prompt 单测**文件:** `internal/prompt/prompt_test.go`
**依赖:** T1, T2, T3
**步骤:**
1. 装配顺序:断言 `BuildSystemPrompt()` 中身份段出现在工具使用段之前;模块以空行分隔。
2. 跳空槽:在 `AssembleSystem` 传入含空 `Content` 的模块,断言其不出现、不产生连续多空行。
3. **N1 确定性**:连续两次 `BuildSystemPrompt()` 结果 `==`。
4. **F5 双重强化**:`BuildSystemPrompt()` 文本含「编辑」与「先读」之意、含「优先」与专用工具名。
5. 环境与 reminder:见 T2/T3 验证。

**验证:** `go test ./internal/prompt/...` 通过。

## T5: 工具描述双重强化**文件:** `internal/tool/edit_file.go`、`internal/tool/bash.go`
**依赖:** 无
**步骤:**
1. `edit_file.Description` 末补:「编辑前请先用 read_file 读取目标文件,确认 old_string 唯一。」
2. `bash.Description` 末补:「读文件、找文件、搜内容请优先用 read_file/glob/grep,不要用 bash 拼凑。」
3. 不改 schema、不改 Execute 行为。

**验证:** `go build ./internal/tool/...`;`go test ./internal/tool/...` 仍通过。

## T6: llm 接口改造**文件:** `internal/llm/provider.go`
**依赖:** 无(但 T7/T8/T9 依赖本任务)
**步骤:**
1. 新增 `System struct { Stable, Environment string }`、`Request struct { Messages []Message; Tools []ToolDefinition; System System; Reminder string }`。
2. `Usage` 加 `CacheWrite int64`、`CacheRead int64`。
3. `Provider.Stream` 改为 `Stream(ctx context.Context, req Request) <-chan StreamEvent`;更新接口文档注释。
4. 删除 `effectiveSystem` 函数与对 `guolaicode/internal/prompt` 的 import。
5. `trySend`/`New` 保持。

**验证:** `go build ./internal/llm/...` 报 anthropic.go/openai.go 未适配的编译错(预期),T7/T8 修复。

## T7: Anthropic 适配缓存通道 + reminder**文件:** `internal/llm/anthropic.go`
**依赖:** T6
**步骤:**
1. `Stream(ctx, req Request)`:
   - 构造 `System []anthropic.TextBlockParam`:`req.System.Stable!=""` → `{Text:Stable, CacheControl: anthropic.NewCacheControlEphemeralParam()}`（**必须用构造器**，空字面量会被 omitzero 丢弃）;`req.System.Environment!=""` → `{Text:Environment}`(无 CacheControl)。
   - `Messages = toAnthropicMessages(req.Messages)`;`req.Reminder!=""` → 调 `appendReminderAnthropic(msgs, req.Reminder)`:把 `anthropic.NewTextBlock(reminder)` 追加到**最后一条消息**的 content;末条非 user 时新起一条 user 消息。
   - `Tools = toAnthropicTools(req.Tools)`(不另打断点)。
   - thinking 逻辑沿用(`assistantUsedTools(req.Messages)`)。
2. Usage 解析:`StreamEvent{Usage:&Usage{InputTokens:acc.Usage.InputTokens, OutputTokens:acc.Usage.OutputTokens, CacheWrite:acc.Usage.CacheCreationInputTokens, CacheRead:acc.Usage.CacheReadInputTokens}}`。

**验证:** `go build ./internal/llm/...` 通过(配合 T8);单测/烟囱 anthropic 跑两轮见 `CacheRead>0`(次轮)。

## T8: OpenAI 适配缓存通道 + reminder**文件:** `internal/llm/openai.go`
**依赖:** T6
**步骤:**
1. `toOpenAIMessages(req)`:首条 system 消息 = `req.System.Stable`(若 `Environment!=""` 拼为 `Stable+"\n\n"+Environment`);随后映射历史;`req.Reminder!=""` → 追加一条尾部 `openai.UserMessage(req.Reminder)`。
2. `Stream(ctx, req Request)` 改用 `req`;`params.Tools = toOpenAITools(req.Tools)`。
3. Usage 解析:`CacheRead = acc.Usage.PromptTokensDetails.CachedTokens`、`CacheWrite = 0`。

**验证:** `go build ./internal/llm/...` 通过;烟囱 openai 兼容端点跑两轮,`cached_tokens` 字段被打印(端点支持则 >0)。

## T9: agent 改造**文件:** `internal/agent/agent.go`
**依赖:** T1, T2, T3, T6
**步骤:**
1. `Agent` 加 `version string` 字段;`New(p llm.Provider, r *tool.Registry, version string)`。
2. 加常量 `planReminderInterval = 4`。
3. `Run` 起始:`env := prompt.GatherEnvironment(a.version, a.provider.Model())`;`sys := prompt.BuildSystemPrompt()`;`defs` 按 mode 选择(规划=`ReadOnlyDefinitions`,普通=`Definitions`)——**移除 suffix 变量**。
4. 每轮迭代算 reminder:`reminder := ""`;`if mode==ModePlan { full := iter==1 || (iter-1)%planReminderInterval==0; reminder = prompt.PlanReminder(full) }`。
5. `streamOnce` 签名改为接收 `sys`、`envText`、`defs`、`reminder`,内部组装 `llm.Request{Messages:conv.Messages(), Tools:defs, System:llm.System{Stable:sys, Environment:envText}, Reminder:reminder}` 调 `a.provider.Stream(ctx, req)`。
6. `agent.Usage` 加 `CacheWrite/CacheRead`;`Run` 透传 `Event{Usage:&Usage{Input,Output,CacheWrite,CacheRead}}`。

**验证:** `go build ./internal/agent/...` 通过(配合 T10/T11)。

## T10: TUI 与 smoke 接线**文件:** `internal/tui/stream.go`、`cmd/smoke/main.go`
**依赖:** T9
**步骤:**
1. `stream.go:95` 改 `agent.New(m.provider, m.registry, m.version)`;`/do` 注入仍用 `prompt.ExecuteDirective`(已迁至 reminder.go,import 路径不变)。
2. `smoke/main.go`:`agent.New(p, tool.NewDefaultRegistry(), "dev")`;消费 `Event.Usage` 时打印 `input/output/cache_write/cache_read`;可改为连发两条消息观察次轮 `cache_read`。

**验证:** `go build ./...` 全绿。

## T11: agent 单测适配**文件:** `internal/agent/agent_test.go`
**依赖:** T9
**步骤:**
1. 修 fake provider:`Stream` 实现新签名 `Stream(ctx, req llm.Request)`;记录收到的 `req`(System.Stable/Environment、Tools、Reminder)。
2. 既有 ch04 场景(A 自然完成、B 上限、C 未知工具、D 并发、E 取消、F 规划只读工具)适配新签名;`New(...)` 传 version。
3. 新增断言:
   - 规划模式下 `req.System.Stable` 非空且**普通/规划一致**;`req.System.Environment` 非空。
   - 规划模式 iter1 的 `req.Reminder` 含完整提醒、含 `<system-reminder>`;iter2 为精简版(构造一个让循环多轮的脚本)。
   - 规划模式 `req.Tools` 仅只读;普通模式全量。
   - reminder **不写入 conv 持久历史**(`conv.Messages()` 不含 reminder 文本)。
   - 缓存用量透传:fake 发 `Usage{CacheWrite:X,CacheRead:Y}` → 收到的 `Event.Usage` 携带 X/Y。

**验证:** `go test ./internal/agent/...` 通过;`go test -race ./internal/agent/...` 无竞争。

## T12: 全量编译测试与规范**文件:** —
**依赖:** T1–T11
**步骤:**
1. `gofmt -l .`(无输出)、`goimports` 分组检查。
2. `go vet ./...`(无告警)。
3. `go build ./...`、`go test ./...`、`go test -race ./internal/agent/... ./internal/tool/...`。

**验证:** 全部通过;检索输出无 api_key 明文。

## 执行顺序

```
T1 ─┐
T2 ─┼─→ T4(prompt 单测)
T3 ─┘
T5(工具描述，独立)

T6(接口) ─┬─→ T7(anthropic) ─┐
          └─→ T8(openai)    ─┤
T1,T2,T3,T6 ─→ T9(agent) ────┼─→ T10(tui/smoke)
                              └─→ T11(agent 单测)

全部 ─→ T12(编译/测试/race/gofmt/vet)
```
````

```markdown
# 系统提示工程化 Checklist

> 每一项通过运行代码或观察行为来验证,聚焦系统行为;括号内为验证方式与对应需求。

## 实现完整性
- [ ] 模块化装配:系统提示由按优先级排列的固定模块拼成、模块间空行分隔(验证:`prompt_test` 断言身份段在工具使用段之前、以空行分隔)。(AC1/F1)
- [ ] 挂载即扩展:新增一个模块只需出现在模块列表,装配自动按优先级插入,不改 `AssembleSystem` 逻辑(验证:`prompt_test` 传入额外模块断言其落在预期位置)。(AC1/F1)
- [ ] 可选空槽:自定义指令/已激活 Skill/长期记忆内容为空时装配跳过、不留多余空行(验证:`prompt_test` 断言空模块不出现、无连续空行)。(AC2/F1)
- [ ] 缓存确定性:连续两次构造稳定系统提示逐字节相等;改变环境信息不改变稳定块(验证:`prompt_test` 断言两次 `BuildSystemPrompt()` 相等、稳定块不含 date/git/cwd)。(AC5/F3/N1)
- [ ] 环境信息呈现:系统提示第二段含工作目录、平台、当前日期、git 状态、版本、模型;与稳定块分属不同内容块(验证:`prompt_test` 断言 `Render()` 含各项;anthropic 请求 `System` 为两个 TextBlock)。(AC3/F2)
- [ ] 双重强化:关键约定(优先用专用工具、编辑前必先读)在工具描述与系统提示模块文本中均出现(验证:`prompt_test` 断言系统提示含相关语句;检索 `edit_file`/`bash` 描述含强化语句)。(AC7/F5)
- [ ] 补充消息注入机制:`PlanReminder` 输出以 `<system-reminder>` 标签包裹(验证:`prompt_test` 断言标签存在)。(AC8/F6)
- [ ] 缓存字段解析:provider 用量对外暴露缓存写/读;Anthropic 取 cache_creation/cache_read,OpenAI 取 cached_tokens,缺字段为 0 不报错(验证:`agent_test` fake 发缓存用量断言 `Event.Usage` 透传;烟囱打印真实字段)。(AC6/F4/N6)
- [ ] Anthropic 缓存断点真实发出:稳定块 marshal 后带 `cache_control:ephemeral`、环境块不带(验证:`llm/anthropic_test.go` 序列化 `toAnthropicSystem` 断言——守护「空字面量被 omitzero 丢弃」回归;**必须用 `NewCacheControlEphemeralParam()` 构造器**)。(AC4/F3)

## 集成
- [ ] 规划模式按轮次注入:`/plan` 后 iter1 注入完整提醒、间隔轮(每 4)重复完整、其余轮精简;reminder 不写入持久历史(验证:`agent_test` 多轮脚本断言各轮 `req.Reminder` 详略 + `conv.Messages()` 不含 reminder 文本)。(AC9/F6/F7)
- [ ] 规划模式工具集:规划模式 `req.Tools` 仅只读、普通模式全量(验证:`agent_test` 断言两模式 Tools 差异)。(AC9/F7)
- [ ] 稳定系统提示跨模式一致:普通与规划模式 `req.System.Stable` 相同(规划提醒已移出系统通道)(验证:`agent_test` 断言两模式 Stable 相等)。(F7/N1)
- [ ] 历史合法:注入 reminder 后请求消息序列角色合法(Anthropic 并入末条 user、不产生连续 user)(验证:`agent_test` 断言织入后末条仍为单一 user 回合;端到端 plan 多轮不报 400)。(AC12/N3)
- [ ] 跨协议一致:anthropic 与 openai(含兼容 base_url)都装配同一系统提示 + 环境段、注入同一 reminder(验证:两配置各跑多轮;anthropic 看缓存命中、openai 看 cached_tokens)。(AC10/F8)
- [ ] 不破坏 ch04:多轮连环、用户取消、流出错恢复、历史一致仍成立(验证:跑 ch04 端到端关键场景;`go test ./...` 通过)。(AC11/N2)
- [ ] 环境采集降级:非 git 目录/git 不可用时环境段对应项省略、不卡界面、请求正常发起(验证:在非 git 临时目录跑 smoke/TUI 观察)。(AC13/N4)
- [ ] 界面不阻塞:环境采集(含 git 外调)不冻结界面、不显著拖慢首字(验证:正常目录跑,观察发起延迟无异常)。(N4)

## 编译与测试
- [ ] `go build ./...` 无错误。
- [ ] `go vet ./...` 无告警。
- [ ] `go test ./...` 通过(config、conversation、tool、agent、prompt 单测)。
- [ ] `go test -race ./internal/agent/... ./internal/tool/...` 无竞争告警。(N2/N6)
- [ ] `gofmt -l .` 无输出(格式合规、goimports 分组正确)。(AC14/N7)
- [ ] 密钥不回显:对话区、环境段与任何输出均不出现 api_key(验证:通读输出、检索无明文 key;确认环境段不含环境变量)。(AC14/N5)

## 端到端场景(tmux 实跑)
- [ ] 场景 1(缓存命中,Anthropic):同一会话连发两条消息 → smoke/调试打印首轮 `cache_write>0`、次轮 `cache_read>0`,证明稳定前缀被缓存复用。(AC4/F3)
- [ ] 场景 2(规划模式按轮次):`/plan` 发一个需多步只读调研的任务 → 模型仅用只读工具产出计划、首轮注入完整提醒、后续轮精简;`/do` 切回全工具并按计划执行(产生写/执行类调用)。(AC9/F7)
- [ ] 场景 3(reminder 不被当用户输入):规划模式下模型不复述/回应 `<system-reminder>` 内容,而是据其约束行事。(AC8/F6)
- [ ] 场景 4(环境感知):问「我现在在哪个目录、什么平台、今天几号」→ 模型据环境段正确回答。(AC3/F2)
- [ ] 场景 5(取消后可继续):规划模式多轮中途按 Esc 取消 → 回空闲态、再发一条继续对话不报 400。(AC12/N3)
- [ ] 场景 6(非 git 目录降级):在非 git 目录启动 → 环境段省略 git 状态、正常对话。(AC13/N4)
```

### Python

```markdown
# 系统提示工程化 Spec## 背景ch04 给 GuoLaiCode 装上了 Agent Loop——能自主多轮想 → 调工具 → 看结果地干活。但「**干活的指令**」还很原始：系统提示是一段揉在一起的固定常量（身份、工具用法、简洁性挤在同一块文本里），没有结构、没有优先级、想加一类新指令只能往字符串里塞；每轮请求都把整段系统提示与全部工具定义**原样重发**，稳定不变的内容反复计费、反复占用首字延迟；模型对**当前环境**（工作目录、平台、日期、git 状态）一无所知；规划模式提醒每轮无差别地拼在系统提示尾部，注入频率不可控。

ch05 给 GuoLaiCode 做**系统提示工程化**：把全局指令拆成按职责划分、按优先级拼装的模块；把稳定内容与变化内容分到**可缓存通道**与**消息通道**，让稳定的指令和工具定义走缓存省钱省首字延迟；引入一种带特殊标签的**补充消息注入**机制，在运行中注入指令而不污染缓存、不被模型当成用户提问来回复；会话级开关（规划模式）的提醒按**轮次频率**注入。这是从「能干活」到「干得好」的一步。

## 目标

- 把全局指令拆成按职责划分的若干模块，按优先级拼装成完整系统提示；新增一类指令只需挂载新模块，不改装配逻辑。
- 区分稳定与变化内容：稳定的系统模块与工具定义走**可缓存通道**；环境信息与对话历史走**不缓存的通道**。
- 在工具描述与系统提示中，对关键约定（优先用专用工具、编辑前必先读）**双重强化**，提高遵守率。
- 提供一种带特殊标签的**补充消息注入**机制：运行中注入补充指令，既不污染缓存，也不会被模型当成用户输入来回复。
- 会话级开关（规划模式）的提醒按轮次注入：**首轮完整、间隔轮次重复完整、其余轮次精简**，控制注入频率。
- 能从 provider 返回中**解析缓存命中信息**（写入 / 读取 token），用于验证缓存策略真实生效。

## 功能需求

- F1: 模块化系统提示拼装
  把全局指令按职责拆成模块，每个模块带名称、优先级、内容。装配时按优先级从高到低拼接成完整系统提示，模块间以空行分隔。固定模块七个，按优先级排：**身份 → 系统约束 → 任务模式 → 动作执行 → 工具使用 → 语气风格 → 文本输出**。其后预留三个可选模块位置：**自定义指令、已激活 Skill、长期记忆**——本章这三个**不接入真实内容来源**（留待后续章节），装配时内容为空则跳过、不留多余空行。新增一类指令只需定义新模块挂到对应优先级，不改装配主逻辑。

- F2: 环境信息构造与呈现
  收集运行环境信息（工作目录、操作系统/平台、当前日期、git 状态、应用版本、当前模型等），构造成一段供模型感知环境的文本。环境信息作为系统提示的**独立第二段**呈现——结构上接在固定模块之后，物理上与可缓存的稳定模块**分属不同内容块**（见 F3），自身不进缓存。

- F3: 稳定内容走可缓存通道
  稳定不变的内容（拼装好的稳定系统模块 + 工具定义）归入**可缓存通道**，变化内容（环境信息 + 对话历史）归入**不缓存通道**。可缓存内容必须**跨轮逐字节一致**（不含任何随轮次变化的成分），否则缓存无法命中。Anthropic 对稳定内容显式标记缓存断点，使工具定义与稳定系统模块被缓存复用，环境信息块与历史在断点之后不缓存；OpenAI 把稳定内容置于请求前缀，靠端点前缀缓存自动受益（不强制端点支持）。

- F4: 缓存命中解析
  从 provider 响应用量中解析缓存字段——**缓存写入**（首次创建缓存的 token）与**缓存读取**（命中复用的 token），随用量一并对外暴露，供烟囱/调试程序打印验证缓存是否真生效。Anthropic 解析其缓存创建/读取字段；OpenAI 解析其缓存命中字段（端点提供则有，否则为零）。

- F5: 关键约定双重强化
  对模型最易违反的关键约定（如「优先用专用工具而非 shell 拼凑」「编辑文件前必先读取」），在**工具描述**与**系统提示模块**中双重表述，提高遵守率。

- F6: 补充消息注入机制（system-reminder）
  提供一种带特殊标签（如 `<system-reminder>`）的补充消息注入形式：构造请求时把补充指令注入**消息通道**，标签语义让模型理解这是系统补充上下文而非用户提问——不针对它直接回复。此类消息**每轮动态构造、不写入持久历史**，因而不污染缓存、不破坏历史角色交替。

- F7: 规划模式按轮次注入
  规划模式提醒改由 F6 机制承载（不再拼到系统提示尾部）。按 Agent Loop 轮次控制注入**频率与详略**：**首轮注入完整提醒，之后每隔固定轮次重复一次完整提醒，其余轮次注入精简版**。模式跨轮保持，`/plan` 进入、`/do` 退出并触发按计划执行；规划模式下仍仅放开只读工具（均沿用 ch04）。

- F8: 跨协议一致
  Anthropic 与 OpenAI（含兼容 base_url）都装配同一套模块化系统提示 + 环境信息、都注入相同补充消息、都尽力走缓存（Anthropic 显式断点 / OpenAI 前缀顺序）。两协议在模块内容、注入时机、规划模式注入节奏上**行为一致**；缓存命中解析在各自字段可得时生效。

## 非功能需求- N1: 缓存确定性——可缓存块（工具定义 + 稳定系统模块）在跨轮、会话内多轮之间**逐字节稳定**：模块装配顺序、工具顺序固定，不混入任何随轮次/时间变化的成分。否则缓存永不命中，本章核心目标落空。
- N2: 不破坏 ch04 行为——Agent Loop 多轮编排、流式双路收集、保序分批并发、用户取消、历史一致、用量/迭代进度展示等 ch04 能力**不退化**。系统提示重构与消息注入是叠加，不改这些既有路径的语义。
- N3: 历史合法性——每轮动态注入的补充消息（不持久）拼进请求后，消息序列仍**角色交替合法、工具调用与结果配对不破坏**，不致 provider 返回 400；补充消息不写入持久历史，故不影响后续轮次与可恢复性。
- N4: 环境采集不阻塞、可降级——环境信息采集（含可能外调 shell 的 git 状态）**快速且有界**，不阻塞界面、不显著拖慢请求发起；任一项取不到时降级（留空/省略），不中断会话。
- N5: 密钥与敏感信息不泄漏——沿用 ch03/ch04，对话区与任何输出均不出现 api_key；环境信息构造**不带入密钥或敏感环境变量**。
- N6: 缓存字段解析健壮——provider / 兼容端点未返回缓存字段时按零处理，不抛未处理异常、不因缺字段中断本轮或会话。
- N7: 代码规范——`ruff format` / `ruff check` 合规（import 分组正确、PEP 8 一致）；可选 `mypy` 通过。
- N8: 可扩展性——新增一类系统提示模块或一类补充消息提醒时，改动局部：不触及装配主逻辑，不改 provider 适配层对外签名的语义稳定性。

## 不做的事- **项目指令文件加载**——「自定义指令」为预留空槽，本章不从 CLAUDE.md / 项目级指令文件读取内容（留待后续章节）。
- **自动记忆 / 长期记忆**——「长期记忆」为预留空槽，本章不做记忆写入与召回。
- **真实 MCP 接入**——「已激活 Skill」等可选模块为预留空槽，不接入 MCP 工具/资源。
- **自动化评估 / 评测框架**——本章只做缓存字段解析验证 + 一组典型场景人工对比的定性评估，不建评测流水线。
- **历史增量缓存**——只缓存稳定前缀（工具定义 + 稳定系统模块），不对对话历史另打缓存断点。
- **缓存 TTL / 断点数配置化**——TTL 用默认值、断点位置内置，本章不通过配置调整。
- **状态栏展示缓存命中**——缓存信息只走 smoke / 调试输出，不进 TUI 状态栏。
- **提示词运行时热加载 / 外部模板文件**——模块内容内置于代码，不做外部模板。
- **多模态、流式工具结果**——沿用 ch04，不做。

## 验收标准

- AC1: 模块化装配——系统提示由按优先级排列的固定模块拼成，模块间以空行分隔；新增一个模块只需挂载、不改装配逻辑。(F1)
- AC2: 可选空槽——「自定义指令/已激活 Skill/长期记忆」内容为空时装配自动跳过、不留多余空行；架构支持后续填充。(F1)
- AC3: 环境信息呈现——系统提示第二段含工作目录、平台、当前日期、git 状态、应用版本、当前模型；与稳定模块分属不同内容块。(F2)
- AC4: 缓存命中（Anthropic）——首轮 `cache_creation_input_tokens > 0`、次轮 `cache_read_input_tokens > 0`（稳定前缀命中）；仅环境信息/历史变化时稳定前缀仍命中。(F3/N1)
- AC5: 缓存确定性——两次构造的稳定块逐字节相等；故意改变环境信息不改变稳定块内容。(F3/N1)
- AC6: 缓存字段解析——provider 用量对外暴露缓存写/读；OpenAI 解析 `cached_tokens`，端点不返回时为 0 且不报错。(F4/N6)
- AC7: 双重强化——关键约定（优先用专用工具、编辑前必先读）在**工具描述**与**系统提示模块**文本中均出现。(F5)
- AC8: 补充消息注入——注入消息以 `<system-reminder>` 标签包裹，模型不把它当用户提问来复述/回应；该消息**不写入持久对话历史**。(F6/N3)
- AC9: 规划模式按轮次注入——`/plan` 后：首轮注入完整提醒、之后每隔固定轮次重复完整、其余轮次注入精简版；规划模式仅放开只读工具；`/do` 切回全工具并按上文计划执行。(F7)
- AC10: 跨协议一致——anthropic 与 openai（含兼容 base_url）都装配同一系统提示 + 环境信息、注入同一 reminder、都尽力缓存（anthropic 看缓存命中、openai 看 `cached_tokens`），行为一致。(F8)
- AC11: 不破坏 ch04——ch04 关键行为（多轮连环、用户取消、历史一致、流出错恢复）仍成立；`pytest` 通过。(N2)
- AC12: 历史合法——注入 reminder 后请求不返回 400；规划模式多轮 + 中途取消后再发一条仍可继续（历史配对合法）。(N3)
- AC13: 环境采集降级——在非 git 目录或 git 不可用时，环境段对应项降级（留空/省略），界面不卡、请求正常发起。(N4)
- AC14: 代码规范——`ruff format --check .` 通过、`ruff check .` 无告警；对话区与输出无 api_key 明文。(N5/N7)
```

````markdown
# 系统提示工程化 Plan

> 技术栈：Python 3.12+；`anthropic`（`AsyncAnthropic`）、`openai`（`AsyncOpenAI`）。SDK 缓存 API 行为以官方文档为准（见技术决策表）。

## 架构概览ch05 在三层叠加，**不改 ch04 的 Agent Loop 控制流**：

- **prompt 包（重写）**：从「单个常量字符串」升级为「模块化装配 + 环境采集 + 补充消息构造」。对外产出三类文本——**稳定系统提示**（可缓存）、**环境信息段**（不缓存）、**system-reminder 包裹的补充指令**。prompt 包不依赖 llm 包（避免循环依赖）。
- **llm 包（改造）**：`Provider.stream` 入参从位置参数改为 `Request` dataclass，承载 `messages / tools / system{stable, environment} / reminder`。Anthropic 把 stable 块打缓存断点、env 块不打；OpenAI 把 stable 置于系统消息前缀。`Usage` 增加缓存写/读字段。两 provider 把 `reminder` 按各自协议安全地织入消息通道（N3）。
- **agent 包（改造）**：每次 `run` 开始采集环境、装配稳定系统提示；每轮迭代按 `mode + iter` 计算本轮 reminder（规划模式按轮次详略），组装 `Request` 发起请求；把缓存用量透传到 `Event.usage`。
- **smoke（改造）**：打印每轮用量的缓存写/读字段，作为缓存策略生效的验证手段（TUI 状态栏不变）。

数据流：`agent.run` → `prompt.build_system_prompt()`（稳定） + `prompt.gather_environment().render()`（环境） + `prompt.plan_reminder(full)`（本轮补充） → 组装 `llm.Request` → `provider.stream` → Anthropic/OpenAI 各自装配缓存通道与消息通道 → 流式事件回到 agent → `Event(usage=Usage(..., cache_write, cache_read))` → smoke 打印。

## 核心数据结构### `prompt.Module`（新增）
```python
from dataclasses import dataclass

@dataclass(frozen=True)
class Module:
    name: str       # 模块标识（身份、系统约束 …），仅用于可读性与测试断言
    priority: int   # 数值越小优先级越高、排越前；固定模块 10..70，可选模块 80..100
    content: str    # 模块正文；为空则装配时跳过（可选空槽）
```

### `prompt.Environment`（新增）
```python
import os, runtime, time  # 实际用 os / platform / datetime
from dataclasses import dataclass

@dataclass
class Environment:
    working_dir: str   # os.getcwd()
    platform: str      # sys.platform 或 platform.system().lower()
    date: str          # datetime.date.today().isoformat()
    git_status: str    # `git status --porcelain` 摘要；非 git 目录/取不到则留空
    version: str       # 应用版本（从 agent 透传）
    model: str         # provider.model
```

### `llm.System`（新增）
```python
from dataclasses import dataclass

@dataclass
class System:
    stable: str = ""       # 可缓存：装配好的稳定系统提示（工具定义随 tools 一并进缓存前缀）
    environment: str = ""  # 不缓存：环境信息段
```

### `llm.Request`（新增，替换 stream 位置参数）
```python
from dataclasses import dataclass, field

@dataclass
class Request:
    messages: list[Message] = field(default_factory=list)        # 持久对话历史（不含本轮 reminder）
    tools: list[ToolDefinition] = field(default_factory=list)    # 本轮工具集（普通=全量 / 规划=只读）
    system: System = field(default_factory=System)               # 稳定系统提示 + 环境段
    reminder: str = ""                                           # 本轮 system-reminder 内容（已含标签；空=不注入）
```

### `llm.Usage`（扩展）
```python
@dataclass
class Usage:
    input_tokens: int = 0
    output_tokens: int = 0
    cache_write: int = 0   # Anthropic: cache_creation_input_tokens；OpenAI: 恒 0（自动缓存无写计数）
    cache_read: int = 0    # Anthropic: cache_read_input_tokens；OpenAI: prompt_tokens_details.cached_tokens
```

### `agent.Usage`（扩展，对外事件）
```python
@dataclass
class Usage:
    input: int = 0
    output: int = 0
    cache_write: int = 0
    cache_read: int = 0    # 透传自 llm.Usage，供 smoke 打印
```

## 核心接口### prompt 包
```python
def fixed_modules() -> list[Module]: ...                  # 七个固定模块（身份…文本输出），内容内置
def optional_modules() -> list[Module]: ...               # 三个可选空槽（自定义指令/已激活 Skill/长期记忆），content=""
def assemble_system(mods: list[Module]) -> str: ...       # 按 priority 升序、跳过空 content、以 "\n\n" 连接
def build_system_prompt() -> str: ...                     # = assemble_system(fixed_modules() + optional_modules())
def gather_environment(version: str, model: str) -> Environment: ...  # 采集环境；git/date 失败降级留空
class Environment:
    def render(self) -> str: ...                          # 渲染为「环境信息」第二段文本
def system_reminder(body: str) -> str: ...                # 用 <system-reminder>…</system-reminder> 包裹 body
def plan_reminder(full: bool) -> str: ...                 # 返回包好标签的规划模式提醒（full=完整 / 否=精简）
```

### `llm.Provider`（签名变更）
```python
from typing import Protocol, AsyncIterator

class Provider(Protocol):
    @property
    def name(self) -> str: ...
    @property
    def model(self) -> str: ...
    def stream(self, req: Request) -> AsyncIterator[StreamEvent]: ...  # 由 Request 承载全部入参
```

## 模块设计### prompt 包
**职责：** 模块化装配稳定系统提示；采集并渲染环境信息；构造 system-reminder 与规划模式提醒。
**对外接口：** 见上。
**关键点：**
- 七个固定模块按优先级排：**身份(10) → 系统约束(20) → 任务模式(30) → 动作执行(40) → 工具使用(50) → 语气风格(60) → 文本输出(70)**；可选空槽：**自定义指令(80) → 已激活 Skill(90) → 长期记忆(100)**（`content=""` 跳过）。
- **F5 双重强化**写在「工具使用(50)」模块：明确「优先用专用工具（`read_file`/`glob`/`grep`）而非用 bash 拼凑」「编辑文件前必须先 `read_file` 读取」；同时同义强化到 `edit_file`、`bash` 工具描述（见 tool 包改动）。
- `assemble_system` 只用常量内容 → 跨轮逐字节一致（**N1**）；环境与时间相关内容只进 `Environment`，绝不进稳定模块。
- `gather_environment`：git 状态用一条 `git status --porcelain` 带短超时执行（`asyncio.create_subprocess_exec` 或 `subprocess.run(timeout=2)`），失败/非 git 目录则 `git_status=""`；不读取任何环境变量（**N5**）。
**依赖：** 标准库（`os`/`sys`/`platform`/`datetime`/`asyncio`/`subprocess`）；不依赖 llm。

### llm 包（`__init__.py` / `anthropic_provider.py` / `openai_provider.py`）
**职责：** 把 `Request` 装配为各协议请求，分离缓存通道与消息通道，解析缓存用量，安全织入 reminder。
**对外接口：** `stream(req: Request) -> AsyncIterator[StreamEvent]`。
**关键点：**
- 删除原 `_effective_system` 辅助函数与对 `guolaicode.prompt` 的 import（系统提示改由 agent 传入）。
- **Anthropic**：构造 `system` 入参为 `list[dict]`：`req.system.stable` 非空 → `{"type": "text", "text": stable, "cache_control": {"type": "ephemeral"}}`（断点，默认 5m TTL）；`req.system.environment` 非空 → `{"type": "text", "text": environment}`（无 `cache_control`）。请求顺序 tools→system→messages，断点打在稳定块 → **缓存前缀 = 全部工具 + 稳定块**；env 与历史在断点后不缓存，env 每轮变化不影响前缀命中。`usage.cache_write = response.usage.cache_creation_input_tokens`、`cache_read = response.usage.cache_read_input_tokens`。
  - reminder 织入：`req.reminder` 非空时，把一个文本块 `{"type": "text", "text": reminder}` **追加到最后一条消息的 content 块**（循环中最后一条恒为 user 或 tool_result→user，追加文本块仍是合法 user 消息，保 N3 角色交替）；极端情形（末尾为 assistant）则新起一条 user 消息。
- **OpenAI**：系统消息 = `req.system.stable`（若 `environment` 非空则拼为 `stable + "\n\n" + environment` 单条 system 消息——兼容端点对多条 system 消息支持不一，统一单条）；stable 居前缀 → 端点前缀缓存命中稳定部分。`usage.cache_read = response.usage.prompt_tokens_details.cached_tokens`、`cache_write = 0`。
  - reminder 织入：`req.reminder` 非空时**追加一条尾部 user 消息**（OpenAI 容忍连续 user / tool 后接 user）。
- **N6**：缓存字段缺失即零值（`getattr(..., None) or 0`），不额外校验、不抛异常。

### agent 包（`agent.py`）
**职责：** 采集环境、装配系统提示、按轮次构造 reminder、组装 Request、透传缓存用量。
**关键点：**
- `Agent.__init__(provider, registry, version: str)` 增加 `version` 字段（供环境段）；`model` 取 `provider.model`。
- `run` 起始：`env = prompt.gather_environment(self._version, self._provider.model)`、`sys = prompt.build_system_prompt()`（稳定，普通/规划模式一致——规划提醒已移出系统通道）。
- 每轮迭代计算 reminder：`mode == PlanMode.PLAN` → `prompt.plan_reminder(full)`，`full = (iter == 1 or (iter - 1) % PLAN_REMINDER_INTERVAL == 0)`；否则 `""`。`PLAN_REMINDER_INTERVAL = 4`（内置常量）。
- `_stream_once` 组装 `llm.Request(messages=conv.messages(), tools=defs, system=llm.System(stable=sys, environment=env.render()), reminder=reminder)` 调 `provider.stream`。
- 删除 `suffix` / `READ_ONLY_DEFINITIONS` 的「系统后缀」用法；**只读工具集仍按 mode 选择**（规划=`READ_ONLY_DEFINITIONS`），原 `PLAN_MODE_REMINDER` 常量从系统后缀迁移为 `prompt.plan_reminder` 的内容。
- 缓存用量透传：`Event(usage=Usage(input, output, cache_write, cache_read))`。

### smoke（`src/guolaicode/smoke.py` 或 `examples/smoke.py`）
**职责：** 端到端验证缓存生效。
**关键点：** 消费 `Event.usage` 时打印 `input/output/cache_write/cache_read`；跑两轮（或多轮）观察次轮 `cache_read > 0`。`Agent(provider, registry, version="dev")`。

### tool 包（描述微调，F5）
- `edit_file.DESCRIPTION`：补「编辑前请先用 `read_file` 读取目标文件，确认 `old_string` 唯一」。
- `bash.DESCRIPTION`：补「读文件/找文件/搜内容请优先用 `read_file`/`glob`/`grep`，不要用 bash 拼凑」。
- 仅改描述文本，不改行为、不改 schema（N2）。

## 模块交互

```
TUI/smoke ─run(ctx, conv, mode)→ agent
  agent.run:
    env  = prompt.gather_environment(version, provider.model)
    sys  = prompt.build_system_prompt()
    for iter:
      reminder = prompt.plan_reminder(full(iter)) if mode == PLAN else ""
      req = llm.Request(messages=conv.messages(),
                        tools=defs(mode),
                        system=llm.System(stable=sys, environment=env.render()),
                        reminder=reminder)
      async for ev in provider.stream(req):
          # StreamEvent: text / tool_calls / usage(+cache) / done / err
          ...
    Event(usage=Usage(..., cache_write, cache_read)) ──→ smoke 打印 / TUI 状态栏（忽略 cache 字段）
```

依赖方向（无环）：`agent → {prompt, llm, conversation, tool}`；`llm → config`（不再 import prompt）；`prompt → 标准库`。

## 文件组织

```
guolaicode/
├── src/guolaicode/prompt/
│   ├── __init__.py        — 改：导出 Module/装配/build_system_prompt；保留 banner（CAT_BANNER/render_banner/READY_HINT）
│   ├── modules.py         — 新：fixed_modules()/optional_modules() 七固定+三空槽的内容常量
│   ├── environment.py     — 新：Environment / gather_environment / Environment.render
│   └── reminder.py        — 新：system_reminder / plan_reminder（完整版/精简版常量）/ EXECUTE_DIRECTIVE
├── src/guolaicode/llm/
│   ├── __init__.py        — 改:Request/System dataclass；Usage 加缓存字段；Provider.stream 签名；删 _effective_system
│   ├── anthropic_provider.py — 改：两块 system（断点+env）、缓存用量解析、reminder 织入
│   └── openai_provider.py    — 改：单条 system（stable+env）、cached_tokens 解析、reminder 尾部注入
├── src/guolaicode/agent/
│   └── agent.py           — 改：__init__(+version)、run 采集环境/装配系统、按轮次 reminder、缓存透传
├── src/guolaicode/tool/
│   ├── edit_file.py       — 改：DESCRIPTION 补强化
│   └── bash.py            — 改：DESCRIPTION 补强化
├── src/guolaicode/tui/
│   └── stream.py          — 改：Agent(...) 传 version（m.version 已有）
├── examples/smoke.py      — 改：打印缓存用量；Agent(p, registry, "dev")
└── tests/
    ├── test_prompt.py     — 新：装配顺序/跳空槽/N1 确定性/双重强化文本断言
    ├── test_anthropic_system.py — 新：序列化稳定块带 cache_control、环境块不带（守护回归）
    └── test_agent.py      — 改：断言 Request 装配（system 两段、规划按轮次 reminder）、缓存用量透传
```

## 技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 系统提示组织 | 模块化（`Module(name, priority, content)` + `assemble_system`） | 满足 F1「挂载即扩展」；优先级排序使顺序确定（N1） |
| 环境信息归属 | system 通道独立第二块（用户拍板） | 结构上接系统提示之后；物理上与稳定块分离，不进缓存 |
| Anthropic 缓存断点 | 仅在稳定 system 块打 `cache_control: ephemeral`（默认 5m） | 请求序 tools→system→messages，断点在稳定块即缓存「工具+稳定块」整段前缀；env 在其后不缓存，env 变化不冲前缀命中 |
| 工具是否单独打断点 | 否 | 稳定块断点的前缀已含全部工具，无需再给 tool 单独标 cache_control |
| OpenAI 环境信息 | 拼入单条 system 消息（stable 在前） | 兼容端点对多条 system 支持不一；stable 居前缀，端点前缀缓存自动命中稳定部分。代价：env 居 system 尾，OpenAI 工具可能不进缓存前缀——本章 OpenAI 缓存为尽力而为、不强制（F8） |
| 缓存用量字段 | `Usage` 加 `cache_write` / `cache_read` | Anthropic 取 `cache_creation_input_tokens`/`cache_read_input_tokens`；OpenAI 取 `prompt_tokens_details.cached_tokens` |
| stream 入参 | 改 `Request` dataclass | 入参从 4 个增至含 `system`/`reminder`，dataclass 更清晰、后续扩展不再改签名（N8） |
| reminder 注入位置 | Anthropic 并入末条 user 消息 content 块；OpenAI 追加尾部 user 消息 | Anthropic 严格角色交替——并入避免连续 user 触发 400（N3）；OpenAI 容忍连续 user |
| reminder 持久化 | 不写入 conversation（用户拍板） | 每轮动态构造；不污染缓存、不破坏历史可恢复性 |
| 规划提醒节奏 | `iter == 1` 或 `(iter - 1) % 4 == 0` → 完整，否则精简（per `run` 内 iter） | 实现 F7「首轮完整、间隔重复、其余精简」；复用已有 iter 计数 |
| 缓存验证呈现 | smoke/调试打印（用户拍板） | 不动 TUI 状态栏；`Usage` 携带字段供打印 |
| prompt↔llm 依赖 | 系统提示由 agent 传入，llm 不再 import prompt | 打破潜在循环依赖；职责更清晰 |
| 子进程外调 git | `asyncio.create_subprocess_exec("git", "status", "--porcelain")` + `asyncio.wait_for(..., timeout=2.0)`；同步路径回退 `subprocess.run(timeout=2)` | 不阻塞 event loop（N4）；超时/失败均降级为空字符串 |
````

````markdown
# 系统提示工程化 Tasks

> 包名：`guolaicode`（Python 3.12+）。源码位于 `src/guolaicode/`，内部模块以 `guolaicode.xxx` 导入。

## 文件清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `src/guolaicode/prompt/modules.py` | `Module` 类型；`fixed_modules()` 七固定模块、`optional_modules()` 三空槽的内容常量 |
| 改   | `src/guolaicode/prompt/__init__.py` | `assemble_system`/`build_system_prompt`；删旧 `SYSTEM_PROMPT`/`PLAN_MODE_REMINDER`/`EXECUTE_DIRECTIVE` 常量（迁移）；保留 banner |
| 新建 | `src/guolaicode/prompt/environment.py` | `Environment` 结构、`gather_environment`、`Environment.render` |
| 新建 | `src/guolaicode/prompt/reminder.py` | `system_reminder` 标签包裹、`plan_reminder(full)`、规划提醒完整/精简常量、`EXECUTE_DIRECTIVE` |
| 新建 | `tests/test_prompt.py` | 装配顺序、跳空槽、N1 确定性、双重强化文本断言 |
| 改   | `src/guolaicode/tool/edit_file.py` | `DESCRIPTION` 补「编辑前先 `read_file`」 |
| 改   | `src/guolaicode/tool/bash.py` | `DESCRIPTION` 补「优先用专用工具而非 bash 拼凑」 |
| 改   | `src/guolaicode/llm/__init__.py` | `System` / `Request` dataclass；`Usage` 加缓存字段；`Provider.stream(req)`；删 `_effective_system` 与 prompt import |
| 改   | `src/guolaicode/llm/anthropic_provider.py` | 两块 system（稳定块打断点 + env 块）、缓存用量解析、reminder 并入末条 user |
| 改   | `src/guolaicode/llm/openai_provider.py` | 单条 system（stable+env 拼接）、`cached_tokens` 解析、reminder 追加尾部 user |
| 改   | `src/guolaicode/agent/agent.py` | `__init__(+version)`；`run` 采集环境/装配系统；按轮次 reminder；缓存用量透传 |
| 改   | `tests/test_agent.py` | 断言 Request 装配（system 两段、规划按轮次 reminder）、缓存用量透传；修既有用例适配新签名 |
| 新建 | `tests/test_anthropic_system.py` | 序列化稳定块带 `cache_control`、环境块不带（守护回归） |
| 改   | `src/guolaicode/tui/stream.py` | `Agent(...)` 传 `self.version` |
| 改   | `examples/smoke.py` | 打印缓存用量；`Agent(p, registry, "dev")` |

---

## T1: prompt 模块化装配**文件：** `src/guolaicode/prompt/modules.py`、`src/guolaicode/prompt/__init__.py`
**依赖：** 无
**步骤：**
1. 在 `modules.py` 定义 `@dataclass(frozen=True) class Module: name: str; priority: int; content: str`。
2. `def fixed_modules() -> list[Module]` 返回七个固定模块，内容内置（中英按现有 `SYSTEM_PROMPT` 风格，英文为主）：
   - 身份(10)：GuoLaiCode 是终端编码 Agent。
   - 系统约束(20)：操作边界——在工作目录约定内行事、不外泄密钥、对破坏性操作谨慎。
   - 任务模式(30)：ReAct——多步推进、读后再改、完成才给终答。
   - 动作执行(40)：何时调工具、连续只读可并发、有副作用谨慎。
   - 工具使用(50)：**优先用 `read_file`/`glob`/`grep` 而非 bash 拼凑；编辑文件前必先 `read_file`**（F5）。
   - 语气风格(60)：简洁、直接、不奉承。
   - 文本输出(70)：必要时用 Markdown（代码块/列表），终答精炼。
3. `def optional_modules() -> list[Module]` 返回三个空槽：自定义指令(80)、已激活 Skill(90)、长期记忆(100)，`content` 均为 `""`。
4. 在 `prompt/__init__.py`：
   - `def assemble_system(mods: list[Module]) -> str`：按 `priority` 升序稳定排序、**跳过 `content == ""`**、以 `"\n\n"` 连接。
   - `def build_system_prompt() -> str`：`assemble_system(fixed_modules() + optional_modules())`。
   - 删除旧 `SYSTEM_PROMPT`、`PLAN_MODE_REMINDER` 常量（内容迁至模块/reminder）；`EXECUTE_DIRECTIVE` 迁至 `reminder.py`。保留 `CAT_BANNER`/`READY_HINT`/`render_banner`。

**验证：** `python -c "from guolaicode.prompt import build_system_prompt; print(build_system_prompt())"` 观察七模块按序、空槽不留空行；`ruff check src/guolaicode/prompt/` 无告警。

## T2: 环境采集与渲染**文件：** `src/guolaicode/prompt/environment.py`
**依赖：** 无
**步骤：**
1. 定义 `@dataclass class Environment: working_dir: str; platform: str; date: str; git_status: str; version: str; model: str`。
2. `def gather_environment(version: str, model: str) -> Environment`：
   - `working_dir = os.getcwd()`（捕获 `OSError` 留空）、`platform = sys.platform`（或 `platform.system().lower()`）、`date = datetime.date.today().isoformat()`。
   - `git_status`：用 `subprocess.run(["git", "status", "--porcelain"], capture_output=True, text=True, timeout=2.0)`；返回码非零/`FileNotFoundError`/`TimeoutExpired`/非 git 目录 → `""`；有输出则取摘要（如「N 个文件改动」或前几行）。同步实现即可（外调耗时已被 2s 超时收口；如需 async 调用方可包 `asyncio.to_thread`）。
   - `version=version`、`model=model`。**不读任何环境变量**（N5）。
3. `Environment.render(self) -> str`：渲染为「环境信息」段——逐行 `Key: Value`，空值项省略。

**验证：** 单测在临时非 git 目录 `gather_environment` 得 `git_status == ""` 且不抛异常；`render()` 含 cwd/platform/date。

## T3: 补充消息与规划提醒构造**文件：** `src/guolaicode/prompt/reminder.py`
**依赖：** 无
**步骤：**
1. `def system_reminder(body: str) -> str`：返回 `f"<system-reminder>\n{body}\n</system-reminder>"`。
2. 规划提醒常量：`_PLAN_REMINDER_FULL`（完整版，含「仅可用只读工具调研、产出分步计划、等 `/do` 批准」）、`_PLAN_REMINDER_CONCISE`（精简版，一两句）。
3. `def plan_reminder(full: bool) -> str`：`system_reminder(_PLAN_REMINDER_FULL if full else _PLAN_REMINDER_CONCISE)`。
4. `EXECUTE_DIRECTIVE`（从 `prompt/__init__.py` 迁来）：`/do` 注入的用户消息文案。

**验证：** 单测断言 `plan_reminder(True)` 含 `<system-reminder>` 与完整文案；`plan_reminder(False)` 用精简文案。

## T4: prompt 单测**文件：** `tests/test_prompt.py`
**依赖：** T1, T2, T3
**步骤：**
1. 装配顺序：断言 `build_system_prompt()` 中身份段出现在工具使用段之前；模块以空行分隔。
2. 跳空槽：在 `assemble_system` 传入含空 `content` 的模块，断言其不出现、不产生连续多空行。
3. **N1 确定性**：连续两次 `build_system_prompt()` 结果 `==`。
4. **F5 双重强化**：`build_system_prompt()` 文本含「编辑」与「先读」之意、含「优先」与专用工具名。
5. 环境与 reminder：见 T2/T3 验证（合并到本文件或拆分）。

**验证：** `pytest tests/test_prompt.py` 通过。

## T5: 工具描述双重强化**文件：** `src/guolaicode/tool/edit_file.py`、`src/guolaicode/tool/bash.py`
**依赖：** 无
**步骤：**
1. `edit_file.DESCRIPTION` 末补：「编辑前请先用 `read_file` 读取目标文件，确认 `old_string` 唯一。」
2. `bash.DESCRIPTION` 末补：「读文件、找文件、搜内容请优先用 `read_file`/`glob`/`grep`，不要用 bash 拼凑。」
3. 不改 schema、不改 `execute` 行为。

**验证：** `pytest tests/test_tool.py` 仍通过；`ruff check src/guolaicode/tool/` 无告警。

## T6: llm 接口改造**文件：** `src/guolaicode/llm/__init__.py`
**依赖：** 无（但 T7/T8/T9 依赖本任务）
**步骤：**
1. 新增 `@dataclass class System: stable: str = ""; environment: str = ""`、`@dataclass class Request: messages: list[Message] = field(default_factory=list); tools: list[ToolDefinition] = field(default_factory=list); system: System = field(default_factory=System); reminder: str = ""`。
2. `Usage` 加 `cache_write: int = 0`、`cache_read: int = 0`。
3. `Provider.stream` 改为 `def stream(self, req: Request) -> AsyncIterator[StreamEvent]`；更新 Protocol 文档字符串。
4. 删除 `_effective_system` 函数与对 `guolaicode.prompt` 的 import。
5. `new_provider` 工厂保持。

**验证：** `python -c "from guolaicode.llm import Request, System, Usage, Provider"` 不报错；anthropic/openai 适配器在 T7/T8 修复前会 import 失败，预期。

## T7: Anthropic 适配缓存通道 + reminder**文件：** `src/guolaicode/llm/anthropic_provider.py`
**依赖：** T6
**步骤：**
1. `async def stream(self, req: Request) -> AsyncIterator[StreamEvent]`：
   - 构造 `system: list[dict] = []`：`req.system.stable` 非空 → `{"type": "text", "text": stable, "cache_control": {"type": "ephemeral"}}`；`req.system.environment` 非空 → `{"type": "text", "text": environment}`（无 `cache_control`）。
   - `messages = _to_anthropic_messages(req.messages)`；`req.reminder` 非空 → 调 `_append_reminder_anthropic(messages, req.reminder)`：把 `{"type": "text", "text": reminder}` 追加到**最后一条消息**的 `content`（确保 `content` 为 list 形态后追加）；末条非 user 时新起一条 user 消息。
   - `tools = _to_anthropic_tools(req.tools)`（不另打断点）。
   - thinking 逻辑沿用（`_assistant_used_tools(req.messages)`）。
2. Usage 解析：`Usage(input_tokens=resp.usage.input_tokens, output_tokens=resp.usage.output_tokens, cache_write=getattr(resp.usage, "cache_creation_input_tokens", 0) or 0, cache_read=getattr(resp.usage, "cache_read_input_tokens", 0) or 0)`。

**验证：** `python -c "from guolaicode.llm.anthropic_provider import AnthropicProvider"` 不报错（配合 T8）；smoke anthropic 跑两轮见 `cache_read > 0`（次轮）。

## T8: OpenAI 适配缓存通道 + reminder**文件：** `src/guolaicode/llm/openai_provider.py`
**依赖：** T6
**步骤：**
1. `_to_openai_messages(req)`：首条 system 消息 = `req.system.stable`（若 `environment` 非空则拼为 `stable + "\n\n" + environment`）；随后映射历史；`req.reminder` 非空 → 追加 `{"role": "user", "content": req.reminder}`。
2. `async def stream(self, req: Request)` 改用 `req`；`params["tools"] = _to_openai_tools(req.tools)`。
3. Usage 解析：`cache_read = getattr(getattr(resp.usage, "prompt_tokens_details", None), "cached_tokens", 0) or 0`、`cache_write = 0`。

**验证：** import 不报错；smoke openai 兼容端点跑两轮，`cached_tokens` 字段被打印（端点支持则 >0）。

## T9: agent 改造**文件：** `src/guolaicode/agent/agent.py`
**依赖：** T1, T2, T3, T6
**步骤：**
1. `Agent.__init__(self, provider, registry, version: str)` 加 `self._version = version` 字段。
2. 加常量 `PLAN_REMINDER_INTERVAL = 4`。
3. `run` 起始：`env = prompt.gather_environment(self._version, self._provider.model)`；`sys = prompt.build_system_prompt()`；`defs` 按 mode 选择（规划=`READ_ONLY_DEFINITIONS`，普通=`DEFINITIONS`）——**移除 suffix 变量**。
4. 每轮迭代算 reminder：
   ```python
   reminder = ""
   if mode == PlanMode.PLAN:
       full = iter_idx == 1 or (iter_idx - 1) % PLAN_REMINDER_INTERVAL == 0
       reminder = prompt.plan_reminder(full)
   ```
5. `_stream_once` 签名改为接收 `sys`、`env_text`、`defs`、`reminder`，内部组装 `llm.Request(messages=conv.messages(), tools=defs, system=llm.System(stable=sys, environment=env_text), reminder=reminder)` 调 `self._provider.stream(req)`。
6. `agent.Usage` 加 `cache_write/cache_read`；`run` 透传 `Event(usage=Usage(input, output, cache_write, cache_read))`。

**验证：** `python -c "from guolaicode.agent.agent import Agent"` 不报错（配合 T10/T11）。

## T10: TUI 与 smoke 接线**文件：** `src/guolaicode/tui/stream.py`、`examples/smoke.py`
**依赖：** T9
**步骤：**
1. `stream.py` 中改 `Agent(self.provider, self.registry, self.version)`；`/do` 注入仍用 `prompt.EXECUTE_DIRECTIVE`（已迁至 `reminder.py`，但通过 `guolaicode.prompt` 包顶层重导出，import 路径不变）。
2. `examples/smoke.py`：`Agent(p, tool.new_default_registry(), "dev")`；消费 `Event.usage` 时打印 `input/output/cache_write/cache_read`；可改为连发两条消息观察次轮 `cache_read`。

**验证：** `python -m guolaicode` 在合法配置下正常启动；`python examples/smoke.py` 能跑通。

## T11: agent 单测适配**文件：** `tests/test_agent.py`
**依赖：** T9
**步骤：**
1. 修 fake provider：`stream(req)` 实现新签名；记录收到的 `req`（`system.stable/environment`、`tools`、`reminder`）。
2. 既有 ch04 场景（A 自然完成、B 上限、C 未知工具、D 并发、E 取消、F 规划只读工具）适配新签名；`Agent(...)` 传 version。
3. 新增断言：
   - 规划模式下 `req.system.stable` 非空且**普通/规划一致**；`req.system.environment` 非空。
   - 规划模式 iter1 的 `req.reminder` 含完整提醒、含 `<system-reminder>`；iter2 为精简版（构造一个让循环多轮的脚本）。
   - 规划模式 `req.tools` 仅只读；普通模式全量。
   - reminder **不写入 conv 持久历史**（`conv.messages()` 不含 reminder 文本）。
   - 缓存用量透传：fake 发 `Usage(cache_write=X, cache_read=Y)` → 收到的 `Event.usage` 携带 X/Y。

**验证：** `pytest tests/test_agent.py` 通过；`pytest -p no:randomly tests/test_agent.py`（如启用 randomly 插件）。

## T12: 全量编译测试与规范**文件：** —
**依赖：** T1–T11
**步骤：**
1. `ruff format --check .`（统一格式）。
2. `ruff check .`（import 分组、无告警）。
3. `pytest`（全量单测通过）。
4. （可选）`mypy src/guolaicode` 通过子集检查。
5. `python -m guolaicode` 能正常启动。

**验证：** 全部通过；检索输出无 api_key 明文。

## 执行顺序

```
T1 ─┐
T2 ─┼─→ T4(prompt 单测)
T3 ─┘
T5(工具描述，独立)

T6(接口) ─┬─→ T7(anthropic) ─┐
          └─→ T8(openai)    ─┤
T1,T2,T3,T6 ─→ T9(agent) ────┼─→ T10(tui/smoke)
                              └─→ T11(agent 单测)

全部 ─→ T12(format/check/test)
```
````

```markdown
# 系统提示工程化 Checklist

> 每一项通过运行代码或观察行为来验证，聚焦系统行为；括号内为验证方式与对应需求。

## 实现完整性
- [ ] 模块化装配：系统提示由按优先级排列的固定模块拼成、模块间空行分隔（验证：`tests/test_prompt.py` 断言身份段在工具使用段之前、以空行分隔）。(AC1/F1)
- [ ] 挂载即扩展：新增一个模块只需出现在模块列表，装配自动按优先级插入，不改 `assemble_system` 逻辑（验证：`tests/test_prompt.py` 传入额外模块断言其落在预期位置）。(AC1/F1)
- [ ] 可选空槽：自定义指令/已激活 Skill/长期记忆内容为空时装配跳过、不留多余空行（验证：`tests/test_prompt.py` 断言空模块不出现、无连续空行）。(AC2/F1)
- [ ] 缓存确定性：连续两次构造稳定系统提示逐字节相等；改变环境信息不改变稳定块（验证：`tests/test_prompt.py` 断言两次 `build_system_prompt()` 相等、稳定块不含 date/git/cwd）。(AC5/F3/N1)
- [ ] 环境信息呈现：系统提示第二段含工作目录、平台、当前日期、git 状态、版本、模型；与稳定块分属不同内容块（验证：`tests/test_prompt.py` 断言 `Environment.render()` 含各项；Anthropic 请求 `system` 为两个文本块）。(AC3/F2)
- [ ] 双重强化：关键约定（优先用专用工具、编辑前必先读）在工具描述与系统提示模块文本中均出现（验证：`tests/test_prompt.py` 断言系统提示含相关语句；检索 `edit_file`/`bash` `DESCRIPTION` 含强化语句）。(AC7/F5)
- [ ] 补充消息注入机制：`plan_reminder` 输出以 `<system-reminder>` 标签包裹（验证：`tests/test_prompt.py` 断言标签存在）。(AC8/F6)
- [ ] 缓存字段解析：provider 用量对外暴露缓存写/读；Anthropic 取 `cache_creation_input_tokens`/`cache_read_input_tokens`，OpenAI 取 `cached_tokens`，缺字段为 0 不抛异常（验证：`tests/test_agent.py` fake 发缓存用量断言 `Event.usage` 透传；smoke 打印真实字段）。(AC6/F4/N6)
- [ ] Anthropic 缓存断点真实发出：稳定块序列化后带 `cache_control: {"type": "ephemeral"}`、环境块不带（验证：`tests/test_anthropic_system.py` 断言——守护回归）。(AC4/F3)

## 集成
- [ ] 规划模式按轮次注入：`/plan` 后 iter1 注入完整提醒、间隔轮（每 4）重复完整、其余轮精简；reminder 不写入持久历史（验证：`tests/test_agent.py` 多轮脚本断言各轮 `req.reminder` 详略 + `conv.messages()` 不含 reminder 文本）。(AC9/F6/F7)
- [ ] 规划模式工具集：规划模式 `req.tools` 仅只读、普通模式全量（验证：`tests/test_agent.py` 断言两模式 tools 差异）。(AC9/F7)
- [ ] 稳定系统提示跨模式一致：普通与规划模式 `req.system.stable` 相同（规划提醒已移出系统通道）（验证：`tests/test_agent.py` 断言两模式 stable 相等）。(F7/N1)
- [ ] 历史合法：注入 reminder 后请求消息序列角色合法（Anthropic 并入末条 user、不产生连续 user）（验证：`tests/test_agent.py` 断言织入后末条仍为单一 user 回合；端到端 plan 多轮不报 400）。(AC12/N3)
- [ ] 跨协议一致：anthropic 与 openai（含兼容 base_url）都装配同一系统提示 + 环境段、注入同一 reminder（验证：两配置各跑多轮；anthropic 看缓存命中、openai 看 `cached_tokens`）。(AC10/F8)
- [ ] 不破坏 ch04：多轮连环、用户取消、流出错恢复、历史一致仍成立（验证：跑 ch04 端到端关键场景；`pytest` 通过）。(AC11/N2)
- [ ] 环境采集降级：非 git 目录/git 不可用时环境段对应项省略、不卡界面、请求正常发起（验证：在非 git 临时目录跑 smoke/TUI 观察）。(AC13/N4)
- [ ] 界面不阻塞：环境采集（含 git 外调）不冻结界面、不显著拖慢首字（验证：正常目录跑，观察发起延迟无异常；git 调用以 `subprocess.run(..., timeout=2)` 或 `asyncio.to_thread` 收口）。(N4)

## 编译与测试
- [ ] `python -m guolaicode` 在合法配置下能正常启动。
- [ ] `ruff check .` 无告警。
- [ ] `ruff format --check .` 通过。
- [ ] `pytest` 通过（`tests/test_config.py`、`tests/test_conversation.py`、`tests/test_tool.py`、`tests/test_agent.py`、`tests/test_prompt.py`、`tests/test_anthropic_system.py`）。
- [ ] （可选）`mypy src/guolaicode` 通过子集检查。(N2/N6)
- [ ] 密钥不回显：对话区、环境段与任何输出均不出现 `api_key`（验证：通读输出、检索无明文 key；确认环境段不含环境变量）。(AC14/N5)

## 端到端场景（tmux 实跑）
- [ ] 场景 1（缓存命中，Anthropic）：同一会话连发两条消息 → smoke/调试打印首轮 `cache_write > 0`、次轮 `cache_read > 0`，证明稳定前缀被缓存复用。(AC4/F3)
- [ ] 场景 2（规划模式按轮次）：`/plan` 发一个需多步只读调研的任务 → 模型仅用只读工具产出计划、首轮注入完整提醒、后续轮精简；`/do` 切回全工具并按计划执行（产生写/执行类调用）。(AC9/F7)
- [ ] 场景 3（reminder 不被当用户输入）：规划模式下模型不复述/回应 `<system-reminder>` 内容，而是据其约束行事。(AC8/F6)
- [ ] 场景 4（环境感知）：问「我现在在哪个目录、什么平台、今天几号」→ 模型据环境段正确回答。(AC3/F2)
- [ ] 场景 5（取消后可继续）：规划模式多轮中途按 Esc 取消 → 回空闲态、再发一条继续对话不报 400。(AC12/N3)
- [ ] 场景 6（非 git 目录降级）：在非 git 目录启动 → 环境段省略 git 状态、正常对话。(AC13/N4)
```

### Java

```markdown
# 系统提示工程化 Spec## 背景ch04 给 GuoLaiCode 装上了 Agent Loop——能自主多轮想 → 调工具 → 看结果地干活。但「**干活的指令**」还很原始:系统提示是一段揉在一起的固定常量(身份、工具用法、简洁性挤在同一块文本里),没有结构、没有优先级、想加一类新指令只能往字符串里塞;每轮请求都把整段系统提示与全部工具定义**原样重发**,稳定不变的内容反复计费、反复占用首字延迟;模型对**当前环境**(工作目录、平台、日期、git 状态)一无所知;规划模式提醒每轮无差别地拼在系统提示尾部,注入频率不可控。

ch05 给 GuoLaiCode 做**系统提示工程化**:把全局指令拆成按职责划分、按优先级拼装的模块;把稳定内容与变化内容分到**可缓存通道**与**消息通道**,让稳定的指令和工具定义走缓存省钱省首字延迟;引入一种带特殊标签的**补充消息注入**机制,在运行中注入指令而不污染缓存、不被模型当成用户提问来回复;会话级开关(规划模式)的提醒按**轮次频率**注入。这是从「能干活」到「干得好」的一步。

## 目标

- 把全局指令拆成按职责划分的若干模块,按优先级拼装成完整系统提示;新增一类指令只需挂载新模块,不改装配逻辑。
- 区分稳定与变化内容:稳定的系统模块与工具定义走**可缓存通道**;环境信息与对话历史走**不缓存的通道**。
- 在工具描述与系统提示中,对关键约定(优先用专用工具、编辑前必先读)**双重强化**,提高遵守率。
- 提供一种带特殊标签的**补充消息注入**机制:运行中注入补充指令,既不污染缓存,也不会被模型当成用户输入来回复。
- 会话级开关(规划模式)的提醒按轮次注入:**首轮完整、间隔轮次重复完整、其余轮次精简**,控制注入频率。
- 能从 provider 返回中**解析缓存命中信息**(写入 / 读取 token),用于验证缓存策略真实生效。

## 功能需求

- F1: 模块化系统提示拼装
  把全局指令按职责拆成模块,每个模块带名称、优先级、内容。装配时按优先级从高到低拼接成完整系统提示,模块间以空行分隔。固定模块七个,按优先级排:**身份 → 系统约束 → 任务模式 → 动作执行 → 工具使用 → 语气风格 → 文本输出**。其后预留三个可选模块位置:**自定义指令、已激活 Skill、长期记忆**——本章这三个**不接入真实内容来源**(留待后续章节),装配时内容为空则跳过、不留多余空行。新增一类指令只需定义新模块挂到对应优先级,不改装配主逻辑。

- F2: 环境信息构造与呈现
  收集运行环境信息(工作目录、操作系统/平台、当前日期、git 状态、应用版本、当前模型等),构造成一段供模型感知环境的文本。环境信息作为系统提示的**独立第二段**呈现——结构上接在固定模块之后,物理上与可缓存的稳定模块**分属不同内容块**(见 F3),自身不进缓存。

- F3: 稳定内容走可缓存通道
  稳定不变的内容(拼装好的稳定系统模块 + 工具定义)归入**可缓存通道**,变化内容(环境信息 + 对话历史)归入**不缓存通道**。可缓存内容必须**跨轮逐字节一致**(不含任何随轮次变化的成分),否则缓存无法命中。Anthropic 对稳定内容显式标记缓存断点,使工具定义与稳定系统模块被缓存复用,环境信息块与历史在断点之后不缓存;OpenAI 把稳定内容置于请求前缀,靠端点前缀缓存自动受益(不强制端点支持)。

- F4: 缓存命中解析
  从 provider 响应用量中解析缓存字段——**缓存写入**(首次创建缓存的 token)与**缓存读取**(命中复用的 token),随用量一并对外暴露,供烟囱/调试程序打印验证缓存是否真生效。Anthropic 解析其缓存创建/读取字段;OpenAI 解析其缓存命中字段(端点提供则有,否则为零)。

- F5: 关键约定双重强化
  对模型最易违反的关键约定(如「优先用专用工具而非 shell 拼凑」「编辑文件前必先读取」),在**工具描述**与**系统提示模块**中双重表述,提高遵守率。

- F6: 补充消息注入机制(system-reminder)
  提供一种带特殊标签(如 `<system-reminder>`)的补充消息注入形式:构造请求时把补充指令注入**消息通道**,标签语义让模型理解这是系统补充上下文而非用户提问——不针对它直接回复。此类消息**每轮动态构造、不写入持久历史**,因而不污染缓存、不破坏历史角色交替。

- F7: 规划模式按轮次注入
  规划模式提醒改由 F6 机制承载(不再拼到系统提示尾部)。按 Agent Loop 轮次控制注入**频率与详略**:**首轮注入完整提醒,之后每隔固定轮次重复一次完整提醒,其余轮次注入精简版**。模式跨轮保持,`/plan` 进入、`/do` 退出并触发按计划执行;规划模式下仍仅放开只读工具(均沿用 ch04)。

- F8: 跨协议一致
  Anthropic 与 OpenAI(含兼容 base_url)都装配同一套模块化系统提示 + 环境信息、都注入相同补充消息、都尽力走缓存(Anthropic 显式断点 / OpenAI 前缀顺序)。两协议在模块内容、注入时机、规划模式注入节奏上**行为一致**;缓存命中解析在各自字段可得时生效。

## 非功能需求- N1: 缓存确定性——可缓存块(工具定义 + 稳定系统模块)在跨轮、会话内多轮之间**逐字节稳定**:模块装配顺序、工具顺序固定,不混入任何随轮次/时间变化的成分。否则缓存永不命中,本章核心目标落空。
- N2: 不破坏 ch04 行为——Agent Loop 多轮编排、流式双路收集、保序分批并发、用户取消、历史一致、用量/迭代进度展示等 ch04 能力**不退化**。系统提示重构与消息注入是叠加,不改这些既有路径的语义。
- N3: 历史合法性——每轮动态注入的补充消息(不持久)拼进请求后,消息序列仍**角色交替合法、工具调用与结果配对不破坏**,不致 provider 返回 400;补充消息不写入持久历史,故不影响后续轮次与可恢复性。
- N4: 环境采集不阻塞、可降级——环境信息采集(含可能外调 shell 的 git 状态)**快速且有界**,不阻塞界面、不显著拖慢请求发起;任一项取不到时降级(留空/省略),不中断会话。
- N5: 密钥与敏感信息不泄漏——沿用 ch03/ch04,对话区与任何输出均不出现 api_key;环境信息构造**不带入密钥或敏感环境变量**。
- N6: 缓存字段解析健壮——provider / 兼容端点未返回缓存字段时按零处理,不 panic、不因缺字段中断本轮或会话。
- N7: 代码规范——`mvn spotless:check`(google-java-format)合规;无明显警告(遵循项目 CLAUDE.md)。
- N8: 可扩展性——新增一类系统提示模块或一类补充消息提醒时,改动局部:不触及装配主逻辑,不改 provider 适配层对外签名的语义稳定性。

## 不做的事- **项目指令文件加载**——「自定义指令」为预留空槽,本章不从 CLAUDE.md / 项目级指令文件读取内容(留待后续章节)。
- **自动记忆 / 长期记忆**——「长期记忆」为预留空槽,本章不做记忆写入与召回。
- **真实 MCP 接入**——「已激活 Skill」等可选模块为预留空槽,不接入 MCP 工具/资源。
- **自动化评估 / 评测框架**——本章只做缓存字段解析验证 + 一组典型场景人工对比的定性评估,不建评测流水线。
- **历史增量缓存**——只缓存稳定前缀(工具定义 + 稳定系统模块),不对对话历史另打缓存断点。
- **缓存 TTL / 断点数配置化**——TTL 用默认值、断点位置内置,本章不通过配置调整。
- **状态栏展示缓存命中**——缓存信息只走 smoke / 调试输出,不进 TUI 状态栏。
- **提示词运行时热加载 / 外部模板文件**——模块内容内置于代码,不做外部模板。
- **多模态、流式工具结果**——沿用 ch04,不做。

## 验收标准

- AC1: 模块化装配——系统提示由按优先级排列的固定模块拼成,模块间以空行分隔;新增一个模块只需挂载、不改装配逻辑。(F1)
- AC2: 可选空槽——「自定义指令/已激活 Skill/长期记忆」内容为空时装配自动跳过、不留多余空行;架构支持后续填充。(F1)
- AC3: 环境信息呈现——系统提示第二段含工作目录、平台、当前日期、git 状态、应用版本、当前模型;与稳定模块分属不同内容块。(F2)
- AC4: 缓存命中(Anthropic)——首轮 `cache_creation_input_tokens > 0`、次轮 `cache_read_input_tokens > 0`(稳定前缀命中);仅环境信息/历史变化时稳定前缀仍命中。(F3/N1)
- AC5: 缓存确定性——两次构造的稳定块逐字节相等;故意改变环境信息不改变稳定块内容。(F3/N1)
- AC6: 缓存字段解析——provider 用量对外暴露缓存写/读;OpenAI 解析 `cached_tokens`,端点不返回时为 0 且不报错。(F4/N6)
- AC7: 双重强化——关键约定(优先用专用工具、编辑前必先读)在**工具描述**与**系统提示模块**文本中均出现。(F5)
- AC8: 补充消息注入——注入消息以 `<system-reminder>` 标签包裹,模型不把它当用户提问来复述/回应;该消息**不写入持久对话历史**。(F6/N3)
- AC9: 规划模式按轮次注入——`/plan` 后:首轮注入完整提醒、之后每隔固定轮次重复完整、其余轮次注入精简版;规划模式仅放开只读工具;`/do` 切回全工具并按上文计划执行。(F7)
- AC10: 跨协议一致——anthropic 与 openai(含兼容 base_url)都装配同一系统提示 + 环境信息、注入同一 reminder、都尽力缓存(anthropic 看缓存命中、openai 看 `cached_tokens`),行为一致。(F8)
- AC11: 不破坏 ch04——ch04 关键行为(多轮连环、用户取消、历史一致、流出错恢复)仍成立;`mvn test` 通过。(N2)
- AC12: 历史合法——注入 reminder 后请求不返回 400;规划模式多轮 + 中途取消后再发一条仍可继续(历史配对合法)。(N3)
- AC13: 环境采集降级——在非 git 目录或 git 不可用时,环境段对应项降级(留空/省略),界面不卡、请求正常发起。(N4)
- AC14: 代码规范——`mvn spotless:check` 通过;对话区与输出无 api_key 明文。(N5/N7)
```

````markdown
# 系统提示工程化 Plan

> 技术栈:Java 21(LTS、virtual thread / record / sealed / pattern matching);Maven;`com.anthropic:anthropic-java` 2.x、`com.openai:openai-java` 4.x。所有 SDK 缓存 API 已对 Maven Central 上的官方 SDK 类型核实(见技术决策表)。

## 架构概览ch05 在三层叠加,**不改 ch04 的 Agent Loop 控制流**:

- **prompt 包(重写)**:从「单个常量字符串」升级为「模块化装配 + 环境采集 + 补充消息构造」。对外产出三类文本——**稳定系统提示**(可缓存)、**环境信息段**(不缓存)、**system-reminder 包裹的补充指令**。prompt 包不依赖 llm 包(避免环依赖)。
- **llm 包(改造)**:`Provider.stream` 入参从位置参数改为 `Request` record,承载 `messages / tools / system{stable,environment} / reminder`。Anthropic 把 stable 块打缓存断点、env 块不打;OpenAI 把 stable 置于系统消息前缀。`Usage` 增加缓存写/读字段。两 provider 把 `reminder` 按各自协议安全地织入消息通道(N3)。
- **agent 包(改造)**:每次 `run` 开始采集环境、装配稳定系统提示;每轮迭代按 `mode + iter` 计算本轮 reminder(规划模式按轮次详略),组装 `Request` 发起请求;把缓存用量透传到 `Event.usage`。
- **smoke(改造)**:打印每轮用量的缓存写/读字段,作为缓存策略生效的验证手段(TUI 状态栏不变)。

数据流:`agent.run` → `Prompt.buildSystemPrompt()`(稳定) + `Environment.gather(...).render()`(环境) + `Reminder.plan(full)`(本轮补充) → 组装 `llm.Request` → `provider.stream` → Anthropic/OpenAI 各自装配缓存通道与消息通道 → 流式事件回到 agent → `Event{usage:{...cacheWrite,cacheRead}}` → smoke 打印。

## 核心数据结构### prompt.Module(新增)
```java
package dev.guolaicode.prompt;

public record Module(
        String name,    // 模块标识(身份、系统约束 …),仅用于可读性与测试断言
        int priority,   // 数值越小优先级越高、排越前;固定模块 10..70,可选模块 80..100
        String content  // 模块正文;为空则装配时跳过(可选空槽)
) {}
```

### prompt.Environment(新增)
```java
package dev.guolaicode.prompt;

public record Environment(
        String workingDir, // System.getProperty("user.dir")
        String platform,   // System.getProperty("os.name")
        String date,       // LocalDate.now().toString()
        String gitStatus,  // `git status --porcelain` 摘要;非 git 目录/取不到则留空
        String version,    // 应用版本(从 agent 透传)
        String model       // provider.model()
) {
    public static Environment gather(String version, String model) { /* … */ }
    public String render() { /* … */ }
}
```

### llm.System(新增)
```java
package dev.guolaicode.llm;

public record System(
        String stable,      // 可缓存:装配好的稳定系统提示(工具定义随 tools 一并进缓存前缀)
        String environment  // 不缓存:环境信息段
) {}
```

### llm.Request(新增,替换 stream 位置参数)
```java
package dev.guolaicode.llm;

public record Request(
        java.util.List<Message> messages,   // 持久对话历史(不含本轮 reminder)
        java.util.List<ToolDefinition> tools, // 本轮工具集(普通=全量 / 规划=只读)
        System system,                       // 稳定系统提示 + 环境段
        String reminder                      // 本轮 system-reminder 内容(已含标签;空=不注入)
) {}
```

### llm.Usage(扩展)
```java
public record Usage(
        long inputTokens,
        long outputTokens,
        long cacheWrite, // Anthropic: cacheCreationInputTokens;OpenAI: 恒 0(自动缓存无写计数)
        long cacheRead   // Anthropic: cacheReadInputTokens;OpenAI: promptTokensDetails.cachedTokens
) {}
```

### agent.Usage(扩展,对外事件)
```java
public record Usage(
        long input,
        long output,
        long cacheWrite,
        long cacheRead    // 透传自 llm.Usage,供 smoke 打印
) {}
```

## 核心接口### prompt 包
```java
public final class Prompt {
    public static java.util.List<Module> fixedModules();      // 七个固定模块(身份…文本输出),内容内置
    public static java.util.List<Module> optionalModules();   // 三个可选空槽(自定义指令/已激活Skill/长期记忆),content=""
    public static String assembleSystem(java.util.List<Module> mods); // 按 priority 升序、跳过空 content、以 "\n\n" 连接
    public static String buildSystemPrompt();                 // = assembleSystem(append(fixedModules(), optionalModules()...))
}

public final class Reminder {
    public static String systemReminder(String body);   // 用 <system-reminder>…</system-reminder> 包裹 body
    public static String plan(boolean full);            // 返回包好标签的规划模式提醒(full=完整 / 否=精简)
    public static final String EXECUTE_DIRECTIVE = "…"; // /do 注入的用户消息文案
}
```

### llm.Provider(签名变更)
```java
public interface Provider {
    String name();
    String model();
    java.util.concurrent.Flow.Publisher<StreamEvent> stream(Request req); // 由 Request 承载全部入参
}
```

## 模块设计### prompt 包
**职责:** 模块化装配稳定系统提示;采集并渲染环境信息;构造 system-reminder 与规划模式提醒。
**对外接口:** 见上。
**关键点:**
- 七个固定模块按优先级排:**身份(10) → 系统约束(20) → 任务模式(30) → 动作执行(40) → 工具使用(50) → 语气风格(60) → 文本输出(70)**;可选空槽:**自定义指令(80) → 已激活 Skill(90) → 长期记忆(100)**(content="" 跳过)。
- **F5 双重强化**写在「工具使用(50)」模块:明确「优先用专用工具(read_file/glob/grep)而非用 bash 拼凑」「编辑文件前必须先 read_file 读取」;同时同义强化到 `EditFileTool`、`BashTool` 的 description(见 tool 包改动)。
- `assembleSystem` 只用常量内容 → 跨轮逐字节一致(**N1**);环境与时间相关内容只进 `Environment`,绝不进稳定模块。
- `Environment.gather`:git 状态用一条 `git status --porcelain`(`ProcessBuilder` + 短超时执行),失败/非 git 目录则 `gitStatus=""`;不读取任何环境变量(**N5**)。
**依赖:** JDK 标准库(`java.lang.System` 读 `user.dir`/`os.name`、`java.time.LocalDate`、`java.lang.ProcessBuilder`);不依赖 llm。

### llm 包(Provider.java / AnthropicProvider.java / OpenAIProvider.java)
**职责:** 把 `Request` 装配为各协议请求,分离缓存通道与消息通道,解析缓存用量,安全织入 reminder。
**对外接口:** `stream(Request)`。
**关键点:**
- 删除 `effectiveSystem` 与对 `dev.guolaicode.prompt` 包的 import(系统提示改由 agent 传入)。
- **Anthropic**:用 `MessageCreateParams.builder().system(List<TextBlockParam>)` 传**两块** TextBlockParam:`req.system().stable()` 非空 → `TextBlockParam.builder().text(stable).cacheControl(CacheControlEphemeral.builder().build()).build()`(断点,默认 5m TTL);`req.system().environment()` 非空 → `TextBlockParam.builder().text(env).build()`(无 cacheControl)。请求顺序 tools→system→messages,断点打在稳定块 → **缓存前缀 = 全部工具 + 稳定块**;env 与历史在断点后不缓存,env 每轮变化不影响前缀命中。`Usage.cacheWrite = acc.usage().cacheCreationInputTokens()`、`cacheRead = acc.usage().cacheReadInputTokens()`。
  - reminder 织入:`req.reminder() != null && !req.reminder().isEmpty()` 时,把 `ContentBlockParam.ofText(TextBlockParam.builder().text(reminder).build())` **追加到最后一条消息的 content 块列表**(循环中最后一条恒为 user 或 tool_result→user,追加文本块仍是合法 user 消息,保 N3 角色交替);极端情形(末尾为 assistant)则新起一条 user 消息。
- **OpenAI**:首条 system 消息 = `req.system().stable()`(若 `environment` 非空则拼为 `stable + "\n\n" + environment` 单条 system 消息——兼容端点对多条 system 消息支持不一,统一单条);stable 居前缀 → 端点前缀缓存命中稳定部分。`Usage.cacheRead = acc.usage().promptTokensDetails().cachedTokens()`、`cacheWrite = 0`。
  - reminder 织入:`req.reminder()` 非空 → **追加一条尾部 user 消息**(OpenAI 容忍连续 user / tool 后接 user)。
- **N6**:缓存字段缺失即零值(`Optional.orElse(0L)`),不额外校验、不报错。

### agent 包(Agent.java)
**职责:** 采集环境、装配系统提示、按轮次构造 reminder、组装 Request、透传缓存用量。
**关键点:**
- 构造器 `Agent(Provider p, ToolRegistry r, String version)` 增加 `version` 字段(供环境段);`model()` 取 `p.model()`。
- `run` 起始:`var env = Environment.gather(this.version, provider.model())`、`var sys = Prompt.buildSystemPrompt()`(稳定,普通/规划模式一致——规划提醒已移出系统通道)。
- 每轮迭代计算 reminder:`mode == Mode.PLAN` → `Reminder.plan(full)`,`full = (iter == 1 || (iter - 1) % PLAN_REMINDER_INTERVAL == 0)`;否则 `""`。`PLAN_REMINDER_INTERVAL = 4`(内置常量)。
- `streamOnce` 组装 `new llm.Request(conv.messages(), defs, new llm.System(sys, env.render()), reminder)` 调 `provider.stream`。
- 删除 `suffix`/`readOnlyDefinitions` 的「系统后缀」用法;**只读工具集仍按 mode 选择**(规划=`ToolRegistry.readOnlyDefinitions()`),`PLAN_MODE_REMINDER` 常量从系统后缀迁移为 `Reminder.plan(...)` 的内容。
- 缓存用量透传:`new Event.Usage(input, output, cacheWrite, cacheRead)`。

### smoke(`src/main/java/dev/guolaicode/smoke/SmokeMain.java`)
**职责:** 端到端验证缓存生效。
**关键点:** 消费 `Event.usage` 时打印 `input/output/cache_write/cache_read`;跑两轮(或多轮)观察次轮 `cacheRead > 0`。`new Agent(p, ToolRegistry.defaultRegistry(), "dev")`。

### tool 包(描述微调,F5)
- `EditFileTool.description()`:补「编辑前请先用 read_file 读取目标文件,确认 old_string 唯一」。
- `BashTool.description()`:补「读文件/找文件/搜内容请优先用 read_file/glob/grep,不要用 bash 拼凑」。
- 仅改描述文本,不改行为、不改 schema(N2)。

## 模块交互

```
TUI/smoke ──run(conv,mode)──> agent
  agent.run:
    env  = Environment.gather(version, provider.model())
    sys  = Prompt.buildSystemPrompt()
    for iter:
      reminder = (mode == PLAN) ? Reminder.plan(full(iter)) : ""
      req = new llm.Request(conv.messages(), defs(mode), new llm.System(sys, env.render()), reminder)
      provider.stream(req) ──Flow.Publisher──> StreamEvent{TextDelta / ToolCalls / Usage(+cache) / Done / Failed}
    Event{usage:{...cacheWrite, cacheRead}} ──> smoke 打印 / TUI 状态栏(忽略 cache 字段)
```

依赖方向(无环):`agent → {prompt, llm, conversation, tool}`;`llm → config`(不再 import prompt);`prompt → JDK 标准库`。

## 文件组织

```
guolaicode/
├── src/main/java/dev/guolaicode/prompt/
│   ├── Prompt.java         — 改:Module 引用/装配/buildSystemPrompt;保留 banner(CAT_BANNER/renderBanner/READY_HINT)
│   ├── Module.java         — 新:Module record
│   ├── Modules.java        — 新:fixedModules()/optionalModules() 七固定+三空槽的内容常量
│   ├── Environment.java    — 新:Environment record / gather / render
│   └── Reminder.java       — 新:systemReminder/plan(完整版/精简版常量)/EXECUTE_DIRECTIVE
├── src/main/java/dev/guolaicode/llm/
│   ├── Provider.java       — 改:Provider 接口签名 stream(Request)
│   ├── Request.java        — 新:Request record
│   ├── System.java         — 新:System record(stable + environment)
│   ├── Usage.java          — 改:加 cacheWrite/cacheRead
│   ├── AnthropicProvider.java — 改:两块 system(断点 + env)、缓存用量解析、reminder 织入末条 user
│   └── OpenAIProvider.java    — 改:单条 system(stable+env)、cachedTokens 解析、reminder 尾部注入
├── src/main/java/dev/guolaicode/agent/
│   └── Agent.java          — 改:构造器加 version、run 采集环境/装配系统、按轮次 reminder、缓存透传
├── src/main/java/dev/guolaicode/tool/
│   ├── EditFileTool.java   — 改:description() 补强化
│   └── BashTool.java       — 改:description() 补强化
├── src/main/java/dev/guolaicode/tui/
│   └── StreamPump.java     — 改:new Agent(...) 传 version(TuiApp.version 已有)
├── src/main/java/dev/guolaicode/smoke/
│   └── SmokeMain.java      — 改:打印缓存用量;new Agent(p, registry, "dev")
└── src/test/java/dev/guolaicode/
    ├── prompt/PromptTest.java   — 新:装配顺序/跳空槽/N1 确定性/双重强化文本 断言
    ├── prompt/EnvironmentTest.java — 新:非 git 目录降级、Render 含 cwd/platform/date
    ├── prompt/ReminderTest.java — 新:<system-reminder> 标签 + 完整/精简
    ├── llm/AnthropicSystemTest.java — 新:序列化 toAnthropicSystem 断言 cacheControl 仅出现在 stable 块
    └── agent/AgentTest.java     — 改/新:断言 Request 装配(system 两段、规划按轮次 reminder)、缓存用量透传
```

## 技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 系统提示组织 | 模块化(`Module(name, priority, content)` record + `assembleSystem`) | 满足 F1「挂载即扩展」;优先级排序使顺序确定(N1) |
| 环境信息归属 | system 通道独立第二块(用户拍板) | 结构上接系统提示之后;物理上与稳定块分离,不进缓存 |
| Anthropic 缓存断点 | 仅在稳定 system 块打 `cacheControl(CacheControlEphemeral)`(默认 5m) | 请求序 tools→system→messages,断点在稳定块即缓存「工具+稳定块」整段前缀;env 在其后不缓存,env 变化不冲前缀命中(SDK 核实:`TextBlockParam.cacheControl` 字段存在) |
| 工具是否单独打断点 | 否 | 稳定块断点的前缀已含全部工具,无需 `ToolUnion.toolWithCacheControl` 再标 |
| OpenAI 环境信息 | 拼入单条 system 消息(stable 在前) | 兼容端点对多条 system 支持不一;stable 居前缀,端点前缀缓存自动命中稳定部分。代价:env 居 system 尾,OpenAI 工具可能不进缓存前缀——本章 OpenAI 缓存为尽力而为、不强制(F8) |
| 缓存用量字段 | `Usage` 加 `cacheWrite/cacheRead` | Anthropic 取 `cacheCreationInputTokens/cacheReadInputTokens`;OpenAI 取 `promptTokensDetails().cachedTokens()`(SDK 核实) |
| stream 入参 | 改 `Request` record | 入参从 4 个增至含 system/reminder,record 更清晰、后续扩展不再改签名(N8) |
| reminder 注入位置 | Anthropic 并入末条 user 消息 content 块;OpenAI 追加尾部 user 消息 | Anthropic 严格角色交替——并入避免连续 user 触发 400(N3);OpenAI 容忍连续 user |
| reminder 持久化 | 不写入 `Conversation`(用户拍板) | 每轮动态构造;不污染缓存、不破坏历史可恢复性 |
| 规划提醒节奏 | `iter == 1` 或 `(iter - 1) % 4 == 0` → 完整,否则精简(per run 内 iter) | 实现 F7「首轮完整、间隔重复、其余精简」;复用已有 iter 计数 |
| 缓存验证呈现 | smoke/调试打印(用户拍板) | 不动 TUI 状态栏;`Usage` 携带字段供打印 |
| prompt↔llm 依赖 | 系统提示由 agent 传入,llm 不再 import prompt | 打破潜在环依赖;职责更清晰 |
| 异步并发 | 沿用 ch04:virtual thread + `SubmissionPublisher`/`Flow` | 与 ch04 一致,不引入新机制(N2) |
````

````markdown
# 系统提示工程化 Tasks

> 顶层包:`dev.guolaicode`(Java 21 / Maven)。构建产物在 `target/`。

## 文件清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `src/main/java/dev/guolaicode/prompt/Module.java` | `Module` record |
| 新建 | `src/main/java/dev/guolaicode/prompt/Modules.java` | `fixedModules()` 七固定模块、`optionalModules()` 三空槽的内容常量 |
| 改   | `src/main/java/dev/guolaicode/prompt/Prompt.java` | `assembleSystem`/`buildSystemPrompt`;删旧 `SYSTEM_PROMPT`/`PLAN_MODE_REMINDER`/`EXECUTE_DIRECTIVE` 常量(迁移);保留 banner |
| 新建 | `src/main/java/dev/guolaicode/prompt/Environment.java` | `Environment` record、`gather`、`render` |
| 新建 | `src/main/java/dev/guolaicode/prompt/Reminder.java` | `systemReminder` 标签包裹、`plan(boolean full)`、规划提醒完整/精简常量、`EXECUTE_DIRECTIVE` |
| 新建 | `src/test/java/dev/guolaicode/prompt/PromptTest.java` | 装配顺序、跳空槽、N1 确定性、双重强化文本断言 |
| 新建 | `src/test/java/dev/guolaicode/prompt/EnvironmentTest.java` | 非 git 降级、`render` 含 cwd/platform/date |
| 新建 | `src/test/java/dev/guolaicode/prompt/ReminderTest.java` | `<system-reminder>` 标签 + 完整/精简 |
| 改   | `src/main/java/dev/guolaicode/tool/EditFileTool.java` | `description()` 补「编辑前先 read_file」 |
| 改   | `src/main/java/dev/guolaicode/tool/BashTool.java` | `description()` 补「优先用专用工具而非 bash 拼凑」 |
| 新建 | `src/main/java/dev/guolaicode/llm/Request.java` | `Request` record |
| 新建 | `src/main/java/dev/guolaicode/llm/System.java` | `System` record(stable + environment) |
| 改   | `src/main/java/dev/guolaicode/llm/Usage.java` | 加 `cacheWrite`/`cacheRead` |
| 改   | `src/main/java/dev/guolaicode/llm/Provider.java` | `stream(Request)`;删 `effectiveSystem` 及 prompt import |
| 改   | `src/main/java/dev/guolaicode/llm/AnthropicProvider.java` | 两块 system(稳定块打断点 + env 块)、缓存用量解析、reminder 并入末条 user |
| 改   | `src/main/java/dev/guolaicode/llm/OpenAIProvider.java` | 单条 system(stable+env 拼接)、`cachedTokens` 解析、reminder 追加尾部 user |
| 新建 | `src/test/java/dev/guolaicode/llm/AnthropicSystemTest.java` | 序列化断言 `cacheControl` 只挂在 stable 块、env 块无 |
| 改   | `src/main/java/dev/guolaicode/agent/Agent.java` | 构造器加 `version`;`run` 采集环境/装配系统;按轮次 reminder;缓存用量透传 |
| 改   | `src/test/java/dev/guolaicode/agent/AgentTest.java` | 断言 Request 装配(system 两段、规划按轮次 reminder)、缓存用量透传;修既有用例适配新签名 |
| 改   | `src/main/java/dev/guolaicode/tui/StreamPump.java` | `new Agent(...)` 传 `tuiApp.version()` |
| 改   | `src/main/java/dev/guolaicode/smoke/SmokeMain.java` | 打印缓存用量;`new Agent(p, registry, "dev")` |

---

## T1: prompt 模块化装配**文件:** `src/main/java/dev/guolaicode/prompt/Module.java`、`src/main/java/dev/guolaicode/prompt/Modules.java`、`src/main/java/dev/guolaicode/prompt/Prompt.java`
**依赖:** 无
**步骤:**
1. 在 `Module.java` 定义 `public record Module(String name, int priority, String content) {}`。
2. `Modules.fixedModules()` 返回 `List<Module>`,七个固定模块,内容内置(中英按现有 `SYSTEM_PROMPT` 风格,英文为主):
   - 身份(10):GuoLaiCode 是终端编码 Agent。
   - 系统约束(20):操作边界——在工作目录约定内行事、不外泄密钥、对破坏性操作谨慎。
   - 任务模式(30):ReAct——多步推进、读后再改、完成才给终答。
   - 动作执行(40):何时调工具、连续只读可并发、有副作用谨慎。
   - 工具使用(50):**优先用 read_file/glob/grep 而非 bash 拼凑;编辑文件前必先 read_file**(F5)。
   - 语气风格(60):简洁、直接、不奉承。
   - 文本输出(70):必要时用 Markdown(代码块/列表),终答精炼。
3. `Modules.optionalModules()` 返回三个空槽:自定义指令(80)、已激活 Skill(90)、长期记忆(100),`content` 均为 `""`。
4. 在 `Prompt.java`:
   - `assembleSystem(List<Module> mods)`:按 `priority` 升序 `Collections.sort`(或 `stream().sorted()`)、**跳过 `content.isEmpty()`**、用 `String.join("\n\n", ...)` 连接。
   - `buildSystemPrompt()`:`assembleSystem(Stream.concat(fixedModules().stream(), optionalModules().stream()).toList())`。
   - 删除旧 `SYSTEM_PROMPT`、`PLAN_MODE_REMINDER` 常量(内容迁至模块/reminder);`EXECUTE_DIRECTIVE` 迁至 `Reminder.java`。保留 `CAT_BANNER`/`READY_HINT`/`renderBanner`。

**验证:** `mvn -q compile`;临时 `System.out.println(Prompt.buildSystemPrompt())` 观察七模块按序、空槽不留空行。

## T2: 环境采集与渲染**文件:** `src/main/java/dev/guolaicode/prompt/Environment.java`
**依赖:** 无
**步骤:**
1. 定义 `public record Environment(String workingDir, String platform, String date, String gitStatus, String version, String model)`。
2. `public static Environment gather(String version, String model)`:
   - `workingDir = java.lang.System.getProperty("user.dir")`(为空降级 `""`)、`platform = java.lang.System.getProperty("os.name")`、`date = java.time.LocalDate.now().toString()`。
   - `gitStatus`:用 `new ProcessBuilder("git", "status", "--porcelain").redirectErrorStream(true).start()`,`process.waitFor(2, TimeUnit.SECONDS)`,非零退出/非 git 目录/超时/`IOException` → `""`;有输出则取摘要(如「N 个文件改动」或前几行)。
   - `version=version`、`model=model`。**不读任何环境变量**(N5)。
3. `public String render()`:渲染为「环境信息」段——逐行 `Key: Value`,空值项省略。

**验证:** 单测 `EnvironmentTest` 在临时非 git 目录调用 `gather` 得 `gitStatus.isEmpty()` 且不抛;`render()` 含 cwd/platform/date。

## T3: 补充消息与规划提醒构造**文件:** `src/main/java/dev/guolaicode/prompt/Reminder.java`
**依赖:** 无
**步骤:**
1. `public static String systemReminder(String body)`:返回 `"<system-reminder>\n" + body + "\n</system-reminder>"`。
2. 规划提醒常量:`PLAN_REMINDER_FULL`(完整版,含「仅可用只读工具调研、产出分步计划、等 /do 批准」)、`PLAN_REMINDER_CONCISE`(精简版,一两句)。
3. `public static String plan(boolean full)`:`systemReminder(full ? PLAN_REMINDER_FULL : PLAN_REMINDER_CONCISE)`。
4. `public static final String EXECUTE_DIRECTIVE`(从 `Prompt.java` 迁来):`/do` 注入的用户消息文案。

**验证:** 单测 `ReminderTest` 断言 `Reminder.plan(true)` 含 `<system-reminder>` 与完整文案;`Reminder.plan(false)` 用精简文案。

## T4: prompt 单测**文件:** `src/test/java/dev/guolaicode/prompt/PromptTest.java`
**依赖:** T1, T2, T3
**步骤:**
1. 装配顺序:断言 `Prompt.buildSystemPrompt()` 中身份段出现在工具使用段之前;模块以空行(`\n\n`)分隔。
2. 跳空槽:在 `Prompt.assembleSystem` 传入含空 `content` 的模块,断言其不出现、不产生连续多空行。
3. **N1 确定性**:连续两次 `Prompt.buildSystemPrompt()` 结果 `equals`。
4. **F5 双重强化**:`Prompt.buildSystemPrompt()` 文本含「编辑」与「先读」之意、含「优先」与专用工具名。
5. 环境与 reminder:见 T2/T3 验证。

**验证:** `mvn test -Dtest='PromptTest,EnvironmentTest,ReminderTest'` 通过。

## T5: 工具描述双重强化**文件:** `src/main/java/dev/guolaicode/tool/EditFileTool.java`、`src/main/java/dev/guolaicode/tool/BashTool.java`
**依赖:** 无
**步骤:**
1. `EditFileTool.description()` 末补:「编辑前请先用 read_file 读取目标文件,确认 old_string 唯一。」
2. `BashTool.description()` 末补:「读文件、找文件、搜内容请优先用 read_file/glob/grep,不要用 bash 拼凑。」
3. 不改 schema、不改 `execute()` 行为。

**验证:** `mvn -q compile`;`mvn test -Dtest='*ToolTest'` 仍通过。

## T6: llm 接口改造**文件:** `src/main/java/dev/guolaicode/llm/System.java`、`src/main/java/dev/guolaicode/llm/Request.java`、`src/main/java/dev/guolaicode/llm/Usage.java`、`src/main/java/dev/guolaicode/llm/Provider.java`
**依赖:** 无(但 T7/T8/T9 依赖本任务)
**步骤:**
1. 新增 `public record System(String stable, String environment)`、`public record Request(List<Message> messages, List<ToolDefinition> tools, System system, String reminder)`。
2. `Usage` 加 `long cacheWrite`、`long cacheRead`(若 `Usage` 已是 record,定义新 record 含全部字段;若为类,加字段与 accessor)。
3. `Provider.stream` 改为 `Flow.Publisher<StreamEvent> stream(Request req)`;更新接口 javadoc。
4. 删除 `effectiveSystem` 方法与对 `dev.guolaicode.prompt` 包的 import。
5. 工厂、构造器签名保持。

**验证:** `mvn -q compile` 在 `AnthropicProvider`/`OpenAIProvider` 报未适配的编译错(预期),T7/T8 修复。

## T7: Anthropic 适配缓存通道 + reminder**文件:** `src/main/java/dev/guolaicode/llm/AnthropicProvider.java`
**依赖:** T6
**步骤:**
1. `stream(Request req)`:
   - 构造 `List<TextBlockParam>`:`req.system().stable()` 非空 → `TextBlockParam.builder().text(stable).cacheControl(CacheControlEphemeral.builder().build()).build()`(断点);`req.system().environment()` 非空 → `TextBlockParam.builder().text(env).build()`(无 `cacheControl`)。
   - `params.system(systemBlocks)`。
   - `messages = toAnthropicMessages(req.messages())`;`req.reminder() != null && !req.reminder().isEmpty()` → 调 `appendReminder(msgs, req.reminder())`:把 `ContentBlockParam.ofText(TextBlockParam.builder().text(reminder).build())` 追加到**最后一条消息**的 content 列表;末条非 user 时新起一条 user 消息。
   - `params.tools(toAnthropicTools(req.tools()))`(不另打断点)。
   - thinking 逻辑沿用(`assistantUsedTools(req.messages())`)。
2. Usage 解析:订阅 `onComplete` 时从 `accumulator.usage()` 取 `inputTokens`/`outputTokens`/`cacheCreationInputTokens`/`cacheReadInputTokens`,推 `pub.submit(new StreamEvent.UsageEvent(new Usage(...)))`。
   - 注意缺字段 `Optional.orElse(0L)` 处理(N6)。

**验证:** `mvn -q compile` 通过(配合 T8);烟囱 anthropic 跑两轮见 `cacheRead > 0`(次轮)。

## T8: OpenAI 适配缓存通道 + reminder**文件:** `src/main/java/dev/guolaicode/llm/OpenAIProvider.java`
**依赖:** T6
**步骤:**
1. `toOpenAIMessages(req)`:首条 system 消息 = `req.system().stable()`(若 `environment` 非空拼为 `stable + "\n\n" + environment`);随后映射历史;`req.reminder()` 非空 → 追加一条尾部 `ChatCompletionUserMessageParam.builder().content(req.reminder()).build()`。
2. `stream(Request req)` 改用 `req`;`params.tools(toOpenAITools(req.tools()))`。
3. Usage 解析:`cacheRead = accumulator.usage().promptTokensDetails().cachedTokens().orElse(0L)`;`cacheWrite = 0L`。

**验证:** `mvn -q compile` 通过;烟囱 openai 兼容端点跑两轮,`cachedTokens` 字段被打印(端点支持则 >0)。

## T9: agent 改造**文件:** `src/main/java/dev/guolaicode/agent/Agent.java`
**依赖:** T1, T2, T3, T6
**步骤:**
1. `Agent` 加 `private final String version` 字段;构造器 `Agent(Provider p, ToolRegistry r, String version)`。
2. 加常量 `private static final int PLAN_REMINDER_INTERVAL = 4;`。
3. `run` 起始:`var env = Environment.gather(this.version, provider.model());`;`var sys = Prompt.buildSystemPrompt();`;`defs` 按 mode 选择(规划=`registry.readOnlyDefinitions()`,普通=`registry.definitions()`)——**移除 suffix 变量**。
4. 每轮迭代算 reminder:`String reminder = "";`;`if (mode == Mode.PLAN) { boolean full = (iter == 1) || ((iter - 1) % PLAN_REMINDER_INTERVAL == 0); reminder = Reminder.plan(full); }`。
5. `streamOnce` 签名改为接收 `sys`、`envText`、`defs`、`reminder`,内部组装 `var req = new llm.Request(conv.messages(), defs, new llm.System(sys, envText), reminder);` 调 `provider.stream(req)`。
6. `agent.Usage` 加 `cacheWrite/cacheRead`;`run` 透传 `new Event.Usage(input, output, cacheWrite, cacheRead)`。

**验证:** `mvn -q compile` 通过(配合 T10/T11)。

## T10: TUI 与 smoke 接线**文件:** `src/main/java/dev/guolaicode/tui/StreamPump.java`、`src/main/java/dev/guolaicode/smoke/SmokeMain.java`
**依赖:** T9
**步骤:**
1. `StreamPump`(或 `TuiApp` 内构造 Agent 的位置):改 `new Agent(provider, registry, tuiApp.version())`;`/do` 注入仍用 `Reminder.EXECUTE_DIRECTIVE`(已迁至 `Reminder.java`,import 路径更新)。
2. `SmokeMain`:`new Agent(p, ToolRegistry.defaultRegistry(), "dev")`;消费 `Event.usage()` 时打印 `input/output/cache_write/cache_read`;可改为连发两条消息观察次轮 `cacheRead`。

**验证:** `mvn -q -DskipTests package` 全绿;`java -jar target/guolaicode-*.jar` 可启动。

## T11: agent 单测适配**文件:** `src/test/java/dev/guolaicode/agent/AgentTest.java`
**依赖:** T9
**步骤:**
1. 修 fake provider:`stream` 实现新签名 `Flow.Publisher<StreamEvent> stream(Request req)`;记录收到的 `req`(`system.stable`/`system.environment`、`tools`、`reminder`)。
2. 既有 ch04 场景(A 自然完成、B 上限、C 未知工具、D 并发、E 取消、F 规划只读工具)适配新签名;`new Agent(...)` 传 version。
3. 新增断言:
   - 规划模式下 `req.system().stable()` 非空且**普通/规划一致**;`req.system().environment()` 非空。
   - 规划模式 iter1 的 `req.reminder()` 含完整提醒、含 `<system-reminder>`;iter2 为精简版(构造一个让循环多轮的脚本)。
   - 规划模式 `req.tools()` 仅只读;普通模式全量。
   - reminder **不写入 conv 持久历史**(`conv.messages()` 不含 reminder 文本)。
   - 缓存用量透传:fake 发 `Usage(..., cacheWrite=X, cacheRead=Y)` → 收到的 `Event.Usage` 携带 X/Y。

**验证:** `mvn test -Dtest=AgentTest` 通过;`mvn -Dsurefire.argLine='-Xshare:off' test -Dtest=AgentTest` 多轮稳定。

## T12: 全量编译测试与规范**文件:** —
**依赖:** T1–T11
**步骤:**
1. `mvn spotless:check`(无差异)。
2. `mvn -q -DskipTests package`、`mvn test`。
3. 多轮跑 `mvn test -Dtest='*Test'` 观察并发稳定。

**验证:** 全部通过;检索输出无 api_key 明文。

## 执行顺序

```
T1 ─┐
T2 ─┼─→ T4(prompt 单测)
T3 ─┘
T5(工具描述,独立)

T6(接口) ─┬─→ T7(anthropic) ─┐
          └─→ T8(openai)    ─┤
T1,T2,T3,T6 ─→ T9(agent) ────┼─→ T10(tui/smoke)
                              └─→ T11(agent 单测)

全部 ─→ T12(编译/测试/spotless)
```
````

```markdown
# 系统提示工程化 Checklist

> 每一项通过运行代码或观察行为来验证,聚焦系统行为;括号内为验证方式与对应需求。

## 实现完整性
- [ ] 模块化装配:系统提示由按优先级排列的固定模块拼成、模块间空行分隔(验证:`PromptTest` 断言身份段在工具使用段之前、以空行分隔)。(AC1/F1)
- [ ] 挂载即扩展:新增一个模块只需出现在模块列表,装配自动按优先级插入,不改 `assembleSystem` 逻辑(验证:`PromptTest` 传入额外模块断言其落在预期位置)。(AC1/F1)
- [ ] 可选空槽:自定义指令/已激活 Skill/长期记忆 content 为空时装配跳过、不留多余空行(验证:`PromptTest` 断言空模块不出现、无连续空行)。(AC2/F1)
- [ ] 缓存确定性:连续两次构造稳定系统提示逐字节相等;改变环境信息不改变稳定块(验证:`PromptTest` 断言两次 `Prompt.buildSystemPrompt()` 相等、稳定块不含 date/git/cwd)。(AC5/F3/N1)
- [ ] 环境信息呈现:系统提示第二段含工作目录、平台、当前日期、git 状态、版本、模型;与稳定块分属不同内容块(验证:`EnvironmentTest` 断言 `render()` 含各项;`AnthropicSystemTest` 请求 `system` 为两个 `TextBlockParam`)。(AC3/F2)
- [ ] 双重强化:关键约定(优先用专用工具、编辑前必先读)在工具描述与系统提示模块文本中均出现(验证:`PromptTest` 断言系统提示含相关语句;检索 `EditFileTool`/`BashTool` 描述含强化语句)。(AC7/F5)
- [ ] 补充消息注入机制:`Reminder.plan(...)` 输出以 `<system-reminder>` 标签包裹(验证:`ReminderTest` 断言标签存在)。(AC8/F6)
- [ ] 缓存字段解析:provider 用量对外暴露缓存写/读;Anthropic 取 `cacheCreationInputTokens/cacheReadInputTokens`,OpenAI 取 `promptTokensDetails.cachedTokens`,缺字段为 0 不报错(验证:`AgentTest` fake 发缓存用量断言 `Event.Usage` 透传;烟囱打印真实字段)。(AC6/F4/N6)
- [ ] Anthropic 缓存断点真实发出:稳定块 `TextBlockParam.cacheControl` 非空、环境块为 `null`(验证:`AnthropicSystemTest` 反射或调用 SDK 序列化断言——守护「cacheControl 字段挂错位置」回归)。(AC4/F3)

## 集成
- [ ] 规划模式按轮次注入:`/plan` 后 iter1 注入完整提醒、间隔轮(每 4)重复完整、其余轮精简;reminder 不写入持久历史(验证:`AgentTest` 多轮脚本断言各轮 `req.reminder()` 详略 + `conv.messages()` 不含 reminder 文本)。(AC9/F6/F7)
- [ ] 规划模式工具集:规划模式 `req.tools()` 仅只读、普通模式全量(验证:`AgentTest` 断言两模式 tools 差异)。(AC9/F7)
- [ ] 稳定系统提示跨模式一致:普通与规划模式 `req.system().stable()` 相同(规划提醒已移出系统通道)(验证:`AgentTest` 断言两模式 stable 相等)。(F7/N1)
- [ ] 历史合法:注入 reminder 后请求消息序列角色合法(Anthropic 并入末条 user、不产生连续 user)(验证:`AgentTest` 断言织入后末条仍为单一 user 回合;端到端 plan 多轮不报 400)。(AC12/N3)
- [ ] 跨协议一致:anthropic 与 openai(含兼容 base_url)都装配同一系统提示 + 环境段、注入同一 reminder(验证:两配置各跑多轮;anthropic 看缓存命中、openai 看 `cachedTokens`)。(AC10/F8)
- [ ] 不破坏 ch04:多轮连环、用户取消、流出错恢复、历史一致仍成立(验证:跑 ch04 端到端关键场景;`mvn test` 通过)。(AC11/N2)
- [ ] 环境采集降级:非 git 目录/git 不可用时环境段对应项省略、不卡界面、请求正常发起(验证:在非 git 临时目录跑 smoke/TUI 观察)。(AC13/N4)
- [ ] 界面不阻塞:环境采集(含 git 外调)不冻结界面、不显著拖慢首字(验证:正常目录跑,观察发起延迟无异常)。(N4)

## 编译与测试
- [ ] `mvn -q -DskipTests package` 无错误。
- [ ] `mvn test` 通过(`ConfigLoaderTest`、`ConversationTest`、`PromptTest`、`EnvironmentTest`、`ReminderTest`、`AnthropicSystemTest`、`AgentTest`)。
- [ ] `mvn spotless:check` 无差异(google-java-format 合规)。(AC14/N7)
- [ ] 多次 `mvn test -Dtest='AgentTest'` 稳定通过(无偶发竞争失败)。(N2/N6)
- [ ] 密钥不回显:对话区、环境段与任何输出均不出现 `api_key`(验证:通读输出、检索无明文 key;确认环境段不含环境变量)。(AC14/N5)

## 端到端场景(tmux 实跑)
- [ ] 场景 1(缓存命中,Anthropic):同一会话连发两条消息 → smoke/调试打印首轮 `cache_write > 0`、次轮 `cache_read > 0`,证明稳定前缀被缓存复用。(AC4/F3)
- [ ] 场景 2(规划模式按轮次):`/plan` 发一个需多步只读调研的任务 → 模型仅用只读工具产出计划、首轮注入完整提醒、后续轮精简;`/do` 切回全工具并按计划执行(产生写/执行类调用)。(AC9/F7)
- [ ] 场景 3(reminder 不被当用户输入):规划模式下模型不复述/回应 `<system-reminder>` 内容,而是据其约束行事。(AC8/F6)
- [ ] 场景 4(环境感知):问「我现在在哪个目录、什么平台、今天几号」→ 模型据环境段正确回答。(AC3/F2)
- [ ] 场景 5(取消后可继续):规划模式多轮中途按 Esc 取消 → 回空闲态、再发一条继续对话不报 400。(AC12/N3)
- [ ] 场景 6(非 git 目录降级):在非 git 目录启动 → 环境段省略 git 状态、正常对话。(AC13/N4)
```

### TypeScript

```markdown
# 系统提示工程化 Spec## 背景ch04 给 GuoLaiCode 装上了 Agent Loop——能自主多轮想 → 调工具 → 看结果地干活。但「**干活的指令**」还很原始:系统提示是一段揉在一起的固定常量(身份、工具用法、简洁性挤在同一块文本里),没有结构、没有优先级、想加一类新指令只能往字符串里塞;每轮请求都把整段系统提示与全部工具定义**原样重发**,稳定不变的内容反复计费、反复占用首字延迟;模型对**当前环境**(工作目录、平台、日期、git 状态)一无所知;规划模式提醒每轮无差别地拼在系统提示尾部,注入频率不可控。

ch05 给 GuoLaiCode 做**系统提示工程化**:把全局指令拆成按职责划分、按优先级拼装的模块;把稳定内容与变化内容分到**可缓存通道**与**消息通道**,让稳定的指令和工具定义走缓存省钱省首字延迟;引入一种带特殊标签的**补充消息注入**机制,在运行中注入指令而不污染缓存、不被模型当成用户提问来回复;会话级开关(规划模式)的提醒按**轮次频率**注入。这是从「能干活」到「干得好」的一步。

## 目标

- 把全局指令拆成按职责划分的若干模块,按优先级拼装成完整系统提示;新增一类指令只需挂载新模块,不改装配逻辑。
- 区分稳定与变化内容:稳定的系统模块与工具定义跨轮逐字节一致,走**可缓存通道**;环境信息与对话历史走**不缓存的通道**。
- 在工具描述与系统提示中,对关键约定(优先用专用工具、编辑前必先读)**双重强化**,提高遵守率。
- 提供一种带特殊标签的**补充消息注入**机制:运行中注入补充指令,既不污染缓存,也不会被模型当成用户输入来回复。
- 会话级开关(规划模式)的提醒按轮次注入:**首轮完整、间隔轮次重复完整、其余轮次精简**,控制注入频率。
- 能从大模型返回中**解析缓存命中信息**(写入 / 读取 token),用于验证缓存策略真实生效。

## 功能需求

- F1: 模块化系统提示拼装
  把全局指令按职责拆成模块,每个模块带名称、优先级、内容。装配时按优先级从高到低拼接成完整系统提示,模块间以空行分隔。固定模块按优先级排:**身份 → 系统约束 → 任务模式 → 动作执行 → 工具使用 → 语气风格 → 文本输出 → 环境信息**。其后预留三个可选模块位置:**已激活 Skill、自定义指令、长期记忆**——本章这三个**不接入真实内容来源**(留待后续章节),装配时内容为空则跳过、不留多余空行。新增一类指令只需定义新模块挂到对应优先级,不改装配主逻辑。

- F2: 环境信息构造与呈现
  收集运行环境信息(工作目录、操作系统/平台、架构、Shell、是否 Git 仓库、Git 分支、当前模型、当前日期等),构造成一段供模型感知环境的文本。环境信息作为系统提示的独立段落呈现,按行罗列各项;非 Git 仓库或某项取不到时,对应行自动省略而非留空。

- F3: 稳定内容走可缓存通道
  稳定不变的内容(拼装好的系统模块 + 工具定义 + 历史前缀)归入**可缓存通道**,变化内容(每轮新增的补充消息、本轮新工具结果)归入**不缓存通道**。可缓存内容必须**跨轮逐字节一致**(不含任何随轮次变化的成分),否则缓存无法命中。对支持显式缓存标记的协议,主动在稳定前缀、工具定义末尾、历史末条用户消息尾部打缓存断点,形成多层可复用前缀;对仅靠端点前缀缓存的协议,把稳定内容置于请求前缀位置,自动受益,不强制端点支持。

- F4: 缓存命中解析
  从大模型响应用量中解析缓存字段——**缓存写入**(首次创建缓存的 token)与**缓存读取**(命中复用的 token),随用量一并对外暴露,供烟囱/调试程序打印验证缓存是否真生效。不同协议字段名不同,各自解析;字段缺失时按零处理,且为避免与原始输入 token 双计,在仅暴露命中 token 的协议路径下从输入 token 中扣除已命中部分。

- F5: 关键约定双重强化
  对模型最易违反的关键约定(如「有专用工具时绝不用通用命令拼凑」「编辑文件前必先读取」),在**工具描述**与**系统提示模块**两处都明确表述。通用命令工具的描述里列出常见误用与对应的专用工具替代关系;文件编辑工具的描述里强制「编辑前必先读」并在违反时直接失败。

- F6: 补充消息注入机制(system-reminder)
  提供一种带特殊标签(形如 `<system-reminder>`)的补充消息注入形式:构造请求时把补充指令注入**消息通道**,以用户角色追加到历史尾部,但用标签语义让模型理解这是系统补充上下文而非用户提问——不针对它直接回复。此类消息**每轮动态构造、不写入持久历史**,因而不污染缓存、不破坏历史角色交替。机制对多类提醒通用:规划模式提醒、已激活 Skill 提醒、外部钩子/通知,以及长期记忆注入,都通过同一通道下发。

- F7: 规划模式按轮次注入
  规划模式提醒改由 F6 机制承载(不再拼到系统提示尾部)。按 Agent Loop 轮次控制注入**频率与详略**:**首轮注入完整提醒(完整工作流 + 计划文件信息),之后每隔固定轮次重复一次完整提醒,其余轮次注入精简版**(仅提示规划模式仍生效、只读工具范围、收尾方式)。模式跨轮保持,`/plan` 进入、`/do` 退出并触发按计划执行;进入与退出本身也通过补充消息通道告知模型;规划模式下仍仅放开只读工具(均沿用 ch04)。

- F8: 跨协议一致
  各支持协议(Anthropic、OpenAI 及其兼容端点)都装配同一套模块化系统提示 + 环境信息、都注入相同补充消息、都尽力走缓存。两协议在模块内容、注入时机、规划模式注入节奏上**行为一致**;缓存命中解析在各自字段可得时生效,字段缺失时按零处理。

## 非功能需求- N1: 缓存确定性——可缓存块(工具定义 + 稳定系统模块 + 历史前缀)在跨轮、会话内多轮之间**逐字节稳定**:模块装配顺序固定、工具顺序固定、缓存断点仅作用于元数据标记不改变文本内容,不混入任何随轮次/时间变化的成分。否则缓存永不命中,本章核心目标落空。
- N2: 不破坏 ch04 行为——Agent Loop 多轮编排、流式双路收集、保序分批并发、用户取消、历史一致、上下文溢出与限流自恢复、自动压缩、用量/迭代进度展示等 ch04 能力**不退化**。系统提示重构与消息注入是叠加,不改这些既有路径的语义。
- N3: 历史合法性——每轮动态注入的补充消息(不持久)拼进请求后,消息序列仍**角色交替合法、工具调用与结果配对不破坏**,不致请求返回 400;连续同角色消息按协议要求自动合并;补充消息不写入持久历史,故不影响后续轮次与可恢复性。
- N4: 环境采集不阻塞、可降级——环境信息采集(含可能外调 shell 的 git 状态)**快速且有界**,不阻塞界面、不显著拖慢请求发起;任一项取不到时降级(留空/省略),不中断会话。
- N5: 密钥与敏感信息不泄漏——沿用 ch03/ch04,对话区与任何输出均不出现 api_key;环境信息构造**不带入密钥或敏感环境变量**,仅读必要的 Shell 标识等无敏感字段。
- N6: 缓存字段解析健壮——大模型或兼容端点未返回缓存字段时按零处理,不抛错、不因缺字段中断本轮或会话。
- N7: 代码规范——遵循项目 CLAUDE.md 中关于格式化、类型检查、模块组织的统一约定。
- N8: 可扩展性——新增一类系统提示模块或一类补充消息提醒时,改动局部:不触及装配主逻辑,不改协议适配层对外签名的语义稳定性。

## 不做的事- **项目指令文件加载**——「自定义指令」为预留空槽,本章不从项目级指令文件读取内容(留待后续章节)。
- **自动记忆 / 长期记忆作为系统提示模块**——「长期记忆」槽位预留,本章长期记忆即便接入也只走补充消息通道注入,不进系统提示模块,且不做记忆写入与召回。
- **真实 MCP 接入**——「已激活 Skill」为预留空槽,不接入真实 MCP 工具/资源。
- **自动化评估 / 评测框架**——本章只做缓存字段解析验证 + 一组典型场景人工对比的定性评估,不建评测流水线。
- **历史增量缓存**——只缓存稳定前缀(工具定义 + 稳定系统模块 + 历史尾部一处断点),不对中间历史另设断点。
- **缓存 TTL / 断点数配置化**——TTL 用默认值、断点位置内置,本章不通过配置调整。
- **状态栏展示缓存命中**——缓存信息只走 smoke / 调试输出,不进 TUI 状态栏。
- **提示词运行时热加载 / 外部模板文件**——模块内容内置于代码,不做外部模板。
- **多模态、流式工具结果**——沿用 ch04,不做。

## 验收标准

- AC1: 模块化装配——系统提示由按优先级排列的固定模块拼成,模块间以空行分隔;新增一个模块只需挂载、不改装配逻辑。(F1)
- AC2: 可选空槽——「已激活 Skill / 自定义指令 / 长期记忆」内容为空时装配自动跳过、不留多余空行;架构支持后续填充。(F1)
- AC3: 环境信息呈现——系统提示尾部含独立环境段,行项含工作目录、平台、Shell、是否 Git 仓库、日期;Git 仓库下额外含分支行,模型非空时含模型行。(F2)
- AC4: 缓存命中——同一会话连发两条消息:首轮缓存写入 token > 0、次轮缓存读取 token > 0,证明稳定前缀被复用;仅环境信息/历史变化时稳定前缀仍命中。(F3/N1)
- AC5: 缓存确定性——两次构造的稳定块逐字节相等;故意改变环境信息不改变稳定块内容。(F3/N1)
- AC6: 缓存字段解析——用量对外暴露缓存写/读两字段;各协议在自家字段可得时填充,缺失时为 0 且不报错。(F4/N6)
- AC7: 双重强化——关键约定(优先用专用工具、编辑前必先读)在**工具描述**与**系统提示模块**文本中均出现,且文件编辑工具在未读取的情况下直接失败。(F5)
- AC8: 补充消息注入——注入消息以 `<system-reminder>` 标签包裹,模型不把它当用户提问来复述/回应;该消息**不写入持久对话历史**。(F6/N3)
- AC9: 规划模式按轮次注入——`/plan` 后:首轮注入完整提醒、之后每隔固定轮次重复完整、其余轮次注入精简版;规划模式仅放开只读工具;`/do` 切回全工具并按上文计划执行;进入与退出本身均通过补充消息通道告知。(F7)
- AC10: 跨协议一致——各支持协议都装配同一系统提示 + 环境信息、注入同一补充消息、都尽力缓存,行为一致;缓存字段在各自可得时填充。(F8)
- AC11: 不破坏 ch04——ch04 关键行为(多轮连环、用户取消、历史一致、流出错恢复、自动压缩)仍成立;现有测试通过。(N2)
- AC12: 历史合法——注入补充消息后请求不返回 400;规划模式多轮 + 中途取消后再发一条仍可继续(历史配对合法)。(N3)
- AC13: 环境采集降级——在非 git 目录或 git 不可用时,环境段对应项降级(留空/省略),界面不卡、请求正常发起。(N4)
- AC14: 代码规范——遵循项目格式化与类型检查约定;对话区与输出无 api_key 明文;环境采集不读取除必要 Shell 标识之外的敏感环境变量。(N5/N7)
```

````markdown
# 系统提示工程化 Plan

> 技术栈:Bun + TypeScript 5.x。LLM SDK:`@anthropic-ai/sdk` ^0.99.0、`openai` ^6.39.0。TUI:Ink 5.x + React 18。

## 技术栈

- 运行时:bun(`bun run src/main.tsx`、`bun test`)
- 语言:TypeScript 5.8.x,`tsc --noEmit` 类型检查
- 模块系统:ESM(`"type": "module"`),import 时显式带 `.js` 后缀
- LLM SDK:`@anthropic-ai/sdk`(`AnthropicClient` 用 `client.messages.stream`)、`openai`(Responses API + Chat Completions 两路)
- TUI:`ink`、`ink-spinner`、`ink-text-input`、`react`
- 终端样式:`chalk`、`marked` + `marked-terminal`
- 工具:`@modelcontextprotocol/sdk`、`fuse.js`、`js-yaml`

## 架构概览ch05 在三层叠加,**不改 ch04 的 Agent Loop 控制流**:

- **prompt 层(`src/prompt/`)**:`sections.ts` 定义八个固定 Section 工厂 + Section 类型;`builder.ts` 提供 `PromptBuilder` 类、`detectEnvironment(workDir)`、`buildSystemPrompt(env, opts)`;`plan-mode.ts` 提供 `buildPlanModeReminder / buildPlanModeExitReminder / buildPlanModeReentryReminder`。prompt 层只依赖 `node:child_process / node:os`,不依赖 llm 层。
- **llm 层(`src/llm/`)**:`LLMClient` 接口签名 `stream(conv, tools, abortSignal)` 不变;`createClient(cfg, systemPrompt)` 在构造时接收已装配好的系统提示;`AnthropicClient` 把 systemPrompt 放进 `system: [{ type:"text", text, cache_control:{type:"ephemeral"} }]`、对最后一个 tool 与最后一条 user 末 block 也打 ephemeral 断点;`OpenAIClient / OpenAICompatClient` 把 systemPrompt 置于 input/messages 前缀。`UsageInfo` 扩展 `cacheReadInputTokens / cacheCreationInputTokens`,三 client 都填。
- **agent 层(`src/agent/agent.ts`)**:`run()` 每轮迭代开始时检查 `this.checker.mode === "plan"`,若是则 `getOrCreatePlanPath / planExists`、`addSystemReminder(buildPlanModeReminder(planPath, exists, iteration))`;同样位置注入 active-skills、hook、外部 notification 三类 reminder。reminder 通过 `ConversationManager.addSystemReminder` 写入 history,作为 role=user 消息 + `<system-reminder>...</system-reminder>` 标签。

数据流:`TUI 初始化` → `detectEnvironment(workDir)` → `env.model = provider.model` → `buildSystemPrompt(env)` → `createClient(provider, systemPrompt)` → `AnthropicClient.systemPrompt / OpenAIClient.systemPrompt` 固化 → `agent.run()` 每轮:`conversation.addSystemReminder(planReminder/...)` → `client.stream(apiConv, toolSchemas, signal)` → Anthropic/OpenAI 把 systemPrompt 放系统位、把 history 放消息位、解析 `usage.cache_*` → `UsageInfo{ inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens }` 透传给 agent 事件。

## 数据流

```
TUI/teammate/spawnSubAgent
    │
    ├─ env = detectEnvironment(workDir);  env.model = provider.model
    ├─ systemPrompt = buildSystemPrompt(env, opts?)  ← Section 列表按 priority 拼接
    └─ client = await createClient(provider, systemPrompt)
            │
            └─→ AnthropicClient { systemPrompt, model, thinking, ... }
                OpenAIClient    { systemPrompt, model, ... }
                OpenAICompatClient { systemPrompt, model, ... }

agent.run() 每轮 iteration++ 后:
    ├─ if (checker.mode === "plan") {
    │      planPath = getOrCreatePlanPath(workDir)
    │      checker.planFilePath = planPath
    │      conv.addSystemReminder(buildPlanModeReminder(planPath, planExists, iteration))
    │   }
    ├─ skillReminder = buildActiveSkillsReminder(activeSkills)
    │   if (skillReminder) conv.addSystemReminder(skillReminder)
    ├─ for note of hookEngine.drainNotifications(): conv.addSystemReminder(note)
    └─ stream = client.stream(apiConv, toolSchemas, signal)
            │
            └─→ for await (event of stream):
                    case "stream_end": event.usage:
                      { inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens }
```

## 核心数据结构与接口### Section 与 EnvironmentContext

```ts
// src/prompt/sections.ts
export interface Section {
  name: string;
  priority: number;
  content: string;
}

export interface EnvironmentContext {
  workDir: string;
  os: string;        // platform()
  arch: string;      // arch()
  shell: string;     // process.env.SHELL ?? "bash"
  isGitRepo: boolean;
  gitBranch: string;
  model: string;
  date: string;      // ISO yyyy-mm-dd
}
```

### PromptBuilder 与 buildSystemPrompt

```ts
// src/prompt/builder.ts
export class PromptBuilder {
  private sections: Section[] = [];
  add(s: Section): this { this.sections.push(s); return this; }
  build(): string {
    const sorted = [...this.sections].sort((a, b) => a.priority - b.priority);
    return sorted.map((s) => s.content.trim()).filter(Boolean).join("\n\n");
  }
}

export interface BuildOptions {
  skillSection?: string;
  customInstructions?: string;
  memorySection?: string;
}

export function detectEnvironment(workDir: string): EnvironmentContext;
export function buildSystemPrompt(env: EnvironmentContext, opts?: BuildOptions): string;
```

### 规划模式提醒构造器

```ts
// src/prompt/plan-mode.ts
export function buildPlanModeReminder(
  planPath: string,
  planExist: boolean,
  iteration: number
): string;

export function buildPlanModeExitReminder(planPath: string, planExists: boolean): string;
export function buildPlanModeReentryReminder(planPath: string, planFileExists: boolean): string;
```

### 缓存用量字段

```ts
// src/llm/events.ts
export interface UsageInfo {
  inputTokens: number;
  outputTokens: number;
  cacheReadInputTokens: number;      // 新增
  cacheCreationInputTokens: number;  // 新增
}
```

### LLM Client 接口(签名不变)

```ts
// src/llm/client.ts
export interface LLMClient {
  stream(
    conv: ConversationManager,
    tools: Record<string, unknown>[],
    abortSignal?: AbortSignal
  ): AsyncGenerator<StreamEvent>;
}

export async function createClient(
  cfg: ProviderConfig,
  systemPrompt: string
): Promise<LLMClient>;
```

### 对话历史中的 system-reminder

```ts
// src/conversation/conversation.ts
addSystemReminder(content: string): void {
  this.history.push({
    role: "user",
    content: `<system-reminder>\n${content}\n</system-reminder>`,
  });
}
```

## 模块设计### `src/prompt/sections.ts`(新增/重写)
**职责:** 提供八个固定 Section 工厂函数与 `EnvironmentContext` 类型 + `environmentSection(env)`。
**对外接口:** `identitySection / systemSection / doingTasksSection / executingActionsSection / usingToolsSection / toneStyleSection / outputEfficiencySection / environmentSection`。
**关键点:**
- Section 内容为中文长字符串字面量,通过函数返回 `{ name, priority, content }` 三元组(priority 依次 0/10/20/30/40/50/60/70)。
- `usingToolsSection`(priority=40)包含 F5 双重强化:「有专用工具时绝不要用 Bash」+ ReadFile/EditFile/WriteFile/Glob/Grep 对应表。
- `systemSection`(priority=10)明确告知模型:「工具结果和用户消息中可能包含 `<system-reminder>` 标签。其中是系统信息,与所在的工具结果或消息没有直接关系。」——这是 F6 模型行为的元提示。
- `environmentSection` 按行渲染 `# 环境` 段:固定行 workDir / platform/arch / shell / isGitRepo / date,条件行 gitBranch(仓库内且分支非空)、model(非空)。

**依赖:** 无外部库,纯字符串构造。

### `src/prompt/builder.ts`(新增/重写)
**职责:** PromptBuilder 类、环境探测、`buildSystemPrompt` 入口。
**对外接口:** `PromptBuilder`、`detectEnvironment(workDir)`、`buildSystemPrompt(env, opts?)`。
**关键点:**
- `PromptBuilder.build()` 排序 → trim → filter(Boolean) → join("\n\n"),三步保证空 content 被自动跳过、无连续空行。
- `detectEnvironment(workDir)` 用 `node:os` 的 `platform() / arch()`、`process.env.SHELL ?? "bash"`、`new Date().toISOString().split("T")[0]`。git 探测用 `execSync("git rev-parse --is-inside-work-tree", { cwd: workDir, stdio: ["pipe","pipe","pipe"], encoding: "utf-8" })`,catch 分支兜底。
- `buildSystemPrompt`:固定 add 8 个 Section,再根据 `opts.skillSection / customInstructions / memorySection` 是否存在,分别 add priority=90/95/100 的可选 Section。
- N1 不变性:函数纯依赖入参 `env / opts`,无 `Date.now` / 无随机源,同一 env+opts 跨调用结果逐字节相等。

**依赖:** `node:child_process / node:os`。

### `src/prompt/plan-mode.ts`(新增)
**职责:** 规划模式 reminder 文本构造,按轮次频率切换完整/精简。
**对外接口:** `buildPlanModeReminder / buildPlanModeExitReminder / buildPlanModeReentryReminder`。
**关键点:**
- 完整版模板 `planModeFullReminder` 含「Plan mode is active... You MUST NOT make any edits」+ Plan File Info 占位符 + 5 阶段工作流(Initial Understanding / Design / Review / Final Plan / Call ExitPlanMode)。
- 精简版模板 `planModeSparseReminder`:一句话总结只读 + 5 阶段 + 结束方式(AskUserQuestion / ExitPlanMode)。
- 频率:`reminderInterval = 5`;`iteration === 1` 总返回完整;否则 `Math.floor((iteration - 1) / reminderInterval) % reminderInterval === 0` 返回完整,其余返回精简。
- `planExist` 控制 plan 文件信息段措辞(已存在 vs 待创建)。

**依赖:** 无。

### `src/llm/events.ts`(改造)
**职责:** 定义 `StreamEvent` 与 `UsageInfo`。
**关键点:** `UsageInfo` 增加 `cacheReadInputTokens / cacheCreationInputTokens` 两字段;`stream_end` 事件携带完整 usage。

### `src/llm/anthropic.ts`(改造)
**职责:** Anthropic 协议 client,装配缓存断点,解析缓存用量。
**关键点:**
- 构造器签名 `constructor(cfg: ProviderConfig, systemPrompt: string)`,把 systemPrompt 存为字段。
- `stream()` 中构造 `params.system = [{ type:"text", text: this.systemPrompt, cache_control:{type:"ephemeral"} }]`。
- 工具列表末尾的 tool 加 `cache_control: { type:"ephemeral" }`(通过 `(tools[tools.length-1] as Record<string,unknown>).cache_control = ...` 注入)。
- `markLastUserTailForCache(messages)` 从后向前找到第一条 role=user 的消息,把其 content 数组末块的 `cache_control` 设为 ephemeral——三层 ephemeral 断点。
- `message_start` 事件回填 `inputTokens / outputTokens / cacheReadInputTokens / cacheCreationInputTokens`(`?? 0` 兜底)。
- `buildAnthropicMessages` 已存在的「连续 user 自动合并」逻辑保证 N3 角色合法。

### `src/llm/openai.ts`(改造)
**职责:** OpenAI Responses + Chat Completions 两 client,装配 system 消息,解析 cached_tokens。
**关键点:**
- `OpenAIClient.stream`(Responses):`input.push({ role:"system", content: this.systemPrompt })` 再追加历史消息;`response.completed` 事件读 `usage.input_tokens_details?.cached_tokens ?? 0`,`inputTokens = Math.max(0, (usage.input_tokens ?? 0) - cacheReadInputTokens)` 避免双计。
- `OpenAICompatClient.stream`(Chat Completions):`messages: [{ role:"system", content: this.systemPrompt }, ...buildChatCompletionMessages(history)]`;chunk.usage 阶段读 `usage.prompt_tokens_details?.cached_tokens ?? 0`,同样减除以避免双计。
- 两 client 的 `cacheCreationInputTokens` 恒为 0(OpenAI 无显式缓存写概念)。

### `src/agent/agent.ts`(改造)
**职责:** Agent 主循环;每轮在 stream 前注入 plan-mode / skills / hook reminder。
**关键点:**
- `run()` 顶部 `iteration++` 后,先做 `manageContext`(自动压缩),再注入 reminder:`if (this.checker.mode === "plan") { ... conv.addSystemReminder(buildPlanModeReminder(planPath, planExists(workDir), iteration)) }`。
- `buildActiveSkillsReminder(activeSkills)` 非空时 `conv.addSystemReminder(skillReminder)`。
- `hookEngine.drainNotifications() / notificationFn()` 产出的字符串逐个 `conv.addSystemReminder(note)`。
- reminder 写入 conversation 即进入持久历史,后续轮次随消息发到 provider;不需要在 client 端二次拼接。

### `src/tui/app.tsx`(改造)
**职责:** 启动时一次性装配 systemPrompt 并 create client;管理 plan / unplan 进入退出 reminder。
**关键点:**
- `initClient`:`env = detectEnvironment(workDir); env.model = provider.model; systemPrompt = buildSystemPrompt(env); client = await createClient(provider, systemPrompt)`。
- `/plan` 进入与 `/do` 退出时,分别在 conversation 上 `addSystemReminder(buildPlanModeReentryReminder(...))` 或 `addSystemReminder(buildPlanModeExitReminder(...))`。

### `src/agents/spawn.ts` / `src/teammate.ts`
**职责:** 子 agent / teammate 启动时也走 `buildSystemPrompt`。
**关键点:** 子 agent 可通过 `definition.systemPromptOverride` 跳过 builder,直接使用自定义 system prompt(适用于 explore/plan 等专用 agent)。

### `src/tools/descriptions.ts`(F5 双重强化)
**职责:** 集中放置所有工具的 description 字符串常量。
**关键点:**
- `BashDescription`:「IMPORTANT: Avoid using this tool to run cat, head, tail, sed, awk, or echo commands. Instead use the dedicated ReadFile, EditFile, or WriteFile tools which provide a better experience.」
- `EditFileDescription`:「You MUST read the file with ReadFile before editing. This tool will fail otherwise.」
- 各工具类(`BashTool / EditFileTool / ...`)在 `description = BashDescription` 处引用,保证工具 schema 中带上同一段强化文本。

## 模块交互

```
TUI/spawn/teammate ─初始化─→ detectEnvironment(workDir)
                          → buildSystemPrompt(env, opts?)
                          → createClient(cfg, systemPrompt)
                                       │
                                       └─ AnthropicClient(systemPrompt)
                                          OpenAIClient(systemPrompt)
                                          OpenAICompatClient(systemPrompt)

Agent.run() 每轮:
  ┌─ manageContext / 压缩
  ├─ if plan mode: conv.addSystemReminder(buildPlanModeReminder(...))
  ├─ if skills:    conv.addSystemReminder(buildActiveSkillsReminder(...))
  ├─ if hooks:     conv.addSystemReminder(note) × N
  └─ client.stream(apiConv, toolSchemas, signal)
         │
         └─→ StreamEvent { text_delta | thinking_* | tool_call_* | stream_end{usage{...,cacheRead,cacheCreation}} }
```

依赖方向(无环):`agent → { prompt, llm, conversation, tools, planfile }`;`llm → conversation(type only)`;`prompt → node:os / node:child_process`(纯标准库)。

## 文件组织

```text
guolaicode/
├── src/
│   ├── prompt/
│   │   ├── sections.ts          ─ 新/重写:8 个固定 Section 工厂 + Section 类型 + environmentSection
│   │   ├── builder.ts           ─ 新/重写:PromptBuilder、detectEnvironment、buildSystemPrompt
│   │   └── plan-mode.ts         ─ 新:buildPlanModeReminder / Exit / Reentry
│   ├── llm/
│   │   ├── events.ts            ─ 改:UsageInfo 加 cacheReadInputTokens / cacheCreationInputTokens
│   │   ├── client.ts            ─ 沿用:createClient(cfg, systemPrompt) 工厂
│   │   ├── anthropic.ts         ─ 改:system 带 ephemeral 断点、tools/messages 末尾加 cache_control、解析 cache_* usage
│   │   └── openai.ts            ─ 改:system 置前缀、解析 cached_tokens、inputTokens 减除避免双计
│   ├── conversation/
│   │   └── conversation.ts      ─ 沿用:addSystemReminder 包 <system-reminder>;injectLongTermMemory 头插
│   ├── agent/
│   │   └── agent.ts             ─ 改:每轮 stream 前注入 plan-mode / skills / hooks reminder
│   ├── tools/
│   │   ├── descriptions.ts      ─ 改/沿用:Bash/EditFile/WriteFile 等 description 含双重强化语句
│   │   ├── bash.ts              ─ 沿用:description = BashDescription
│   │   └── edit-file.ts         ─ 沿用:description = EditFileDescription
│   ├── tui/
│   │   └── app.tsx              ─ 改:initClient 调 detectEnvironment + buildSystemPrompt
│   ├── agents/spawn.ts          ─ 改:子 agent 启动走 detectEnvironment + buildSystemPrompt(env)
│   └── teammate.ts              ─ 改:teammate 启动同上
└── tests/
    └── conversation.test.ts     ─ 已存在:断言 system-reminder 标签
```

## 技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 系统提示组织 | Section 工厂 + PromptBuilder 类 + priority 排序 | F1 「挂载即扩展」;优先级数值化保证排序稳定(N1);函数返回值便于 unit test 单 Section |
| 环境信息归属 | 作为 priority=70 的固定 Section 嵌入 systemPrompt 字符串 | TS 实现把 systemPrompt 一次性传入 LLMClient 构造,运行时不再分块;同 client 实例的 systemPrompt 跨轮稳定即 N1 |
| Anthropic 缓存断点 | system 单 TextBlockParam 显式 `cache_control:{type:"ephemeral"}`、工具列表最后一个 tool 加 ephemeral、`markLastUserTailForCache` 给末条 user 末块加 ephemeral | 三层 ephemeral 断点形成「system → tools → history 前缀」嵌套命中;默认 TTL 5 分钟;SDK 类型上 cache_control 字段必须显式提供值,不能省略 |
| 工具是否单独打断点 | 是(末尾 tool 加 ephemeral) | TS 实现走 `(tools[last] as Record<string,unknown>).cache_control = {type:"ephemeral"}`,允许工具集变化时只前面部分命中(与 system 断点叠加复用) |
| OpenAI 环境信息 | 拼入 systemPrompt 字符串 → 单条 role=system 消息置于 input/messages 前缀 | Responses API 用 `input.push({role:"system",content})`;Chat Completions 用 `messages:[{role:"system",content},...]`;端点前缀缓存自动命中稳定部分 |
| 缓存用量字段 | UsageInfo 加 cacheReadInputTokens / cacheCreationInputTokens | Anthropic 取 `event.message.usage.cache_read_input_tokens / cache_creation_input_tokens`;OpenAI Responses 取 `usage.input_tokens_details?.cached_tokens`;OpenAI Chat 取 `usage.prompt_tokens_details?.cached_tokens` |
| OpenAI inputTokens 减除 | `Math.max(0, (usage.input_tokens ?? 0) - cacheReadInputTokens)` | `input_tokens` 已含缓存命中部分;减除后 anchor 公式 `input + cache_read + cache_creation + output` 不双计 |
| LLMClient 接口签名 | 维持 `stream(conv, tools, abortSignal)`,systemPrompt 通过构造器传入 | TS 实现把 systemPrompt 视为 client 不可变属性(初始化一次、整个 agent 生命周期复用),不需要每轮 stream 调用都传;reminder 走 conversation 通道,不占 stream 参数位 |
| reminder 注入位置 | conversation.addSystemReminder → role=user + `<system-reminder>` 标签 → 写入持久 history | Anthropic / OpenAI 三协议都把它当普通 user 消息序列化;buildAnthropicMessages 已有连续 user 合并逻辑保证 N3 |
| reminder 是否持久 | 持久写入 history | plan-mode 是会话级状态,每轮重新调 addSystemReminder 累加;退出规划模式也通过新增 system-reminder 消息显式通知,而非清除历史 |
| 规划提醒节奏 | iteration===1 总完整;否则 floor((iter-1)/5) % 5 === 0 完整 | F7 首轮完整、间隔重复、其余精简;reminderInterval=5 内置常量 |
| 缓存验证呈现 | usage 字段透传到 anchor / debug,TUI 状态栏不展示 | 不动 UI;UsageInfo 携带字段供 `manageContext` 的 token anchor 与可选 debug 日志使用 |
| prompt ↔ llm 依赖 | systemPrompt 字符串在 createClient 时注入 llm,llm 不 import prompt | 打破环依赖;prompt 只依赖 node 标准库,易于 unit test |
````

````markdown
# 系统提示工程化 Tasks

> 模块路径在 `src/prompt/`、`src/llm/`、`src/agent/`、`src/tools/`、`src/conversation/`、`src/tui/` 下;运行时 bun + TypeScript 5.x,`bun test` 执行单测、`tsc --noEmit` 类型检查。

## 文件清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `src/prompt/sections.ts` | `Section` 接口、`EnvironmentContext` 接口;八个固定 Section 工厂(identitySection ... environmentSection) |
| 新建 | `src/prompt/builder.ts` | `PromptBuilder` 类、`detectEnvironment(workDir)`、`BuildOptions` 接口、`buildSystemPrompt(env, opts?)` |
| 新建 | `src/prompt/plan-mode.ts` | `buildPlanModeReminder / buildPlanModeExitReminder / buildPlanModeReentryReminder` + `reminderInterval` 常量 + 完整/精简提醒模板 |
| 改   | `src/llm/events.ts` | `UsageInfo` 增加 `cacheReadInputTokens / cacheCreationInputTokens` 两字段 |
| 改   | `src/llm/anthropic.ts` | 构造器接收 `systemPrompt`;`stream()` 中 system 单 TextBlockParam + ephemeral 断点;末 tool / `markLastUserTailForCache` 加 ephemeral;解析 `cache_read_input_tokens / cache_creation_input_tokens` |
| 改   | `src/llm/openai.ts` | `OpenAIClient / OpenAICompatClient` 构造器接收 `systemPrompt`;Responses input / Chat messages 前缀 system;解析 `input_tokens_details / prompt_tokens_details.cached_tokens`;`inputTokens` 减除避免双计 |
| 改   | `src/llm/client.ts` | `createClient(cfg, systemPrompt)` 工厂分发三协议 |
| 沿用 | `src/conversation/conversation.ts` | `addSystemReminder(content)` 包 `<system-reminder>` 标签写入 history;`injectLongTermMemory` 头插一条 system-reminder user 消息 |
| 改   | `src/agent/agent.ts` | `run()` 每轮 `manageContext` 后注入 plan-mode / active-skills / hook reminder;`buildActiveSkillsReminder(activeSkills)` 内部辅助函数 |
| 改/沿用 | `src/tools/descriptions.ts` | `BashDescription` 含「Avoid using this tool to run cat, head, tail...」;`EditFileDescription` 含「You MUST read the file with ReadFile before editing」 |
| 改   | `src/tui/app.tsx` | `initClient` 调 `detectEnvironment + buildSystemPrompt`;`/plan` `/do` 切换时通过 `addSystemReminder(buildPlanModeReentryReminder / buildPlanModeExitReminder)` 注入 |
| 改   | `src/agents/spawn.ts` | 子 agent 启动时 `detectEnvironment(workDir); env.model = resolvedModel; systemPrompt = definition.systemPromptOverride ?? buildSystemPrompt(env)` |
| 改   | `src/teammate.ts` | teammate 启动时同上 |
| 新建 | `tests/prompt-builder.test.ts` | PromptBuilder 排序、跳空、N1 稳定性、F5 双重强化文本断言 |
| 新建 | `tests/plan-mode-reminder.test.ts` | iteration=1 完整版、iteration=2 精简版、iteration=6 完整版重复 |

---

## T1: Section 工厂与 Environment 渲染**文件:** `src/prompt/sections.ts`
**依赖:** 无
**步骤:**
1. 定义 `export interface Section { name: string; priority: number; content: string }`。
2. 实现八个工厂函数,各自返回 `Section`:
   - `identitySection()` priority=0:「你是 GuoLaiCode...优先编写安全、正确的代码...」。
   - `systemSection()` priority=10:`# 系统`,包含「工具结果和用户消息中可能包含 `<system-reminder>` 标签」(让模型理解 F6 的语义)。
   - `doingTasksSection()` priority=20:`# 任务执行`,详尽列出 ReAct 行为约定。
   - `executingActionsSection()` priority=30:`# 谨慎执行操作`,列出破坏性操作 / 难撤销操作 / 影响他人操作三类高风险。
   - `usingToolsSection()` priority=40:`# 使用你的工具`——**F5 双重强化**列表,「ReadFile 而非 cat/head/tail」「EditFile 而非 sed/awk」等。
   - `toneStyleSection()` priority=50:`# 语气与风格`,简洁、file_path:line_number、不要冒号。
   - `outputEfficiencySection()` priority=60:`# 文本输出`,假设用户看不到工具调用与思考、简短更新等。
3. 定义 `export interface EnvironmentContext { workDir; os; arch; shell; isGitRepo; gitBranch; model; date }`。
4. 实现 `environmentSection(env)` priority=70:输出 `# 环境` 段,固定行 workDir / `${os}/${arch}` / shell / isGitRepo / date;条件行 gitBranch(`env.isGitRepo && env.gitBranch`)、model(非空)。

**验证:** `bun test tests/prompt-builder.test.ts` 中断言各 Section name/priority 正确。

## T2: PromptBuilder 与 buildSystemPrompt**文件:** `src/prompt/builder.ts`
**依赖:** T1
**步骤:**
1. `import { execSync } from "node:child_process"; import { platform, arch } from "node:os"; import type { Section, EnvironmentContext } from "./sections.js"; import { identitySection, ..., environmentSection } from "./sections.js";`
2. 实现 `export class PromptBuilder`:私有 `sections: Section[]`;`add(s): this` 返回 this 支持链式;`build(): string` 排序 → trim → filter(Boolean) → join("\n\n")。
3. 实现 `export function detectEnvironment(workDir: string): EnvironmentContext`:
   - 初始化 `env = { workDir, os: platform(), arch: arch(), shell: process.env.SHELL ?? "bash", isGitRepo: false, gitBranch: "", model: "", date: new Date().toISOString().split("T")[0] }`。
   - try 块:`execSync("git rev-parse --is-inside-work-tree", { cwd: workDir, stdio: ["pipe","pipe","pipe"], encoding: "utf-8" }).trim() === "true"` 则 `env.isGitRepo = true; env.gitBranch = execSync("git rev-parse --abbrev-ref HEAD", ...).trim()`。
   - catch 块:留空,函数继续返回 env。
4. 定义 `export interface BuildOptions { skillSection?: string; customInstructions?: string; memorySection?: string }`。
5. 实现 `export function buildSystemPrompt(env, opts = {}): string`:
   - new PromptBuilder;依次 add Identity ... Environment 八个 Section。
   - `opts.skillSection` → `b.add({ name:"Skills", priority:90, content: opts.skillSection })`。
   - `opts.customInstructions` → `b.add({ name:"CustomInstructions", priority:95, content: opts.customInstructions })`。
   - `opts.memorySection` → `b.add({ name:"Memory", priority:100, content: opts.memorySection })`。
   - return b.build()。

**验证:** `tsc --noEmit` 无报错;`bun test tests/prompt-builder.test.ts` 通过。

## T3: 规划模式 reminder 构造器**文件:** `src/prompt/plan-mode.ts`
**依赖:** 无
**步骤:**
1. 定义模板字符串常量:`planModeFullReminder`(含 `%PLAN_FILE_INFO%` 占位符 + 5 阶段工作流)、`planModeSparseReminder`(含 `%PLAN_PATH%` 占位符,一句话精简)、`planModeExitTemplate`(含 `%EXTRA%` 占位符)、`planModeReentryTemplate`(含 `%PLAN_PATH%` 占位符)。
2. 定义 `const reminderInterval = 5`。
3. 实现 `export function buildPlanModeReminder(planPath, planExist, iteration): string`:
   - 构造 `planFileInfo`:`Plan file: ${planPath}` + (planExist 时「A plan file already exists...」/ 否则「No plan file exists yet...」)。
   - `iteration === 1` → return `planModeFullReminder.replace("%PLAN_FILE_INFO%", planFileInfo)`。
   - 否则 `const attachmentIndex = Math.floor((iteration - 1) / reminderInterval); if (attachmentIndex % reminderInterval === 0)` → 返回完整版。
   - 其余 → return `planModeSparseReminder.replace("%PLAN_PATH%", planPath)`。
4. 实现 `export function buildPlanModeExitReminder(planPath, planExists): string`:`extra` 在 `planExists` 时为 `The plan file is located at ${planPath}...`,否则 `""`;return `planModeExitTemplate.replace("%EXTRA%", extra)`。
5. 实现 `export function buildPlanModeReentryReminder(planPath, planFileExists): string`:`planFileExists` false 则 return `""`;否则 return `planModeReentryTemplate.replace("%PLAN_PATH%", planPath)`。

**验证:** `bun test tests/plan-mode-reminder.test.ts` 断言 iteration=1 含「Plan mode is active」、iteration=2 含「Plan mode still active」、iteration=6 含「Plan mode is active」(完整)。

## T4: UsageInfo 与 LLMClient 接口**文件:** `src/llm/events.ts`、`src/llm/client.ts`
**依赖:** 无
**步骤:**
1. `src/llm/events.ts`:`UsageInfo` 增加 `cacheReadInputTokens: number; cacheCreationInputTokens: number`;`stream_end` 事件的 `usage` 类型自动跟随更新。
2. `src/llm/client.ts`:确认 `createClient(cfg: ProviderConfig, systemPrompt: string)` 签名;三协议分支 dynamic import 并 `new XxxClient(cfg, systemPrompt)`。

**验证:** `tsc --noEmit` 通过;旧 client 实现暂时编译失败(T5/T6 修复)。

## T5: Anthropic adapter 改造**文件:** `src/llm/anthropic.ts`
**依赖:** T4
**步骤:**
1. `constructor(cfg, systemPrompt)`:把 systemPrompt 存为 `this.systemPrompt`;沿用 `this.model / this.thinking / this.maxOutputTokens / this.contextWindow` 字段。
2. `stream(conv, toolSchemas, abortSignal)`:
   - `const messages = buildAnthropicMessages(conv.getMessages())`。
   - tools 数组从 toolSchemas 映射 `{ name, description, input_schema: { type:"object", properties, required } }`。
   - `if (tools.length > 0)` 把最后一个 tool 的 `cache_control = { type:"ephemeral" }`。
   - `markLastUserTailForCache(messages)`:从后向前找首条 role=user,把其 content 数组末块的 `cache_control` 设为 ephemeral。
   - 构造 `params`:`system: [{ type:"text", text: this.systemPrompt, cache_control: {type:"ephemeral"} }]`、`messages`、`tools`(可选)。
3. 流式收集:`message_start` 事件读 `event.message.usage.input_tokens / output_tokens / cache_read_input_tokens / cache_creation_input_tokens`(`?? 0` 兜底);最后 yield `stream_end { stopReason, usage: { inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens } }`。
4. `buildAnthropicMessages` 已有连续 user 合并逻辑沿用,不改。

**验证:** `bun build src/llm/anthropic.ts` 无错;手工跑 smoke 看 Anthropic 路径 message_start 字段。

## T6: OpenAI / OpenAI-Compat adapter 改造**文件:** `src/llm/openai.ts`
**依赖:** T4
**步骤:**
1. `OpenAIClient.constructor(cfg, systemPrompt)`:存 `this.systemPrompt`;`this.client = new OpenAI({ apiKey, baseURL })`。
2. `OpenAIClient.stream()`:`input = []; input.push({ role:"system", content: this.systemPrompt });` 再 `for (const msg of buildOpenAIInput(history)) input.push(msg)`。`response.completed` 事件 `usage`:`cacheReadInputTokens = usage.input_tokens_details?.cached_tokens ?? 0; inputTokens = Math.max(0, (usage.input_tokens ?? 0) - cacheReadInputTokens); outputTokens = usage.output_tokens ?? 0;`,`cacheCreationInputTokens: 0`。
3. `OpenAICompatClient.stream()`:`messages: [{ role:"system", content: this.systemPrompt }, ...buildChatCompletionMessages(history)]`。chunk.usage 阶段 `cacheReadInputTokens = chunk.usage.prompt_tokens_details?.cached_tokens ?? 0; inputTokens = Math.max(0, (chunk.usage.prompt_tokens ?? 0) - cacheReadInputTokens); outputTokens = chunk.usage.completion_tokens ?? 0`,`cacheCreationInputTokens: 0`。
4. `buildOpenAIInput` 和 `buildChatCompletionMessages` 沿用,不改。

**验证:** `tsc --noEmit` 全部通过;smoke openai 看 cached_tokens 字段(端点支持则 >0)。

## T7: 工具描述双重强化**文件:** `src/tools/descriptions.ts`、`src/tools/bash.ts`、`src/tools/edit-file.ts`
**依赖:** 无
**步骤:**
1. `descriptions.ts`:
   - `BashDescription` 首段确保含「IMPORTANT: Avoid using this tool to run cat, head, tail, sed, awk, or echo commands. Instead use the dedicated ReadFile, EditFile, or WriteFile tools」。
   - `EditFileDescription` 首条 Usage 含「You MUST read the file with ReadFile before editing. This tool will fail otherwise.」
2. `bash.ts` / `edit-file.ts` 各自 `description = BashDescription / EditFileDescription`,不另行修改 schema。

**验证:** `bun test` 涉及工具 schema 的用例通过。

## T8: agent reminder 注入循环**文件:** `src/agent/agent.ts`
**依赖:** T2、T3
**步骤:**
1. 顶部 `import { getOrCreatePlanPath, planExists } from "../planfile/planfile.js"; import { buildPlanModeReminder } from "../prompt/plan-mode.js";`。
2. `run()` 主循环每轮 `iteration++`、`manageContext` 之后,`fireLifecycle("turn_start")` 之前:
   - `if (this.checker.mode === "plan") { const planPath = getOrCreatePlanPath(this.workDir); this.checker.planFilePath = planPath; this.conversation.addSystemReminder(buildPlanModeReminder(planPath, planExists(this.workDir), iteration)); }`
   - `const skillReminder = buildActiveSkillsReminder(this.activeSkills); if (skillReminder) this.conversation.addSystemReminder(skillReminder);`
   - `if (this.hookEngine) for (const note of this.hookEngine.drainNotifications()) this.conversation.addSystemReminder(note);`
   - `if (this.notificationFn) for (const note of this.notificationFn()) this.conversation.addSystemReminder(note);`
3. 文件末追加 `function buildActiveSkillsReminder(active: Map<string, string>): string`:active 空时返回 `""`;否则拼接 `# Active Skills\n\nThe following Skill SOPs are pinned to the environment context. ...\n\n## Active Skill: ${name}\n\n${body}\n\n` × N。

**验证:** `tsc --noEmit` 通过;agent 单测覆盖三类 reminder 各自注入逻辑。

## T9: TUI 与 spawn / teammate 接线**文件:** `src/tui/app.tsx`、`src/agents/spawn.ts`、`src/teammate.ts`
**依赖:** T2、T3、T4
**步骤:**
1. `src/tui/app.tsx`:`initClient` 中 `const env = detectEnvironment(workDir); env.model = provider.model; const systemPrompt = buildSystemPrompt(env); const client = await createClient(provider, systemPrompt); clientRef.current = client;`。
2. `/plan` 进入路径:在已有 plan 文件存在时 `convRef.current.addSystemReminder(buildPlanModeReentryReminder(planPath, true))`。
3. `/do` 退出路径:`convRef.current.addSystemReminder(buildPlanModeExitReminder(planPath, !!planContent))`,两处(主输入分支与 plan-approval 分支均处理)。
4. `src/agents/spawn.ts`:`const env = detectEnvironment(workDir); env.model = resolvedModel; const systemPrompt = definition.systemPromptOverride ?? buildSystemPrompt(env); const client = effectiveModel ? await createClient({ ...parentProvider, model: resolvedModel }, systemPrompt) : parentClient;`。
5. `src/teammate.ts`:`const env = detectEnvironment(process.cwd()); const systemPrompt = buildSystemPrompt(env);`。

**验证:** `bun run src/main.tsx`(或在 tmux 内启动)进入 `/plan` 模式,首轮看到完整提醒、第二轮看到精简提醒。

## T10: PromptBuilder 单测**文件:** `tests/prompt-builder.test.ts`
**依赖:** T1、T2
**步骤:**
1. `import { test, expect } from "bun:test"; import { buildSystemPrompt, detectEnvironment, PromptBuilder } from "../src/prompt/builder.js"; import { identitySection, usingToolsSection } from "../src/prompt/sections.js";`
2. 测试 1:Section 排序——`new PromptBuilder().add(usingToolsSection()).add(identitySection()).build()` 中 identity 出现在 usingTools 之前。
3. 测试 2:跳空 content——传入 `{name:"X", priority:50, content:""}` 不出现、无连续 `\n\n\n`。
4. 测试 3:N1 稳定性——两次 `buildSystemPrompt(fakeEnv)` 结果 `===`(同一 env 字面量)。
5. 测试 4:F5 双重强化——`buildSystemPrompt(fakeEnv)` 含子串「ReadFile」「Bash」「EditFile」。
6. 测试 5:可选 Section——`buildSystemPrompt(env, { skillSection:"S", customInstructions:"C", memorySection:"M" })` 末尾按 Skills / CustomInstructions / Memory 顺序出现。

**验证:** `bun test tests/prompt-builder.test.ts` 全绿。

## T11: plan-mode 单测**文件:** `tests/plan-mode-reminder.test.ts`
**依赖:** T3
**步骤:**
1. iteration=1 → 完整版,含「Plan mode is active」「Phase 1: Initial Understanding」。
2. iteration=2 → 精简版,含「Plan mode still active」、不含「Phase 1」。
3. iteration=6(刚好下一个 attachmentIndex 边界)→ 完整版。
4. `buildPlanModeExitReminder(p, true)` 含「You have exited plan mode」「The plan file is located at」;`buildPlanModeExitReminder(p, false)` 不含后者。
5. `buildPlanModeReentryReminder(p, false)` 返回 `""`;`buildPlanModeReentryReminder(p, true)` 含「You have re-entered plan mode」。

**验证:** `bun test tests/plan-mode-reminder.test.ts` 全绿。

## T12: 全量类型检查与测试**文件:** —
**依赖:** T1–T11
**步骤:**
1. `tsc --noEmit`——无任何报错。
2. `bun test`——`tests/` 全部用例通过(含 ch04 用例不退化)。
3. 在 tmux 内手工启动 `bun run src/main.tsx`,验证三个端到端场景:
   - 同一会话连发两条消息(Anthropic 路径),观察日志 / debug 打印的 `cacheCreationInputTokens / cacheReadInputTokens`。
   - 进入 `/plan`,在多轮调研后退出 `/do`,确认提醒详略按 iteration 切换。
   - 在非 git 目录启动,确认 `# 环境` 段无 Git 分支行、不报错。

**验证:** 全部通过;输出无 api_key 明文。

## 执行顺序

```text
T1(sections)─┐
             ├─→ T2(builder) ─┬─→ T10(prompt 单测)
             │                └─→ T9(TUI/spawn/teammate 接线)
T3(plan-mode)──→ T11(plan-mode 单测)
                  ↘
T4(UsageInfo/client) ─┬─→ T5(anthropic) ─┐
                      └─→ T6(openai)    ─┤
T7(工具描述,独立)                         │
T1,T2,T3,T4 ─→ T8(agent) ────────────────┴─→ T12(全量类型 / 测试 / tmux 端到端)
```
````

```markdown
# 系统提示工程化 Checklist

> 每一项通过运行代码或观察行为来验证,聚焦系统行为;括号内为验证方式与对应需求。

## 实现完整性
- [ ] 模块化装配:`buildSystemPrompt(env)` 输出按 Identity(0) → System(10) → DoingTasks(20) → ExecutingActions(30) → UsingTools(40) → ToneStyle(50) → TextOutput(60) → Environment(70) 顺序拼接,以 `\n\n` 分隔(验证:`tests/prompt-builder.test.ts` 断言 Identity 段出现在 UsingTools 段之前、各段以空行分隔)。(AC1/F1)
- [ ] 挂载即扩展:`new PromptBuilder().add({...自定义 Section})` 后 build,该 Section 落在与其 priority 对应的位置,不改 PromptBuilder.build 主逻辑(验证:`tests/prompt-builder.test.ts` 传入额外 Section 断言其位置)。(AC1/F1)
- [ ] 可选空槽:`buildSystemPrompt(env)`(无 opts)输出不含 Skills / CustomInstructions / Memory 段、无连续空行;传入 `opts.skillSection` 等之一才出现(验证:`tests/prompt-builder.test.ts` 断言)。(AC2/F1)
- [ ] 缓存确定性:连续两次 `buildSystemPrompt(sameEnv)` 字符串严格相等;`AnthropicClient.this.systemPrompt` 跨轮不变(验证:`tests/prompt-builder.test.ts` 双调用断言 `===`;手工 console.log 两轮 `params.system[0].text` 相等)。(AC5/F3/N1)
- [ ] 环境信息呈现:`environmentSection(env)` 在系统提示尾部,输出 `# 环境` 段含工作目录 / 平台/架构 / Shell / 是否 Git 仓库 / 日期;git 仓库下含 Git 分支;model 非空时含模型行(验证:`tests/prompt-builder.test.ts` 断言 Render 含各行)。(AC3/F2)
- [ ] 双重强化:`usingToolsSection().content` 含「有专用工具时绝不要用 Bash」+ ReadFile / EditFile / WriteFile / Glob / Grep 对应表;`BashDescription` 含「Avoid using this tool to run cat, head, tail...」;`EditFileDescription` 含「You MUST read the file with ReadFile before editing」(验证:`bun test` + 直接 import descriptions 断言子串)。(AC7/F5)
- [ ] 补充消息注入机制:`addSystemReminder("X")` 后 `getMessages()` 末条 role=user、content === `<system-reminder>\nX\n</system-reminder>`(验证:`tests/conversation.test.ts` 已断言;新增断言精确字符串匹配)。(AC8/F6)
- [ ] 缓存字段解析:`UsageInfo` 含 `cacheReadInputTokens / cacheCreationInputTokens`;Anthropic 路径 `message_start` 事件读 `cache_read_input_tokens / cache_creation_input_tokens`;OpenAI Responses 读 `input_tokens_details?.cached_tokens`;OpenAI Chat 读 `prompt_tokens_details?.cached_tokens`;缺字段 `?? 0` 不抛(验证:三协议 smoke 跑两轮观察字段)。(AC6/F4/N6)
- [ ] Anthropic 缓存断点真实发出:`params.system[0].cache_control === { type:"ephemeral" }`、`params.tools[last].cache_control === { type:"ephemeral" }`(若有 tools)、`messages` 末条 user 的 content 末块 `cache_control === { type:"ephemeral" }`(验证:`tests/anthropic-client.test.ts` 序列化 params 断言)。(AC4/F3)

## 集成
- [ ] 规划模式按轮次注入:iteration=1 注入完整版(含「Plan mode is active」+ 5 阶段工作流);iteration=2 注入精简版(「Plan mode still active」);iteration 命中 `floor((iter-1)/5) % 5 === 0` 重复完整版;reminder 进入持久 history(验证:`tests/plan-mode-reminder.test.ts` 各 iteration 断言 + `tests/agent.test.ts` 多轮脚本)。(AC9/F6/F7)
- [ ] 规划模式工具集:规划模式下 `checker.planFilePath` 被设置;ExitPlanMode 工具可调,其他写工具被 PermissionChecker 拦截(验证:`bun test` 涉及 plan-mode permissions 的用例通过)。(AC9/F7)
- [ ] 稳定系统提示跨模式一致:`/plan` 与 `/do` 切换时 LLMClient 不重建,`this.systemPrompt` 不变(验证:`tests/app.test.tsx` 或手工断点)。(F7/N1)
- [ ] 历史合法:`addSystemReminder` 写入连续 user 后 `buildAnthropicMessages` 自动合并成多 TextBlock 单条 user,Anthropic API 不报 400(验证:`tests/conversation.test.ts` + Anthropic smoke 多轮)。(AC12/N3)
- [ ] 跨协议一致:Anthropic / OpenAI / OpenAI-Compat 三 client 都接收同一份 `systemPrompt` 字符串;`UsageInfo` 字段三家都填充(验证:三 client 单测各跑一次断言 systemPrompt 字符串一致)。(AC10/F8)
- [ ] 不破坏 ch04:多轮连环、用户取消(`abortSignal`)、`ContextTooLongError` 自恢复、自动压缩、`maxTokens` 升级均成立(验证:`bun test` 全量通过)。(AC11/N2)
- [ ] 环境采集降级:在非 git 临时目录 `detectEnvironment` catch 分支生效,`isGitRepo=false / gitBranch=""`,函数返回正常 EnvironmentContext 不抛(验证:`tests/prompt-builder.test.ts` 用 tmp 目录调用)。(AC13/N4)
- [ ] 界面不阻塞:`detectEnvironment` 通过 `execSync` 同步跑 `git rev-parse` 两条短命令,初始化只调一次,不阻塞 TUI 渲染(验证:正常 git 仓库内启动 TUI 观察启动延迟无异常)。(N4)

## 编译与测试
- [ ] `tsc --noEmit` 无错误。
- [ ] `bun test` 通过(`tests/prompt-builder.test.ts`、`tests/plan-mode-reminder.test.ts`、`tests/conversation.test.ts`、`tests/agent.test.ts` 等)。
- [ ] `bun build src/main.tsx --target bun --outdir /tmp/guolaicode-build` 编译无错(可选,验证 bundle 不报错)。
- [ ] 文件后缀检查:所有 import 在 TS 源码中带 `.js` 后缀(ESM 规范),`grep -nE "from \"\\./.*[^.][^j][^s]\"" src/prompt/` 无命中。
- [ ] 密钥不回显:对话区、`# 环境` 段与任何输出均不出现 api_key(验证:通读输出、`grep -i "api[_-]key" .guolaicode/`无明文;确认 `detectEnvironment` 不读 `ANTHROPIC_API_KEY / OPENAI_API_KEY`)。(AC14/N5)

## 端到端场景(tmux 内 `bun run src/main.tsx` 实跑)
- [ ] 场景 1(缓存命中,Anthropic):同一会话连发两条消息 → 在 debug 日志或临时 console.log 中观察首轮 `cacheCreationInputTokens > 0`、次轮 `cacheReadInputTokens > 0`,证明 system + tools + 历史前缀被 ephemeral 缓存复用。(AC4/F3)
- [ ] 场景 2(规划模式按轮次):`/plan` 发一个需多步只读调研的任务 → 模型仅用只读工具(ReadFile/Glob/Grep/Agent)产出计划;首轮注入完整提醒、第二轮注入精简提醒、第六轮再次完整;`/do` 切回全工具并按 plan 文件执行。(AC9/F7)
- [ ] 场景 3(reminder 不被当用户输入):规划模式下模型不复述 / 不直接回应 `<system-reminder>` 内容,而是按其约束行事(只读、5 阶段)。(AC8/F6)
- [ ] 场景 4(环境感知):问「我现在在哪个目录、什么平台、今天几号」→ 模型据 `# 环境` 段正确回答工作目录、`platform/arch`、ISO 日期。(AC3/F2)
- [ ] 场景 5(取消后可继续):规划模式多轮调研中途按 Esc 取消 → 回空闲态、再发一条继续对话;Anthropic 请求不返回 400(`buildAnthropicMessages` 合并保护)。(AC12/N3)
- [ ] 场景 6(非 git 目录降级):在非 git 目录(如 `/tmp`)启动 `bun run src/main.tsx` → 启动正常、`# 环境` 段不含 Git 分支行、`isGitRepo: false`、对话可正常进行。(AC13/N4)
```



